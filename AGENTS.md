# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Cruise FM — project context

A premium driving-companion app (React Native / Expo SDK 56, Expo Router). Users bring their own Spotify playlists; Cruise FM wraps them in a cinematic driving experience — mood stations, full-screen visual modes, atmosphere. "Spotify organises by artist/genre; Cruise FM organises by how a drive feels."

The owner does not code — describe changes in plain English, Claude implements everything. Keep explanations non-technical.

## Product decisions (fixed)
- Monetisation (locked 2026-07-22): £1.99/mo + £14.99/yr annual ("save 37%"), 7-day free trial; optional launch-week Founder lifetime £24.99 one-time. Launch FREE first, payments as fast-follow update. (RevenueCat SDK wired: sandbox test_ key in config.ts, entitlement "premium", safe no-op on web/old builds via src/utils/purchases.ts; real store products + keys still pending)
- FREE tier: Cassette + Equalizer modes, basic playback, limited custom stations (3), badges
- PREMIUM: Vinyl + Retro Radio modes, all mood themes, unlimited playlists, future additions
- Never premium: offline listening, badges, founder cosmetics, seasonal themes
- Live song titles are Spotify-only (decided 2026-07-23): companion-mode listeners (YouTube Music etc.) see the station's mood tagline — Google exposes no now-playing API. Possible future: Android media-notification reader (native module, fresh build, Android-only); iOS has no equivalent.
- `OWNER_MODE` in src/constants/config.ts bypasses all locks during development — set FALSE for launch (2026-07-23). `LAUNCH_FREE = true` (same file) is the public launch state: everyone premium, no locks/paywall, Sentry live; flip LAUNCH_FREE off only when real store products + platform RevenueCat keys ship (payments fast-follow)

## Architecture map
- `src/app/(tabs)/` — cruise (home w/ one-tap Start Drive), stations, modes, profile
- `src/app/premium.tsx` — amber paywall screen; `src/app/auth.tsx` — Spotify OAuth redirect handler (cruisefm://auth)
- `src/components/{Equalizer,Cassette,Vinyl,RetroRadio}Mode.tsx` — full-screen visual modes; all four share the same shell: full-bleed station image (blurRadius 3.5, `imageStyle={{width:'100%',height:'100%'}}` — REQUIRED, web falls back to intrinsic size without it), 5-stop dark gradient capped ~0.58, glow band tinted from station eqColors, "PLAYING FROM" header, bottom-left song title, white 6px progress bar, white 80px play circle (custom 8x30 pause bars), MaterialCommunityIcons skip-previous/next 48
- `src/components/StationDetailModal.tsx` — station page: full-bleed image, Add your playlist (Spotify picker), mode picker, Start Drive
- `src/constants/stations.ts` — 8 stations; each has cardGradient (muted card preview), eqColors (equalizer bar palette), iconName (white MCI icon)
- `src/utils/spotify.ts` — OAuth PKCE + playback API; `useSpotifyPlayback.ts` — live play/pause/skip + 5s now-playing poll used by all modes; `stationPlaylists.ts` — per-station linked playlist (AsyncStorage); `lastCruise.ts` — resume logic
- `src/components/DriveCheckCard.tsx` — "Are you driving?" honesty check: 45 min of untouched playback → card; ignored for 2 min → drive clock pauses (driveStats suspend/resume) until the next playback touch. Playback controls signal life via activityPing in NowPlayingContext
- `website/index.html` — self-contained waitlist landing page (Formspree form ID still a placeholder)
- Credentials in `.env` (EXPO_PUBLIC_SPOTIFY_*) — gitignored, copy manually between machines

## Gotchas learned the hard way
- Never define background JSX as an inline component inside render — new identity each render remounts the blurred image (visible twitching). Use a plain JSX const.
- Station images: assets/stations/*.jpg, must be ≥ ~1100px on the short side or web renders them at intrinsic size (bottom cut-off). Compress to ≤ ~600KB, JPEG only (no .avif/.webp/.png — bundler/perf). Source from Unsplash/Pexels, never Pinterest.
- Expo Go is a dead end (SDK 54 vs our 56 + no custom deep links). Android testing = EAS dev build; iOS on Windows blocked (needs paid Apple acct) — use Safari at the LAN IP for visuals, or the iOS Simulator on a Mac.
- Spotify playback API needs the user's Spotify app active on some device, and Premium.
- Spotify quota policy (confirmed 2026-07-18): extension requests are ORGANIZATIONS-ONLY since May 2025 and require an established business w/ 250k+ MAU — unreachable at launch. Dev-mode allowlist confirmed at 5 users (+ the owner account) — reserve slots for genuine music-testing testers; everyone else runs demo mode. Before public launch, build the no-API fallback: Start Drive deep-links the linked playlist into the Spotify app (spotify: URI, no auth needed) for non-allowlisted users; Cruise FM stays the visual layer.

## Current state / next steps
- DONE: all visuals unified, Spotify OAuth working on Android build, playback CONFIRMED WORKING on device (2026-07-18: playlists load, music plays, controls obey; play uses active-device fast path, failures surface via PlaybackNotice card)
- DONE 2026-07-19: sandbox purchase CONFIRMED on device (free-user preview → locks appear → Unlock Premium → RevenueCat test purchase unlocks → Restore purchases works)
- DONE 2026-07-20: EAS Update (OTA) wired — expo-updates ~56.0.22 installed; app.json updates.url = https://u.expo.dev/<projectId> + runtimeVersion policy "fingerprint"; eas.json preview/production build profiles carry channel "preview"/"production". Default release behaviour = check-on-launch, apply-on-next-launch (no runtime code added). REQUIRES ONE FRESH BUILD before OTA works — current installed builds lack the expo-updates native module and will NOT receive updates. Owner runs on their machine: (1) `eas build -p android --profile preview` once to bake in expo-updates; after that, JS/asset-only changes ship via `eas update --branch preview --environment preview -m "<note>"` (minutes, no store, no reinstall). Fingerprint policy auto-blocks OTA when native deps change → that's the signal a new build is needed, not an update. TRAP (bitten 2026-07-23): `eas.json` is a fingerprint source — ANY edit to it silently changes the runtime version, so existing builds stop receiving updates. Treat eas.json/app.json/package.json edits as "fresh build required" events; after such an edit, cut a new testflight-profile build for the owner's phone so preview OTA keeps flowing. **SUPERSEDED 2026-07-30** — the fingerprint policy is gone (it had started disagreeing with EAS's own build-time fingerprint, see the pinned-runtime entry below); everything in this entry about config files silently changing the runtime version no longer applies.
- DONE 2026-07-20: honest handoff — non-allowlisted drives that hand music to the Spotify app now swap the dead transport for a "Playing in Spotify" panel (src/components/HandoffOverlay.tsx, dropped into all 7 modes; `handoff` flag + `returnToSpotify` on NowPlayingContext). Price dropped £2.99 → £1.99 everywhere.
- DONE 2026-07-20: mic-reactive visuals (v1, Equalizer only) — expo-audio ~56.0.12 installed; app.json expo-audio plugin carries the mic permission string; `useMicLevel(active)` (src/utils/useMicLevel.ts) meters live loudness in `mixWithOthers` mode so it never interrupts Spotify, normalises dBFS→0..1, eases it, and returns available:false on web/denial → modes keep their timed animation. Setting `micReactive` (default ON) on MotionContext + Profile toggle; privacy clause added to legal.ts. Wired into EqualizerFullscreen bars (timed loop preserved as the fallback) + a reactive glow bloom. CONFIRMED ON DEVICE 2026-07-20: mic level tracks the music (owner-only "mic 0.xx" readout in EqualizerFullscreen) AND Spotify keeps playing cleanly (mixWithOthers works). NEEDED ONE FRESH BUILD for the native mic module; all subsequent glow/reactivity tuning ships via OTA. Shared `src/components/MicGlow.tsx` (self-contained: owns the hook + micReactive setting, renders null unless live loudness is available) now drops the reactive glow into all other modes (Cassette/Vinyl/Tuner/Horizon/SoundWave/CircularWave) via one line each above the HandoffOverlay.
- DONE 2026-07-21/22 (iPhone polish round, all OTA): mic-reactive now DEFAULT OFF (iOS: recorder + Spotify contend for the audio session — confirmed on device; smart auto-pause `micQuietTick` hush built but mic stays opt-in in Profile); ModeCloseButton chevron on all 7 modes (iOS dismiss gesture unreliable); station-owns-its-playlist rule — playStationMusic never resumes stray music: no linked playlist → pause + 'no-playlist' notice ("add a playlist"), StationDetailModal glows Add-Playlist for ANY unlinked station; mini-player ✕ now pauses Spotify; changing a station's playlist takes effect immediately (relinkStationPlaylist); keep-awake for whole drive via NowPlayingContext; SpotifyNudgeCard home tip (connected users, "wake Spotify first", dismissible); Vinyl/Cassette vertical layout aligned with other modes; settings pages swipe-right-to-back (SettingsPageShell PanResponder) + honest Account page (no fake "coming soon" — working two-tap Delete My Data: disconnectSpotify + AsyncStorage.clear).
- iOS DISTRIBUTION (2026-07-22): Apple Dev enrolled (cruisefmservice@gmail.com, team JESSICA ARROYO). eas.json `testflight` profile = store distribution on the PREVIEW channel → one `eas update --branch preview` feeds owner device + all TestFlight testers. App record "Cruise FM" created in App Store Connect (SKU cruisefm). Build succeeded; next: `eas submit -p ios --profile testflight --latest`, external tester group (~1-day Beta App Review), invite friends. Guide: docs/launch/testflight.md. LAUNCH PLAN: iOS public launch this week (launch FREE first, £1.99 sub as fast-follow); Android blocked by Google's 14-day closed test — follows later.
- DONE 2026-07-22 (launch-prep round 2, all OTA): mic-reactive visuals REMOVED entirely (Bluetooth distortion while driving — owner call; MicGlow/useMicLevel/micSettings deleted, Profile toggle gone, Equalizer glow now a calm timed breathe, legal.ts mic clause now "does not use the microphone"; expo-audio stays installed/plugin so the fingerprint — and OTA to existing builds — is unchanged; drop the plugin at the next fresh build); Recommended-card text shadow removed; STRICT playlist gate — StationDetailModal Start Drive on an unlinked station opens the playlist picker instead ("Add a Playlist to Start"); Create Station sheet restyled (deep-navy #060812, neutral glass washes, 16 colours, glowing deep-blue preview card); PlaylistSheet wrapped in KeyboardAvoidingView (paste bar was under the iOS keyboard); proactive wake-Spotify nudge — drive-start and play surface the "still waking Spotify" notice if no verdict after 2.5s (showWakeNudge on NowPlayingContext), cleared automatically by a 'playing' verdict.
- DONE 2026-07-22 (evening polish, all OTA, owner device-testing throughout): smoke-machine AmbientGlow (3 radial hazes breathing out of phase + ~100BPM beat plume gated on real is_playing) in 6 modes; drag-to-seek EVERYWHERE (useTrackClock scrub API + shared SeekBar; Cassette adapter; Vinyl keeps record-scrub); Tuner dial claims touches on start (iOS lost move-negotiation — dial classifies tune-vs-dismiss itself; DialRuler Svg pointerEvents none); cassette reels restart on reopen-from-mini-player; instant wake-Spotify notice on every start/play (default, cleared by 'playing' verdict) + stale-resume re-check at 4s; previews resume any song (strict playlist rule exempted via resumeAny) + 30s gate "Like what you're seeing?"; settings back = edge-swipe with capture-on-MOVE (capture-on-start killed the back button — reviewed & fixed) ; smooth re-entry (AppState foreground → instant Spotify re-sync + adopt real play state); battery pass (paused scenes freeze, mini-player polls 12s, scene loops 25fps); AUTO-DIM while driving (30s untouched → brightness 0.22, tap wakes via invisible catch layer, restores on every exit path; Profile toggle default ON); Circular EQ kicks on simulated beat; premium modes stacked under PREMIUM label in Modes tab; freemium split confirmed: free = Cassette/Equalizer/SoundWaves (SW is the right giveaway — redundant with EQ; premium keeps the 4 distinct worlds). Pre-launch code sweep 2026-07-22: full-day diff reviewed (1 bug found+fixed: edge-capture vs back button), web smoke sweep green, tsc clean.
- WEBSITE LIVE 2026-07-24 1:37pm (pre-launch page: "Launching this week" badge, waitlist forms, real-screenshot showcase). DEPLOY METHOD = MANUAL: Netlify git auto-deploy was abandoned — free plan blocked builds with "unrecognized Git contributor" (Claude's commits) and kept the block cached even after the repo went public (2026-07-24). Repo was unlinked from the Netlify site; owner deploys by dragging the local `website` folder onto the cruisefm site's Deploys-tab drop zone (git pull first!). netlify.toml remains in the repo, harmless, useful if git-linking is ever retried. RELEASE DAY: Claude flips index.html to launch mode (App Store button markup in git history, commit "Launch-mode landing page"), owner pulls + drags `website` once more. Repo is now PUBLIC (owner's choice, no secrets tracked).
- SUBMITTED TO APP REVIEW 2026-07-23: build 7 (production channel, launch-free state), status Waiting for Review. Manual release selected — owner presses Release after approval. Price free (base AUD 0), category Music/Lifestyle, age 4+, privacy = crash data only, Mac/Vision Pro availability unticked. Screenshots 1284x2778 in screenshots-appstore/ (source in screenshots-raw/). IMPORTANT while in review: do NOT `eas update --branch production` (changing the app mid-review); preview-channel updates for the owner's phone remain safe.
- DONE 2026-07-24 (waiting-room polish day, all OTA): cassette realism (living tape packs driven by track progress, take-up reel spins faster, tape ribbon+guides, capstan/pinch holes, Phillips screws, translucent colour shell w/ sharper corners, no window frame, pulsing orb REMOVED); vinyl (album-art label at 40%, silver faint-J tonearm w/ counterweight+anti-skate dial, groove-riding needle, mood ring got pointerEvents none — it had EATEN every record tap/scrub since 22.07); atmosphere transition-hold (beat rests 2.2s on song-title change, all 7 modes + Circular EQ's own kick) + Profile "Atmosphere" toggle (default on, AmbientGlow renders null when off); honest home stats (drives count only at 2+ banked minutes); playlist pill names the ACTUALLY-playing playlist (Spotify context + cached name lookup); home header EQ wakes on real Spotify playback (15s poll, focused only); MarqueeText ghost-measurer (iOS ellipsized before measure — pan never fired); PreviewGate moved IN-MODE (iOS won't stack a second Modal window — card never appeared; silences via the mode's own spotify.pause); CPU diet: scene rAF loops 25→15fps after iOS cpu_resource reports showed 53-59% sustained CPU (deeper memoization is the follow-up if reports persist — check Settings>Privacy>Analytics Data for new CruiseFM.cpu_resource files).
- DONE 2026-07-24 (late night, all OTA): new "Disco Ball" visual mode (owner's idea) — `src/components/DiscoBallMode.tsx`, premium, registered in modeCatalog/NowPlayingHost/Modes tab (icon `mirror-variant` — `disco-ball` glyph doesn't exist in MaterialCommunityIcons). Chunky geodesic cel-shaded mirror tiles (seam-tiling scroll trick for a true axis-spin with zero seam), station-mood colour-cycling wash (uses each station's own eqColors, not a fixed palette), two prominent on-ball sparkle stars + an orbiting light field, gentle hang/float. Iterated twice against owner references (video style match) before landing. Also: Vinyl mode fix — the album label wasn't actually desynced from the disc (both share one rotation value, mathematically impossible to drift), it just LOOKED slower because the disc's surface was almost pure concentric circles with no visual rotation cue; added small asymmetric scuff/fleck texture marks so the disc's spin reads as clearly as the label's, plus grew the album-art label ~12-13% (both fullscreen deck and preview card).
- DONE 2026-07-24 (Disco Ball premium visualizer pass, OTA): kept the stylized cel-shaded ball (not photoreal) but raised production value — beveled tiles (offset shadow + bright inner rim per facet), four independent neon cyan/magenta/purple/blue reflection streaks drifting across the surface (own speed/phase each, layered above the mood-colour wash), bloom halo bleeding outside the ball's own clip (layered-gradient trick, no real blur filter available), volumetric light shafts + floating bokeh particles in the room, a whisper-quiet lens flare, spin slowed 9s→14s/rev (reads as a real hanging ball, not a toy spinning fast). Glow/reflections now nudge gently on the beat (same simulated ~100BPM pattern as AmbientGlow, gated on real playback). All native-driver opacity/transform only, no new dependencies — normal OTA.
- DONE 2026-07-25 (Disco Ball round 4 — owner reference photo, OTA): the facets read as a flat brick wall because the old surface was a flat texture strip scrolling behind a circular mask — no perspective baked in at all. Rebuilt as a GENUINE SPHERE PROJECTION: each mirror is a quad between two latitudes/longitudes, projected through the sphere with a ~17° downward viewing tilt and back-face culled, so rows compress toward the poles, columns converge at the top pole, facets squash to slivers at the silhouette. Per-facet Lambert shading off each tile's own normal, quantised into a ramp (stylised, not photoreal) + a minority of mirrors given a neon cast. ~240 facets (ROWS 17 × COLS 32 after culling), built once, NEVER animated. KEY INSIGHT: the grid is now static and the light moves behind it — cheaper AND truer, since on a real mirror ball the grid barely appears to move; what travels is the light across the facets (scrolling patch layer beneath, carved into mirrors by the grid's seams). Also: ball turns ONLY while audio genuinely plays and coasts to a stop when it stops (resumes from where it left off — `spin` must stay 0..1 or it scrolls off its own two-copy strip, so every target is clamped); spin 14s→9.5s/rev; the two 4-point sparkle stars REMOVED (owner: "doesn't enhance the style"); room sparkle reworked into star dust (fine glitter, hairline cross-flares, densest around the ball, coarser particle layers dialled back to make room); specular highlight softened from a hard ellipse to a gradient (it was sitting on the ball like a painted blob and hiding the mirrors).
- DONE 2026-07-25 (OTA): BOTTOM-SEAM BUG FIXED (owner spotted a faint cut-off line near the home indicator "on almost every screen") — AmbientGlow's `ag.wrap` had `overflow:'hidden'` with `top: SCREEN_H*0.34 + height: SCREEN_H*0.62`, i.e. a hard clip at EXACTLY 0.96×screen height; the hazes breathe up to scale 1.18, so the scaled-up smoke was sliced against that edge into a straight horizontal line. Present in all 8 visual modes, hence "everywhere". Fixed by running the layer to `bottom: 0` with NO clip (smoke fades out on its own gradient; the screen edge is the only clip wanted) — comment left in the file, never re-add overflow:hidden there. Also: Recommended cards (StationCard CompactCard, used only by the home strip) restyled from a full-strength accent gradient slab to smoked glass — `rgba(15,17,29,0.55)` base + accent palette at 0.42 opacity, hairline rim 0.14, softer/deeper shadow (0.32/26) instead of the neon halo; variants rendered and compared before picking.
- DONE 2026-07-25 (Disco Ball round 5 — owner feedback + reference clip of a REAL mirror ball, OTA): bob REMOVED (hangs dead still now); turn 9.5s→7s/rev and REVERSED so the surface travels right — that direction is load-bearing, it makes "drag right = scrub forward" agree with the playback direction instead of fighting it; SWIPE-TO-SCRUB on the ball (like vinyl): surface tracks the finger 1:1, a ball-width of drag = 1/5 of the song, release seeks Spotify, only horizontal drags claimed so a downward swipe still dismisses, auto-turn suspends while a finger is down and resumes from the angle left behind. Facet CONTRAST raised so rotation actually reads (brighter/tighter hot spots, darker base, less opaque tile fills, WASH_PEAK_OPACITY 0.6→0.44 — the mood tint was flattening the facets into one flat colour). The owner's reference clip CONFIRMED the round-4 architecture: on a real mirror ball the grid barely appears to move and what travels is the flashes across the mirrors, which is exactly the static-grid + scrolling-light model. Light strip now drawn over 4 periods (k=-1..2) not 2, so the wrap is exactly seamless at the silhouette. NOTE for future rounds: the straight vertical line down the ball's centre is NOT a bug — it's the front meridian, which correctly projects to a straight line (measured: tiles either side are 123.9 vs 127.5 luminance, no discontinuity); don't "fix" it.
- DONE 2026-07-25 (Disco Ball round 6 — "still doesn't spin", OTA): it WAS spinning; it was invisible. Measured properly instead of guessing — rendered the composite offscreen at turn 0 vs turn 0.25 and diffed: median |Δlum| was 6/255, only 9% of the ball changing >20. Three FLAT layers were sitting on top of the travelling light and cancelling it: ColorCycleWash (solid slab, 0.44→0.22), dbShade's inner WHITE stop (flat 0.5 sitting exactly where the light travels → 0.12; the dark outer stops make the ball read round so they stayed), and the dbHot specular (0.5→0.26). Tile fills lowered too (0.28+0.38·nz → 0.18+0.26·nz) since they must pass light through, and FOUR large soft washes (lpBig) added to the scrolling layer — the small hot spots give per-mirror sparkle but are too fine to carry rotation on their own. Re-measured: median 17/255, 44% changing >20 (~3×). Timing/gating/geometry untouched. LESSON: for "is it animating?" questions on this ball, measure the composite diff — don't eyeball layer opacities. Also note the spin gate `playing && !musicSwitching && (track?.isPlaying ?? true)` is the SAME expression driving AmbientGlow's beat, which the owner has confirmed works — so the gate is sound.
- DONE 2026-07-25 (Disco Ball rounds 7-8 — THE SPIN BUG, finally found by MEASURING, OTA): after three rounds of "still not spinning" that I wrongly treated as a visibility problem (tuning wash/shade/tile opacity — all wasted), the owner's screen recording settled it. METHOD, reuse this: extract frames with the ffmpeg bundled in imageio-ffmpeg (`/usr/local/lib/python3.11/dist-packages/imageio_ffmpeg/binaries/ffmpeg-linux-x86_64-v7.0.2`; no system ffmpeg, and Chromium can't decode HEVC), then diff regions with PIL/numpy. A temporary SPIN_DEBUG strip (a green marker driven by the SAME spin value + a text readout of the gate flags) made the value observable; tracking it BY COLOUR gave the phase over time — but mask the marker's own row band, or the static green TEXT drags the centroid and fakes a frozen reading. Result: phase frozen at 0.851 for 4s while the gate said SPIN1, then a play/pause tap revived it and it ran at exactly 6.95s/turn. Ball pixel std: 0.43 frozen vs 13.0 running. ROOT CAUSE: resuming called `spin.stopAnimation(cb)` to read the current value — ASYNC for a native-driven value (NativeAnimatedAPI.getValue) — and everything that started the next animation lived inside that callback, so one dropped answer stopped the ball forever. FIX: the turn is linear at a known duration, so the phase is now derived from the wall clock (phaseRef/runStartRef/turningRef/readPhase) and NEVER read back; every branch starts an animation synchronously. NEVER reintroduce a native value read-back on this path.
- DONE 2026-07-25 (Disco Ball round 9 — the grid itself now turns, OTA): owner could see the light travelling but not the tiles. A sphere on its polar axis maps latitude circles onto themselves, so horizontal rings correctly DON'T move — only meridians do. KEY TRICK: a meridian's projection is the ball's own profile curve scaled horizontally by sin(longitude) plus a vertical shear proportional to that scaled x — i.e. `transform: [{skewY}, {scaleX}]`, and BOTH are in RN's native-driver SUPPORTED_TRANSFORMS allowlist (checked node_modules/react-native/Libraries/Animated/NativeAnimatedAllowlist.js). So the whole rotating grid is 24 animated views, not hundreds of animated facets. Verified by overlaying the transform output on the true projection — pixel-identical at every angle except within ~0.3° of centre, where the meridian is a straight vertical line anyway (clamp the skew to ±89° and give scaleX a 0.002 epsilon; a zero scaleX is a degenerate matrix). REJECTED on the renders, don't retry blind: (a) animating every facet as a moveable rectangle — poles break into overlapping blocks, silhouette goes ragged, because axis-aligned rects can't be the converging quads; (b) smoothing the fills so only seams move — becomes a wireframe globe and loses all mirror character.
- DONE 2026-07-25 (MIRROR BALL round 10 — renamed + beveled facets + its own icon, OTA): mode RENAMED "Disco Ball" → "Mirror Ball" everywhere user-facing (modeCatalog label, MODE_META, Modes-tab card title, the mode's own header) — the id stays `disco` and the file stays `DiscoBallMode.tsx`, because saved cruises and per-station picks store the bare string. Ball enlarged ~14% (`Math.min(winW*0.71, winH*0.39, 340)`). SURFACE TEXTURE: each mirror is now TWO quads, not one flat fill — a darker full-tile frame plus a brighter inner face inset ASYMMETRICALLY toward the light (`BEVEL = {u0:.13, u1:.79, v0:.21, v1:.87}`, thin lit rim top/left, thick shadow bottom/right; a symmetric inset just reads as smaller squares). Inner face found by bilinear interpolation of the tile's own four projected corners. 478 static paths, still built once and never animated, so no per-frame cost. Jitter 1.7→2.1. Combined opacity kept near the old 0.44 ceiling so the travelling light still shows through (that ceiling is what round 6 measured — don't raise it). BRICK BOND added at the same time (`bond = (j%2)*0.5` on longitude): with visible tile edges the 32 static columns were stacking into continuous vertical lines that competed with the 24 rotating meridians for "the grid that turns" — offsetting alternate rows kills the static verticals AND is how real mirror balls are built. Verified by rendering the real builders at three rotation phases with true-projection meridians overlaid. NEW ICON: `src/components/MirrorBallGlyph.tsx` — MCI has no mirror-ball glyph (`mirror-variant` reads as a hand mirror), so it's the same sphere projection at ROWS 7 × COLS 12 with each quad shrunk to 0.82 (the GAP is the grid; hairline strokes vanish below 1px), plus a hanging stem. Static, memoized, legible at 17px in the mini-player. CARD: frosted silver glass instead of the purple slab — white/blue-white gradient on the DIAGONAL (a neutral wash only reads as metal if it falls off corner-to-corner), bright rim, cool outward shadow on a WRAPPER view (the card clips its own children, and on iOS that clips the shadow too), plus 16 twinkling specks walking the card's PERIMETER — glitter scattered at random lands on the title and reads as dirt.
- DONE 2026-07-25 (Mirror Ball round 11 — the two owner reference photos, OTA): the round-10 bevel was structurally right but visually inverted. Fat frame (a third of each tile) + one jittered brightness for frame and face = small squares floating on a grey sphere; the references show the opposite — the mirror is nearly the whole tile and the seams are HAIRLINES. Three changes, all in `buildSphereTiles`: (1) `BEVEL` tightened to {u0:.10, u1:.94, v0:.06, v1:.90} so the frame is ~10% not ~34%; (2) frame and face now shade by DIFFERENT rules — the frame is continuous metal so it takes smooth lambert with NO jitter and a +2.6 lift (bright grid on the lit side, dark on the shadow side), while each mirror reflects its own bit of the room so the face takes lambert ±2.6 steps of jitter (neighbours land wildly apart). Driving both from one value was what made the whole surface speckle together and lost the lit grid; (3) colour on nearly every tile (0.10–0.44 cast from a palette of the station's three eqColors + the four NEONs) instead of the old 26% minority at a fixed 0.28 — `buildSphereTiles` now takes `eq` and memoizes on it. Plus a station-coloured rim light at the silhouette, and the latitude arcs dropped 0.45→0.26 (they were cutting through the newly-lit grid). CRITICAL: the frame path is the outer tile AND the face quad in one `d` with `fillRule="evenodd"`, so it paints only the border. As a full quad under the face its opacity lands on the WHOLE ball and cancels the travelling light — the round-6 failure exactly. Measured after: median |Δlum| between quarter-turns 36/255 with 70% of the ball changing >20, vs round 6's post-fix 17/255 / 44%, so the turn reads roughly twice as strongly as the version the owner signed off.
- DONE 2026-07-25 (Mirror Ball round 12 — "the tiles don't seem to be moving", OTA): round 11 caused it. Giving every tile a BRIGHT hairline frame drew each tile's VERTICAL edges too — and those are static meridian seams, which directly contradict the meridians that rotate. Bright and perfectly regular, they became the most legible structure on the ball and pinned the whole surface; the eye locks onto whatever doesn't move. NEW MEASUREMENT, reuse it for any "is the grid turning?" question (the round-6 composite-diff test can't answer this one — it happily reported 36/255 while the ball looked frozen, because the travelling LIGHT dominates that number): render N frames across a window, take the per-frame vertical-edge map `|∂x|`, then `min` over time = structure pinned in place vs `std` over time = structure travelling; report moving/static. TRAP that nearly sent this the wrong way: 24 meridians repeat every 1/24 turn, so sampling 12 frames at 1/12-turn steps landed on IDENTICAL meridian positions every time and scored the moving lines as static — sample a window that is NOT a multiple of the meridian period (now 24 frames over 1/6 turn). FIX: `BEVEL.u0/u1` are 0 and 1, so the frame is top/bottom edges only, no vertical sides — never give it a horizontal inset again. Meridians then take over as the ball's only vertical seams: count 24→32 to match the tile columns, width 0.0035→0.004, and each gained a paired pale stroke (offset in PATH space inside the same animated view, so no extra animated views; scaleX shrinks the gap toward the front meridian, which is correct — face-on you don't see a seam's edge). Measured static 2.19→0.81, moving 6.73→10.49, ratio 3.1→13.0. Rejected on the render: meridians at width 1.8 / opacity 0.62 — reads as a wire cage over the mirrors.
- DONE 2026-07-26 (Mirror Ball round 13 — flashing mirrors + rim fix, OTA): owner, on the round-12 build (confirmed from their recording: seam repeat measured 200ms ≈ 7000/32, vs 292ms for the old 24), said "I see the coloured tiles staying still but the shape moving". Correct, and the diagnostic that shows it: diff two frames ~0.35s apart and look at WHAT changed — only the thin vertical seams did, the tile bodies were frozen. The smooth light layer does vary each tile (measured face temporal std 13.7 with no flash layer at all) but it slides over the mosaic as a soft glow instead of being carved up by it, so no mirror ever changes as a mirror. FIX: a MirrorFlash layer — a second, brighter copy of every mirror face, filed into 26 phase buckets by screen x (plus ±0.13 per-tile jitter so tiles pop individually instead of in rigid columns), each bucket one Animated.View whose whole layer fades up and down. The bump uses CIRCULAR distance between the turn's phase and the bucket's, so the sweep wraps seamlessly — as the band leaves the right limb the left limb is already lighting, matching the two-copy light strip. Opacity-only, native driver, so 26 groups cost about one animation; ~239 extra static paths (~730 total, watch this if CPU reports return). Tuning that mattered: at WIDTH 0.12 the band faded over 1.7s and read as another soft glow — the metric barely moved (13.74→13.47). Sharp and narrow is the whole point: 0.07 now. RIM: the round-11 station-coloured stroke at the silhouette was the "outline of the shape that looks too noticeable" — ANY stroke there reads as a drawn outline. Replaced with a radial gradient that fades inward (transparent to 0.80, colour 0.16 at 0.955, white 0.10 at the edge); never put a hard ring back. REJECTED, with reasons, for making the tiles literally translate: (a) per-column affine (skewY+scaleX, the meridian trick applied to a strip) — a strip's two bounding meridians have different λ so one affine can't map both, and the error is catastrophic exactly at the front centre where scaleX→0 collapses the column to a line; (b) true 3D via [perspective, rotateX(tilt), rotateY(λ+τ), rotateX(-b), translateZ(R)] per tile with backfaceVisibility hidden — mathematically right, front-facing tiles never overlap so z-order wouldn't matter, and only rotateY animates, BUT it needs ~540 animated views and RN gives no `preserve-3d`, so nested/grouped 3D is unreliable and it can't be validated without a device. Not worth the gamble mid-launch; revisit only if the flash layer proves insufficient.
- DONE 2026-07-26 (EQUALIZER → retro segmented LED meter, OTA): owner references = a segmented VU meter (stacked lamps, unlit track visible above the level, mirrored reflection below). BUG FOUND doing it: the bars were never actually segmented in fullscreen. `GapStrips` faked the gaps by painting strips of the BACKGROUND colour over a solid gradient, and fullscreen passed `bgColor="transparent"` — so the strips painted nothing and the bars were solid gradients. Lamps are now real Views with real transparent gaps (station artwork shows between them, which is most of the retro look). Added: the unlit track behind the level (the strongest retro cue — a hi-fi meter always shows its whole scale), a white peak cap riding the top of each bar, per-lamp colour sampled up the station's 3-stop palette, segment pitch 7→8px (gap 2→3, chunkier), and animation targets snapped to whole lamps so the meter lands on a lamp instead of half way up one. Reflection re-uses the SAME Animated.Values, so it costs views but no extra animations. TWO TRAPS: (1) the reflection's flip needs NO vertical offset — scaleY(-1) already puts the bars' bottom lamps against the baseline; offsetting the box reveals the far end of the flip, so short bars vanish and tall ones float detached (that was the first render); (2) `fs.vizSection` CLIPS, so the meter must be sized for bars + reflection together — `FS_MAX_SEGS = (VIZ_H + 9) / (UNIT * (1 + REFLECT_RATIO))`, VIZ_H raised 0.26→0.30 of the screen. Verified fits at 926pt and 667pt. `EqualizerModeCard`/`EqualizerModePreview` are dead code (nothing imports them) — don't spend time on their layout.
- DONE 2026-07-26 (Mirror Ball round 14 + Equalizer trim, OTA): EQUALIZER — owner: "remove the black pills in the background and remove the reflection, just keeping the equalizer bars". Both gone (the unlit track read as a dark grid over the artwork, and the reflection's fade gradient painted a solid dark box across the photo). `Lamps` lost its `dim` variant, `Bars` lost the `track` prop, `BarsWithReflection` + `REFLECT_RATIO` deleted, and VIZ_H went back to 0.26 with the meter filling it (no reflection to make room for). Peak caps KEPT — the owner didn't name them and they read as part of the bars. FLASH PACE BUG (the "have the ball move at the same time as the reflective animation" note, and it was a real mismatch): the meridians are a true sphere projection, so a seam crosses the visible face in HALF a turn, sinusoidally; the flash band was keyed to screen x over a WHOLE turn, linearly — so highlights travelled at half the speed of the spinning grid. FIX: phase is now the tile's own LONGITUDE folded into `FLASH_PERIOD = 0.5` (two lit bands, one either side, so the face is never dark), which also gives the band the correct screen speed for free — quick across the middle, slowing into each limb, exactly like the surface. FLASH_CORE/FLASH_WIDTH are in turns so they scale with the period, NOT with 1 — halve them if the period ever changes. Verified by tracking the bright centroid: 0.65 of the radius per 1/6 turn vs 0.33 under the old mapping, i.e. the 2× the diagnosis predicted. Spin 7000→6000ms as asked. BRIGHTNESS — owner: "a bit dim". Round 13's rebalance overshot into gloom (median luminance 72, only 2.6% of the ball above 200). Lifted the shade ramp's dark end off near-black, faceStep/frameStep up, mirror colour cast 0.45→0.72, wash 0.10→0.14, underglow up. First attempt overshot the other way (median 121, 20% above 200 — washed out, no dark mirrors left); landed at median ~103 with 12-16% above 200. Reference clip is median 53 / 8.4% — deliberately NOT matched, because that ball is photographed in a black room and ours sits over lit station artwork.
- DONE 2026-07-26 (launch-eve sweep — 7 owner items, all OTA): (1) MIRROR BALL COLOURS ON-THEME — the mode carried a fixed club-neon set (cyan/magenta/purple/blue) blended alongside each station's own eqColors, so every ball got pinks and teals regardless of mood; Mountain Pass (pure white eqColors) came out speckled magenta. All colour now flows through `stationPalette(eq)` = the station's three stops plus a lighter and a deeper version of each, giving variety with no foreign hue. Also swept the same fix through NeonSweep (was 4 fixed neons), BokehField and bloomColor. The `NEON` constant is DELETED — don't reintroduce a fixed palette here. (2) SHINE — new `BallSheen`: a wide soft diagonal band sweeping across the ball on a 5.2s yo-yo, above the flashes. Deliberately a wide band, not a hotspot (round 4 already proved a hotspot reads as a painted blob); VolumetricRays lifted 0.16→0.26 so the ball also throws light into the room. (3) Ball raised via `paddingBottom: ballSize * 0.30` on its flex box — it hangs from above, so sitting high reads better than dead centre. (4) SOFTER ATMOSPHERE setting — new `softAtmosphere` in motionSettings/MotionContext, default ON, scaling AmbientGlow's whole layer to 0.5 via one wrapper View (scaling the layer, not each haze, keeps shapes and timing identical). Profile row sits directly under Atmosphere and hides when Atmosphere is off. (5) FREEZE BUG, partial — found and fixed a real one in `AutoDim`: the 30s timer fires and then AWAITS the brightness calls, so closing the mode in that gap left the continuation to run `setDimmed(true)` anyway, mounting the invisible full-screen catch Modal over the app the user had just returned to. clearTimeout is far too late for that; there's now a `cancelled` flag checked after each await. NOT confirmed to be the owner's symptom — no repro, so if "can't swipe after leaving a page" persists, ask WHICH page and whether the drive was ~30s idle. (6) CUSTOM STATION COLOURS — `CreateStationModal` never writes `eqColors`, and `StationDetailModal` receives custom stations RAW (customToStation only runs when a mode resolves one), so `eqColors?.[1] ?? theme.accentColor` fell through to the app's violet: every user-made station's mode chips and Start Drive button were purple regardless of the colour picked. Added a `(station as CustomStation).color` fallback, and `customToStation` now derives a light→base→deep ramp via `rampFromColor()` instead of three identical stops (three identical stops give the EQ and mirror ball nothing to work with). Derived, not stored, so stations saved earlier get it too. (7) MirrorBallGlyph is now PLAIN WHITE — tiles vary only in opacity, body gradient and specular removed; the silver/blue version read as a colour illustration among flat white glyphs.
- DONE 2026-07-26 (share pill + one shared action row, OTA): all eight modes carried a BYTE-IDENTICAL copy of the action-row markup and its four styles — the classic way things drift apart. Now one component, `src/components/ModeActionRow.tsx`: `[Change Mood] [playlist name] [share]`. The share pill renders ONLY when `spotify.track` exists — with no track there is nothing to share but a mood tagline, and a button that sometimes shares nothing is worse than no button. Icon is Ionicons `share-outline` on iOS (the box-with-arrow Apple users know) and `share-social-outline` on Android. Playlist label is capped at 18 chars AND flex-shrunk with `minWidth: 0` — minWidth is load-bearing on a flex row, without it the text sets the pill's minimum width and the share button gets pushed off the edge. Station name varies per mode (`station` / `currentStation` / `lockedStation`), so it's passed in. SPACING: pills' marginTop 18→26, and the five modes whose transport row had `marginTop: 18` (CD, Circular EQ, Disco/Mirror Ball, Horizon, Tuner) went to 14 so the buttons sit slightly higher — don't take that lower, the progress bar is directly above. Cassette/Vinyl/Equalizer already sat at `marginTop: 10, marginBottom: 8` and were left alone; their gap below grows from the pills' side anyway.
- DONE 2026-07-26 (SHARE CARD — Spotify-style now-playing pin, OTA): share now opens a preview sheet with a real card, then hands a PNG to the OS share sheet. KEY CONSTRAINT AND HOW IT WAS MET: turning a View into an image normally needs `react-native-view-shot`, a NATIVE module — installing it changes the fingerprint and every existing build stops receiving OTA until a new binary ships, which is unacceptable mid-launch. Instead the card is drawn ENTIRELY in react-native-svg (already in the build) and captured with `<Svg>.toDataURL()`, then written with `expo-file-system/legacy` (`writeAsStringAsync` + `EncodingType.Base64` into `cacheDirectory`) — expo-file-system ships inside `expo` itself, so it is already autolinked. Net: zero new dependencies, pure OTA. CONSEQUENCE: everything in `ShareCard.tsx` must be SVG primitives — no Views, no expo-linear-gradient, no MarqueeText. TWO IMPLEMENTATION TRAPS: (1) the capture copy is a SECOND full-size `<Svg>` parked off-screen (`left: -CARD_W*2`, opacity 0 — NOT display:none, a hidden view may not rasterise), because toDataURL rasterises at the view's own size and capturing the small preview would export a soft thumbnail; (2) both Svg roots are on screen at once, so gradient ids are namespaced per instance via a `uid` prop — duplicate ids across roots is a known way to get one rendering blank. The preview step also earns its keep by giving the album art time to load; capturing straight from the tap often exported an empty cover. SVG `<Text>` does not wrap, so `wrapLines()` estimates at 0.52×fontSize per char and errs short. Card is 1080×1350 (4:5 — iMessage/WhatsApp show it uncropped). Name comes from `getProfileName()` (Spotify `/me` display_name, cached in AsyncStorage); with no name the line reads "Now playing on <station>". Footer carries cruisefm.app so recipients without the app can install. ANDROID: React Native's `Share` cannot attach a file (needs a content:// provider), so Android shares the text form — expo-sharing would fix it but is a native module, so it waits for the next fresh build. Station colours drive the card, and pale stations (Mountain Pass is pure white) are mixed 0.68/0.82 toward black or white text can't sit on them.
- DONE 2026-07-26 (TUNER → dot-matrix head unit, OTA): owner references = a car head unit and an FM receiver, both dot-matrix LCDs. NEW `src/components/DotMatrix.tsx`: a real 5×7 matrix renderer (51 glyphs: A-Z, 0-9, punctuation), drawing LIT AND UNLIT dots — the visible unlit dots are the whole reason it reads as hardware, and no font can do that. No font file, so it ships OTA. TRAP while building it: the glyph table was first written as one 35-char string per letter and several were silently malformed (S, X, 5 came out mangled, two even needed `.slice(0,35)` to compile) — it's now seven 5-char rows per glyph joined at load, where a mistake is visible on sight, plus a length assertion. Always render the full character set before trusting it. LAYOUT per the owner's sketch: `[● ON AIR] SONG TITLE / ARTIST`, then `STEREO … TUNED`, then big `FM … 92.10`. The ON AIR lamp breathes only when `playing && lock > 0.9`. Long titles SCROLL (`DmLine`, transform-only on the native driver) rather than truncating, but the window width is SNAPPED to a whole number of character cells — a window ending mid-glyph instantly breaks the illusion. The duplicate song-title block below the dial is gone; it only appears now when there's no track, carrying the mood tagline. Retired: `onAirChip`/`onAirDot`/`onAirText`/`freqBig`/`freqUnit` styles and the MarqueeText import. Frequency now shows 2 decimals (92.10) to match a real receiver.
- DONE 2026-07-26 (share card → mode artwork, OTA): owner wanted the card to read as artwork for the mode and mood, not a Spotify clone in different colours. `ModeArt` in ShareCard.tsx draws a per-mode composition behind the cover, always in the STATION's eqColors — so the mode gives the shape and the station gives the palette. cd/vinyl = concentric grooves centred on the cover (the artwork becomes the label); disco = sphere latitudes + meridians + scattered light; cassette = shell, two reels and the tape run (deliberately WIDER than the cover and pushed low, or it hides behind the artwork entirely); equalizer = a bell-curved meter along the bottom; orb = concentric rings; radio = dial ticks with a needle; horizon = sun on a receding grid. `modeId` is threaded from ModeActionRow (session mode) alongside the existing label. Everything is SVG primitives — same OTA constraint as the rest of the card. PLATFORM LIMIT, unchanged and worth restating: iOS shares the real PNG; ANDROID shares text only, because React Native's `Share` cannot attach a file without a content:// provider. Fix is `expo-sharing`, a native module → next fresh build, which suits the Android timeline anyway (blocked on Google's 14-day closed test).
- RUNTIME VERSION IS NOW PINNED, NOT COMPUTED (2026-07-30) — read this before touching app.json. `runtimeVersion` was `{"policy":"fingerprint"}` and is now the literal string `"1.0.0"`. WHY: EAS's build-time fingerprint and eas-cli's publish-time fingerprint had DIVERGED — build 10 (EAS macOS builder, commit 247faef) reported `1a4deffd3fee3f9ad1ccc3d53843eefcf3e4705d` while this repo fingerprinted as `eb670d785ceebd4b977406ca67cab1364bf2df63` on both a Linux AND a macOS runner from the same commit. Consequence: every update published for two days succeeded, appeared on the dashboard, and reached ZERO phones. Ruled out first (don't re-check these): the OS, repo changes since that commit, leftover expo-audio. CONSEQUENCES OF PINNING, both load-bearing: (1) editing app.json/eas.json/package.json NO LONGER breaks OTA by itself — the old "any edit to eas.json silently changes the runtime version" trap is GONE, so the NEXT-FRESH-BUILD batching below is now about NATIVE code only, not about protecting a hash; (2) the automatic native-mismatch seatbelt is gone, so ANY change that alters native code (new native module, plugin adding an Info.plist key, new permission) MUST bump `version` AND `runtimeVersion` together in the same change, or an update will land on a build that cannot run it. `.github/workflows/ota-update.yml` now refuses to publish unless a FINISHED iOS build on that channel is listening on the current runtime version, so a silent no-op cannot recur; the temporary `diagnose` mode and macOS fingerprint job it was debugged with are removed.
- NEXT FRESH BUILD — BATCH THESE (do NOT do them early). These are NATIVE changes: an OTA update carries JS and assets only, so none of them can reach a phone without a new binary. (Since 30.07 they no longer break OTA merely by editing app.json — see the pinned-runtime entry above — but each still needs `runtimeVersion` bumped in the same change.) Owner's plan (26.07): the next build happens after Apple approval and once they're satisfied with functions/design.
  1. `expo-sharing` — lets Android attach the share-card PNG. React Native's own `Share` can only send text on Android (a file needs a content:// provider). iOS already works. One Android user today, so nothing is lost by waiting. After installing, swap the Android branch in `ShareCard.tsx`'s `shareCard()` for `Sharing.shareAsync(path)` and keep the text path as the fallback.
  2. `react-native-view-shot` — lets the share card be a REAL SCREENSHOT of the mode instead of a hand-drawn SVG lookalike (owner asked 27.07: "since this is a more visual app… share this like a screenshot but in a card form", comparing the actual Mirror Ball on device against the redrawn one and rightly preferring the real thing). This is THE reason the card is SVG at all — capturing a View needs a native module, which changes the fingerprint. Once it lands: capture the mode's own fullscreen view (minus transport/pills), inset it into the 1080x1350 card with the eyebrow, song block and cruisefm.app footer around it, and keep the SVG heroes as the fallback for when a capture fails or there's no live mode (e.g. sharing from the mini-player). `ShareModeArt.tsx` stays useful either way.
  3. Apple Music via MusicKit — OWNER-CONFIRMED PRIORITY (28.07): full in-app music for everyone on iOS, no quota. Native module + Apple Developer MusicKit key + config plugin; user needs an Apple Music subscription. Start with `expo-music-kit`-style community plugin or a small custom Swift module; entitlement `com.apple.developer.applemusic` + NSAppleMusicUsageDescription. Wire behind the existing platform abstraction (musicPlatform.ts) so Spotify and Apple Music are peers.
  4. Drop the `expo-audio` plugin from app.json — mic-reactive visuals were removed 22.07 and legal.ts now says the app "does not use the microphone", but the plugin still declares a microphone permission. Leaving it costs an unexplained permission on the store listing; it was kept only to protect the fingerprint.
  Bump `version` + `runtimeVersion` in app.json as part of the batch, then cut a `testflight`-profile build afterwards so the owner's phone stays on the preview channel and OTA keeps flowing.
- DONE 2026-07-26 (Tuner round 2 — dual-band dial, OTA): (1) the standard `PLAYING FROM <station>` header is BACK at the top of Tuner (it was the only mode without one), and the duplicate station name that sat between the display and the dial is gone. (2) NEEDLE is now a fixed neon red (`NEEDLE_RED = '#FF3B30'`), never the station accent — on every dial worth copying the pointer is the one element that doesn't change hue, which is what lets the eye find it instantly. Built as three stacked rects: a wide soft glow, a 3.2px core, a 1.2px near-white hairline. First cut at 14px/5px/2px read as a bar, not a pointer. (3) DUAL SCALE — FM numbers and ticks above the lit band, a decorative AM scale below it, both labelled in the dot-matrix type via the new `DotMatrixGroup` (a `<G>` rather than its own `<Svg>`, since react-native-svg can't nest Svg roots). The AM row is cosmetic — Cruise FM only tunes FM — and is mapped across the same travel at its own rate, so it drifts past at a different speed as you tune, exactly like a real printed dial. `AM_PX_PER_KHZ = PX_PER_MHZ / 220`: at the first value (/78) a 200kHz gap was wider than the screen and only one AM number was ever visible. (4) The space the removed song-title block left is taken by the display itself — padding 14→22, row gaps 10→18, and 28px between panel and dial. NOTE: `G` is the glyph table's name inside DotMatrix.tsx, so the SVG group import there is aliased `SvgG`.
- DONE 2026-07-26 (SHARE CARD → mode artwork, round 2 — the mode IS the picture, OTA): the first card drew the mode as a faint pattern BEHIND a big album cover, so every mode came out as the same Spotify pin in a different colour — which defeats the point, since the modes are the part nobody else has. New `src/components/ShareModeArt.tsx` owns a HERO stage (y 150..890 of the 1080x1350 card) and each mode draws a real illustration there with the ALBUM ART AS A COMPONENT OF IT, not on top of it: vinyl = the record's label (plus grooves, sheen, silver J tonearm), cd = the disc face under jewel-case plastic with the diffraction fan UNDERNEATH the art (a printed face is matte — same rule as the mode), cassette = the printed inlay in the shell above the reels, disco = a genuine sphere projection (same lat/long quads, back-face cull, Lambert shading + brick bond as the mode, ~150 visible facets, static) with beams falling onto the cover below, equalizer = the cover with the segmented LED meter standing in front of it, orb = the cover ringed by radial bars, radio = the head unit — real dot-matrix title/artist/frequency via DotMatrixGroup, cover in the panel, dual-band dial with the fixed red needle, horizon = the cover becomes the sliced retrowave sun over a receding grid. Card geometry (CARD_W/H, stage bounds, mixHex) moved INTO ShareModeArt so the two files don't import each other in a circle; ShareCard re-exports them. `STATION_FREQS` moved to constants/stations.ts (+ `stationFrequency()` with a hash fallback so custom stations tune somewhere stable) because the card draws the same dial and importing it from TunerMode would cycle mode -> action row -> card -> mode.
  VERIFICATION METHOD, reuse it: the components were rendered for real by transpiling their OWN source with `ts.transpileModule` under a JSX factory that emits SVG markup (tag map Circle->circle etc., camelCase->kebab attrs, `href={{uri}}`->href), then screenshotted in Chromium at 8 modes x 3 fixtures. Four traps if you rebuild that harness: the fragment factory arrives THROUGH the element factory as `h(F, null, ...kids)` so F must read props.children; child markup strings must be wrapped in a marker class or they get HTML-escaped into visible text; DotMatrix's glyph table is named `G` and collides with the SVG `<G>`; and name the factory `__h`, because a local named `h` in the component (a bar height, a rect height prop) silently shadows it.
  BUGS THE RENDER CAUGHT: CoverSquare only takes one `size`, so the cassette inlay drew an 804-SQUARE straight over the song title (added `CoverRect`); the horizon grid ran to the card's bottom edge and struck through the title; the vinyl tonearm was a short stick in the corner. PALE STATIONS: Mountain Pass's eqColors are literally three whites, and a white haze over half the card turned it light grey — every big soft glow now goes through `glowCol(eq)`, which deepens by perceived luminance, so pale stations keep their hue and come out charcoal-with-silver-hardware instead of washed out. Checked with no album art too (the tint fallbacks stand in fine). Still SVG primitives only and zero new dependencies, so it remains a normal OTA; ANDROID still shares text only until `expo-sharing` lands in the next fresh build.
- DONE 2026-07-26 (Tuner round 3 — use the space, OTA): (1) SONG TITLE moved onto its OWN full-width row instead of sharing row 1 with the ON AIR lamp. Sharing was what kept it small — the lamp plus its gap left about ten characters, so growing the dots just meant scrolling sooner. Full width buys bigger dots AND more of the name visible: TITLE 2.75/1.0 → 3.5/1.15 (glyph height 25 → 31px) with the same 11-12 characters, ARTIST 1.95/0.7 → 2.1/0.78. The lamp now sits alone above them, which is also what the owner's original sketch asked for. Title/artist are LEFT-aligned on their own row (`DmLine` gained an `align` prop) — right-alignment only made sense when they were the right-hand column of a two-column row. (2) BAND LABELS vs SCROLLING NUMBERS: "FM" sat directly in the FM numbers' travel path and collided into "FM02"; same for "AM" and "1000". Fixed in two parts, and the SECOND part is the one that matters — a backing plate ALONE is not enough, because a number half-hidden behind it reads as a different, shorter number ("1000" → "000"), which is worse than a gap. So any scale number whose glyph box would overlap the plate is now dropped outright (`underPlate()`), and the plate itself fades out on its right edge (`dialPlate` gradient) so the thin ticks dissolve under it rather than being sliced. (3) DIAL GAP is now responsive: `clamp((winH - 640) * 0.18, 14, 56)`. A flat 52 gave the owner the lower dial they asked for but ran off the bottom of a short phone — the panel and the dial are both fixed-height blocks inside one centred column, so there is no slack to absorb it. Panel paddingVertical trimmed 30 → 26 for the same reason. NOTE: on a 667pt screen (SE) the column still overflows; it did before this round too, so it's pre-existing, not a regression — worth a look if an SE user ever appears.
- DONE 2026-07-26 (STATIONS PAGE → a receiver, AM/FM bands, OTA): owner's note was that the page had a dial *motif* but the idea was "kind of forgotten", and it read as the Modes tab with different cards. It's now laid out like a real head unit. BANDS: `AM` = free stations + the user's own creations, `FM` = premium — one visible reason to pay instead of a PREMIUM badge repeated seven times (that per-card badge is now DELETED; the band header carries it). Both bands sort ASCENDING by dial number, which is the whole point of the metaphor, so custom stations interleave with the free ones by frequency rather than sitting in their own "MY STATIONS" block. New in constants/stations.ts: `STATION_AM` (night-run 810, sunset 1010, daylight 1240), `stationAm()` (hash into 540–1600 on round 10kHz steps, so a made-up station always lands in the same place), `stationDial(id, premium)` returning `{band,label,value}`. ROW = rail | number | card. The rail line is drawn INSIDE every row rather than once behind the list — the rows stack into one continuous line by themselves and the red marker lands on the right station with nothing to measure; it needs `top:-1/bottom:-1` or sub-pixel rounding leaves visible breaks at the seams. The red marker (horizontal, matching the Tuner's needle colour `#FF3B30`) parks on `np.session?.stationId`, falling back to the hour's on-air station. TRAP, applies to the marker box, the number and the lock overlay alike: the card carries `marginBottom:14` from `cardShadow`, so anything that wants to line up with the CARD must stop 14 short of the row's bottom — otherwise it centres on the gap. That also fixed a pre-existing bug where the premium lock panel hung 14px below the card it covered. CARD: name/tagline/tag now CENTRED, which means nothing may share the name's row (the right-hand block is padded to the icon's width so the middle is really the middle) and tags are capped at ONE — two long ones ("sunset glow" + "warm vibes") wrapped to a second line and made that station's card taller than its neighbours, which a dial can't have.
  (The Tuner was made dual-band the same day — see the entry above — so the page and the dial now agree.)
  Verified by running the real web build (`npx expo start --web`) and screenshotting in Chromium at 393pt — worth reusing for layout work; `/stations` goes straight there, and the platform-picker sheet needs "Skip for now" clicking first. Mouse-wheel scrolling does NOT move the RN ScrollView on web; set a tall viewport (2100) and take one shot instead.
- DONE 2026-07-26 (TUNER → genuinely dual-band, OTA): the Stations page's AM/FM split made the Tuner's FM-only dial a lie, so the dial now really has two bands and the AM row stopped being decoration. `BAND_CFG` holds each band's range, drag rate, tick size and lock window: FM 87.5-108.5 at 110px/MHz, AM 530-1600 at 2.2px/kHz — chosen so both bands take about the same thumb travel end to end, and so a tick lands ~22px apart on either. Both label every TENTH tick (1 MHz / 100 kHz), which is why one `MAJOR_EVERY` serves both. `DIAL` maps every station to `{band,value}` once via `stationDial`, and `nearestStation(band, freq)` only sees the current band's stations — that is what a band switch MEANS on a receiver. LAYOUT RULE: the LIVE band is always the top scale and carries the station markers; the other band is printed underneath and drifts at its own rate (its whole span laid across ~2.2 screens, labels thinned to at least 78px apart). TRAP: draw only halves and majors on the printed row — at full density it became a solid grey mass that out-shouted the scale actually being tuned. Also `Math.round(k*tick*1000)/1000` on every tick, or FM labels come out as 92.30000000000001. BAND BUTTON sits on the STEREO/TUNED row, where a head unit keeps it; tapping a band tunes to that band's first station and starts it, because on a real set the band and the audio move together. It's a TouchableOpacity inside the dial's PanResponder zone and works — RN asks the deepest child first on touch-start — but that's the thing to re-check if band taps ever stop responding on device. ROUND 2 (owner: "wasn't too obvious, I wouldn't have known it would work"): the first version used the same flat dot-matrix type as the STEREO/TUNED labels sitting beside it, so it read as printed text rather than a control. The keys now have a raised top bevel, a border, and a lamp dot that lights on the selected band — and the hint under the dial reads "tap am / fm · drag to tune" instead of just "drag to tune", because the dial now has two gestures and only one was named. MoodSheet picks that cross bands flip the dial instantly instead of sweeping (the two scales aren't one ruler, so there is nothing to travel across). Custom stations are deliberately NOT on the dial: MoodSheet doesn't list them either, so the mode stays consistent with itself.
- DONE 2026-07-26 (station page slides in from the right, OTA): `StationDetailModal` used to rise from the bottom like a sheet; it now pushes in from the RIGHT like turning to a page (`slideX` SCREEN_W->0, 300ms ease-out cubic — timing not spring, a page push should arrive and stop rather than wobble). Both exits are kept and each leaves the way it would have come in: an edge-swipe back slides it off right, the old pull-down still drops it, and `handleClose(down)` picks the axis. The dismiss PanResponder claims on MOVE only (capture-on-start is what killed the settings back button) and the rightward branch is EDGE-ONLY (`g.x0 < 44`) or it fights the horizontal Visual Mode strip; a `swipeAxis` ref locks the axis on the first few pixels so the two gestures can't argue mid-drag. Close now goes through a `closeRef` because the responder is built once and would otherwise hold the first render's copy. ALSO FOUND doing it: the sheet had NO visible way out at all — only the gesture — so there's now a back chevron at top-left, and the custom-station ... menu (plus its dropdown) moved to the right to make room.
- DONE 2026-07-26 (stations page -> SMOKED GLASS cards + the free stations are now AM, OTA): four card directions were mocked at real phone width against the real station colours, artwork and MCI icon font (harness in scratchpad: pull the fields out of stations.ts with a regex, copy the jpgs, @font-face the MaterialCommunityIcons.ttf and index the glyphmap for the icon chars — a quick way to compare directions without building them). A = the shipping mood-gradient slab, B = smoked glass, C = the station photo darkened, D = no cards, a printed listing. Owner picked B. The slab was the specific thing making Stations and Modes look like twins (Modes is gradient slabs too) and it shouted over the dial. `ListCard` now: `rgba(15,17,29,0.72)` glass base, the station's `eqColors[1]` as a wash off the LEFT EDGE only (2E -> 10 -> transparent at 0.42/0.72), rim at `accent+'44'`, icon ring `accent+'22'` on `accent+'66'`, and the neon halo shadow softened to 0.34/22 — the same move the Recommended cards had on 25.07. `CustomStationCard` got the identical finish so the user's own stations sit on the dial looking like everything else (it needs `overflow:'hidden'` on the inner view for the wash to clip). NAMES: the free stations are on the AM band, so "Sunset FM" contradicted the dial it sat on — night-run/sunset/daylight are now Night Run AM / Sunset AM / Daylight AM (also updated in website/index.html's station list and alt text, ThemeGenerator's sample text and HeroCard's doc comment). App Store screenshots still say FM; cosmetic, and worth refreshing whenever they're next regenerated.
- DONE 2026-07-27 (share card gets the real station photograph, OTA): owner compared the on-device Mirror Ball with the share card's redrawn one and asked for "a screenshot but in a card form". A true screenshot needs `react-native-view-shot` (native → next fresh build, now item 2 in that batch), but the biggest visible gap was OTA-able: every mode is a full-bleed BLURRED STATION PHOTO with the object on top, and the card was drawing a flat gradient instead. `ShareCardBody` now paints `station.imageBlur ?? station.image` full-bleed behind everything — react-native-svg's `<Image href>` takes a require()'d asset directly, so no new dependency — under a 4-stop scrim (0.72 → 0.60 → 0.74 → 0.93) shaped like the one the modes lay over their own backdrop: lighter where the object sits, deepest under the type. Custom stations have no artwork and fall through to the gradient. HARNESS NOTE: scratchpad/share/render.js needed two tweaks to see this — `href` may now be a bare string as well as `{uri}`, and the fixtures carry `imageBlur` as a base64 data URI of the real assets/stations/blur/*.jpg.
- DONE 2026-07-27 (stations page colour + orange dial numbers + left-aligned names, OTA): on device the smoked glass came out far darker than the web render — several stations read grey-brown rather than their own mood. Three changes: (1) COLOUR — glass base 0.72 -> 0.58 so less black sits over the wash, and the wash itself now travels the WHOLE card (82 -> 46 -> 1A across 0/0.55/1) instead of fading out at 0.72, plus a faint top-down white lift so it doesn't read as a flat fill; rim 44 -> 5C, icon ring 22/66 -> 38/99. Still glass, not the old slab. (2) DIAL NUMBERS are car-stereo amber `#FF9A2E` — grey read as UI text rather than a lit dial — and the tuned one burns brighter (`#FFD79B` + an orange textShadow glow); the red rail marker says WHERE, the bright number says which one is live. (3) NAMES AND TAGLINES LEFT-ALIGNED again, reversing the 26.07 centring at the owner's request: with the dial number sitting in its own column to the left, the name reads better hard against the icon than floating mid-card. The chevron block's fixed 42 width existed ONLY to balance the icon and keep the name centred — it is 26 now. Same treatment applied to `CustomStationCard`.
- DONE 2026-07-27 (paywall brought back in line with the real line-up, OTA): `src/app/premium.tsx` still sold "Vinyl, the Tuner, Horizon and the Circular Wave orb" — but Circular EQ went FREE on 25.07 and Mirror Ball + CD had never been added, and the premium FM band (26.07) was not mentioned anywhere. The FIX IS STRUCTURAL, not a copy edit: FEATURES and COMPARISON are now DERIVED from `MODE_CATALOG` (m.pro) and `STATIONS` (s.premium), so the paywall cannot go stale again when the line-up moves — the hand-typed duplicate list is exactly how it drifted. New rows for the AM/FM split ("AM band stations" free, "FM band - 7 stations" premium) since that is now a headline reason to pay. `PaywallShowcase` was also advertising Circular EQ as a premium teaser: its Orb miniature is replaced by a MIRROR BALL one (uniform tile grid clipped to a circle with alternate rows offset — how a real ball is built — plus a travelling two-band light, a top-left catch and a thick dark inner rim for roundness; the light moves, the grid does not, same trick as the full mode). Scene order Vinyl -> Tuner -> Mirror Ball -> Horizon. RULE recorded in that file: anything on the showcase stage must be pro:true in MODE_CATALOG.
- DONE 2026-07-27 (Mirror Ball round 15 — denser mirrors + a real gradient in each, OTA): owner sent two reference balls and asked for smaller/more tiles with "mini gradients", keeping the themed colours. GRID 17x32 -> 21x40 (~240 -> ~380 visible facets; a front-face tile is now ~8px). `buildLatitudeArcs` carries its own ROWS constant and MUST be changed with it or the arcs stop sitting on the row seams. PER-TILE GRADIENT: each mirror face is drawn twice — its own colour, then the same path filled `url(#dbFacet)`. That is ONE gradient definition for the entire ball, because SVG gradients default to objectBoundingBox units and so rescale to whatever path references them; per-tile defs (~380 nodes) was the obvious approach and the wrong one. REJECTED first: a flat two-tone split (light top-left triangle over a darker face) — rendered, and it read as a triangle drawn on every tile rather than a falloff. Stops are white 0.66 -> white 0.10 at 0.46 -> #05060d 0.42, i.e. lit corner top-left and shadow bottom-right, matching the key light. Face base pulled down 0.55 of a shade step to pay for the sheen. MEASURED before/after with the offscreen builder harness (scratchpad/ball/render.js — transpiles hash01..buildSphereTiles straight out of the component, so it tests the shipped geometry): median luminance 98 -> 103, share above 200 10.1% -> 12.0%, i.e. landed back on the round-14 numbers the owner signed off. Checked on Night Run, Sunset, Mountain Pass and Daylight — the white station still comes out silver, the warm ones stay warm. COST: 717 -> 1456 paths. All STATIC (built once, never animated; only the 26 flash groups animate), but that is 2x the shapes — if `cpu_resource` reports come back, ROWS/COLS is the first knob to turn down. Partly paid for already by skipping flash paths for mirrors with catchRoll <= 0.12, whose catch tops out near 0.09 opacity and is invisible.
- DONE 2026-07-27 (Mirror Ball round 16 — head on + gradients at different angles, OTA): (1) TILT 0.30 -> 0.05. The comment on that constant warned that dead-level would flatten the latitude rings into straight lines and read as a brick wall — that was TRUE OF THE OLD BUILD, which faked the surface with a scrolling texture strip, and is NOT true of the sphere projection: rendered at 0.30/0.10/0.04/0 side by side, roundness is identical because the columns still pinch toward the silhouette and the shading still falls off. Kept a sliver rather than exactly 0 so the poles never land on the silhouette, where z = 0 is the degenerate case for the back-face cull. TILT feeds the tiles, the latitude arcs AND the meridian transform from one constant, so they stay in agreement automatically. (2) PER-TILE GRADIENT DIRECTION: round 15 gave every facet the same top-left -> bottom-right ramp, so the whole surface leaned one way; on both reference balls the mirrors disagree, and that disagreement is most of what stops the surface looking printed. `FACET_DIRS` holds six directions and `facetDir(j,k)` picks one per tile, weighted toward the first two so the ball still reads as lit from the upper left rather than as noise. Still only six gradient nodes for ~380 facets (objectBoundingBox units). Measured: median 103 -> 107, >200 12.0% -> 11.9% — brightness unmoved.
- DONE 2026-07-27 (Mirror Ball round 17 — denser again, seams as hairlines, OTA): owner re-sent the reference ball with no words; the remaining gap at a glance was tile DENSITY. 21x40 -> 25x48. Paid for by DELETING the per-tile frame path (was ~380 paths): the BEVEL gap already exposes the ball's dark body between rows, so the seam draws itself. TRAP found immediately on the render: with the frame gone the gap showed near-black instead of dark metal, and at the higher row count those bands dominated and the ball read as STRIPES — the fix is that the gap must be a hairline, so BEVEL v0/v1 went 0.06/0.90 -> 0.03/0.955. u0/u1 stay 0/1 (no horizontal inset — vertical seams belong to RotatingMeridians alone; see round 12). Net 1445 -> 1540 paths for 37% more mirrors. Measured median 107 -> 106, >200 11.9% -> 12.5%, so brightness is where the owner left it. NOTE when reading the offscreen harness output: it renders the tile layer only, so tiles in a row appear to butt together — the 32 rotating meridians that draw the vertical seams are a separate layer the harness doesn't include.
- DONE 2026-07-27 (CRASH FIX — background timers were getting the app SIGKILLed, OTA): owner reported several crashes "leaving and entering the app again", with NOTHING in Sentry. Sentry was fine (DSN hardcoded so it can't go missing from a bundle, native module + plugin installed, OWNER_MODE false so nothing was being filtered). The four reports in Settings > Privacy > Analytics Data were all `bug_type 309`, `EXC_CRASH / SIGKILL`, exception codes 0x0/0x0 — i.e. the SYSTEM shot the process. A crash reporter can never see that: there is no signal to catch and no chance to flush, which is exactly why Sentry was silent. Don't chase Sentry when the reports say 309. ROOT CAUSE: `useSpotifyPlayback`'s 5-second poll had NO AppState gate, so a backgrounded app kept making a network request every 5s indefinitely; iOS gives a background app a few seconds of grace and then kills it for exactly this. FIX: the interval is now owned by `pollRef` with `startPoll`/`stopPoll`, and the existing AppState listener stops it on background and restarts it (plus an immediate refresh) on foreground. Same treatment for VinylMode's 3s "restart the spin" safety net via a new `useAppActive()` hook (src/utils/useAppActive.ts) — anything on a repeating timer must ask that hook. Checked: these were the ONLY two setIntervals in src/. rAF loops are fine, iOS doesn't run them in the background. NOT the mirror ball — the crash timestamps (17:25, 17:45, 18:56 and 01:28) bracket the whole day and the poll has been unguarded for far longer; the density work just meant a lot more leave/enter cycles that day.
- DONE 2026-07-27 (Mirror Ball round 18 — fixed lamps, softer facet ramp, brighter, OTA) + THE ROTATION QUESTION SETTLED: owner sent a clip of a REAL mirror ball and asked for (1) light from fixed sources rather than a band sliding across, (2) softer per-facet gradients, (3) THE TILES THEMSELVES TO TURN, (4) brighter tiles.
  (3) IS NOT POSSIBLE OVER THE AIR AND THE PROOF IS NOW ON RECORD — do not spend another round on it. A tile's screen x is `R*cos(b)*sin(lam)`, so the transform needed to rotate it depends on its LATITUDE as well as its longitude; one transform per column therefore cannot serve the rows inside that column. Measured with a least-squares affine fit per column over a full turn (scratchpad/ball/affine.py): worst error 89px on a 170px radius, and it does NOT improve with more columns (48/96/160 all ~88px) because the error is driven by the latitude spread, not the longitude spread. Every column also passes through the ball's centre line each turn, where the required scale goes to infinity. That is the same wall rounds 13's two rejected approaches hit, now quantified. Real rotation needs a per-tile transform (~1200 animated views, and RN has no preserve-3d) or a GPU canvas — react-native-skia, which is a NATIVE module and so a fresh-build item.
  (1) SUBSTITUTE THAT WORKS: a static grid cannot hold a lit patch still while mirrors move through it — whichever mirrors are lit sit at fixed screen positions, so if the lit SET changes the lit REGION must move. What can be done is make the light read as beams rather than a wash: `LAMPS` assigns every mirror to one of three fixed lamps, each with its own `tilt` (shifts the mirror's flash timing by latitude, so a lamp's lit mirrors form a DIAGONAL patch instead of a full-height stripe), its own `hue`/`sat` from stationPalette, and its own `phase`. Costs nothing — the mirrors just land in different buckets of the existing set. FLASH_CORE/WIDTH 0.034/0.070 -> 0.012/0.052 so a mirror snaps on and off instead of swelling, and the flash opacity split went to 0.94-1.00 vs 0.02-0.14: on the reference a lit mirror is beside a dead one, and it is that contrast, not the brightness, that makes each read as a separate mirror.
  THROB TRAP, and the reason `phase` exists: a beam on the FRONT of a sphere lights a wide area, the same beam at the limb lights a sliver, so with all lamps in step the ball swung between 22% and 4% of pixels above 200 twice per turn — visible pulsing. Staggering the lamps a third of a cycle apart holds it at 12.8-16.1%. Measured with scratchpad/ball/sweep.sh, which samples brightness across a turn; reuse it after ANY flash change. FLASH_GROUPS was raised to 40 chasing this and it made no difference (26 gives the same spread) — it was never a bucket-resolution problem, so it is back at 26 and costs no extra animated views.
  (2) FACET RAMP: 3 stops -> 6. The hard line was two steep segments meeting at a corner (white 0.66 falling to 0.10 across the first 46%, then re-darkening); eased through the middle and softened at both ends it reads as a curve.
  (4) BRIGHTER: SHADE_ANCHORS' dark end lifted off near-black (#101320 -> #1b2030 and up the ramp) and faceStep +1.0 with jitter 4.8 -> 5.4. Median luminance 106 -> 113-125 across the turn, share above 200 12.5% -> 12.8-16.1%. The round-6/13 warning (a brighter RESTING surface kills the sense of motion) is respected because the flash contrast went up at the same time — the gap between a caught mirror and its neighbour is now larger than it was, not smaller. Path count unchanged at 1540.
- DONE 2026-07-27 (mode header copy, OTA): the eyebrow above the station name in all 8 fullscreen modes reads “YOU’RE LISTENING TO” instead of “PLAYING FROM” (owner). Curly apostrophe, not a straight one — better in a letterspaced small-caps line and it also sidesteps eslint's react/no-unescaped-entities. All eight already shared identical styling (fontSize 10, weight 700, letterSpacing 2, white at 0.45), so it was a straight swap; checked at 393pt and at 320pt (the narrowest phone still around) and it fits on one line at both.
- DONE 2026-07-28 (Mirror Ball round 19 — station colour that actually shows, beams instead of stars, dark when paused, OTA): four owner asks.
  (1) "SUNSET AM SEEMS DULL" — the cause was structural, not a tuning number. `SHADE_ANCHORS` is a strongly BLUE ramp (#1b2030 ... #c2cbe2), and every station's colour was being mixed INTO that navy grey. Opposite hues average toward grey however much you add, so Sunset's amber and hot pink were arriving as dusty mauve while the cool stations happened to look right. Tinting harder cannot fix it. The ramp is now rebuilt per station by `stationShade(eq)`: each anchor is flattened to a pure grey of the same brightness (killing the baked-in blue), then pushed toward a DEEPENED copy of the station's mid stop by `0.80 * (1-t)^1.6`. Two things about that curve are load-bearing — it must be steep, because a shallow one puts heavy colour in the MID tones and since `deep` is darkened that halves the ball's bright pixels (measured); and the target must be deepened, because Mountain Pass's mid stop is literally WHITE and mixing that into the shadows lifts the floor until no mirror can be dark. `shadeAt` now takes the ramp as an argument. Alongside: stationPalette's pale variant 0.42 -> 0.28 toward white (42% into white is most of a pastel, and nine of those drive every mirror's cast), its deep variant 0.34 -> 0.42, per-mirror `cast` 0.18+0.48 -> 0.24+0.56 applied at 0.95 rather than 0.72, and LAMP saturation 0.10/0.26/0.18 -> 0.20/0.46/0.36 — the beams carry most of the colour, because an unlit mirror shows almost none of its own. Measured on Sunset: mean saturation 0.239 -> 0.376 with median luminance held at 115 (round 18's approved band was 106-125). Night Run stays cyan/violet, Mountain Pass stays pure silver (sat 0.038), Coastal warm-teal.
  MEASUREMENT NOTE: the ">200 luminance" share used since round 6 PENALISES colour — a fully bright saturated orange sits near 190 and doesn't count. Once the lamps are tinted, read median luminance plus a max-channel test instead, or you will chase a number that says "dimmer" while the screen says "more vivid". Also fixed: scratchpad/ball/render.js had FLASH_CORE/FLASH_WIDTH hardcoded at the round-13 values and had silently drifted out of step with the shipped flash curve; it now reads them out of the component.
  (2) STARS -> BEAMS: `BallGlitter`, `StarDust` and the now-dead `Glint` are deleted (the owner: the tiny 2D stars "don't enhance the style" — they read as sparkles pasted over the picture). New `LightStreaks`: seven long, thin, very faint white lines at shallow angles (14-36deg), each breathing on its own slow loop. They fade out at BOTH ends via a gradient — a flat white bar has two hard stops and reads as a drawn line the moment one lands on screen — and their y positions are STRATIFIED down the screen, not seven independent rolls, which had put two beams ten pixels apart at similar angles. They sit behind the ball and deliberately do NOT travel with it: a beam comes from a fixed lamp.
  (3) PAUSED = DULL AND UNLIT: `live` now animates to 0, not 0.15. `lit` is threaded through `MirrorBall` into `MirrorFlash`, so the caught mirrors fade out and the ball drops back to its bare mosaic; `BallSheen` and `LensFlare` are wrapped in the same gate (they are light PLAYING on the ball, so nothing may still be sliding across a stopped ball); `VolumetricRays` and `BallBloom` likewise, being light LEAVING it. With the streaks, bokeh and light field already gated, a paused ball now sits in a genuinely dark room.
  (4) THE ROOM'S REFLECTIONS TRAVEL WITH THE BALL: the light field used to orbit on its own independent 26s loop, so the dots drifted while the ball was stopped and moved at their own rate while it turned. `fieldSpin`/`fieldRotate` are gone; the layer now rides `spin` itself via `fieldDrift = spin.interpolate([0,1] -> [0, winW])` — one screen-width of travel per turn, in the direction the surface travels (right). Two `LightField` copies a screen apart (`offsetX` 0 and -winW) make the wrap seamless. Same Animated.Value as the ball, so they cannot drift apart or outlive it.
- DONE 2026-07-28 (MIRROR BALL round 20 — REBUILT AS CHROME, mood becomes lighting, OTA): owner sent two reference photos (a real ball throwing dozens of thin shafts, and a lens glint) with a full brief: "the mood should control the lighting, not the mirror ball itself", "the silver chrome should always remain visible beneath the coloured reflections", "the mirror ball should almost never be the brightest thing on screen". This SUPERSEDES the colour direction of rounds 11-19 — every one of those pushed the station's colour further into the material, and the cumulative result was a coloured sphere rather than a mirrored one. Round 19's `stationShade()` is DELETED; do not reintroduce a per-station base ramp.
  MATERIAL: `SHADE_ANCHORS` is a fixed slightly-cool silver, #07080d -> #ffffff. The WIDE range is the point — a narrow ramp reads as matte plastic. Per-tile brightness scatter is pushed toward the ENDS of its range (`Math.sign(roll) * |roll|^0.68 * 4.6`) rather than clustered around the mean, because a real mirror reflects a completely different part of the room from its neighbour; the owner drew this as a checkerboard of dark and light squares and it is most of what sells chrome. Colour cast now applies to about a QUARTER of tiles (`hash01 > 0.74`) at 0.16-0.44, not all of them at 0.24-0.80 — that is what keeps bare silver visible.
  LIGHTING: `ColorCycleWash` (three full-ball colour slabs) is GONE, replaced by `ColourReflections` — five soft coloured patches, r 13-28 in a 100 viewBox, each breathing on its own period. Radius matters: at r=40 one patch is 80% of the diameter and you are straight back to a tinted sphere. They do NOT ride the spin (a lamp is bolted to the room). LAMPS: the key light is now essentially white (sat 0.05); the other two carry the mood at 0.52/0.40. `dbShade`'s station-coloured stop removed and `dbWarm` (a hardcoded orange!) deleted. NeonSweep's peaks halved to 0.10-0.13 and it is now gated on `lit` — two colour systems on one surface add up to the whole-ball wash both exist to avoid.
  NEW `MirrorGlints`: eleven four-point stars (two crossed HAIRLINES plus a small hot core — anything thicker reads as a plus sign drawn on the ball) that flash for ~0.55s on their own long, mutually non-multiple periods, so the pops look random with nothing random at runtime. They sit ABOVE MirrorFlash, with a small blown-out white core on the hotspot, because both are the light itself rather than a lit surface. Placement is polar with rr <= 0.40, which keeps every arm inside the ball's clip.
  NEW `LightShafts` replaces `VolumetricRays`: 22 hairline beams radiating from the ball, whole fan turning on 74s. TWO THINGS LEARNED ON THE RENDER: (a) the first cut was up to 24px wide and read as a flat starburst — widths are now `size * (0.004 + hash^2.6 * 0.020)`, i.e. ~1-6px; (b) a single hard-edged bar still reads as a drawn line however thin, so each shaft is TWO gradients — a 6x-wide copy at 0.22 standing in for the bloom (no blur filter exists) with the bright core inside it. Each is a full-diameter view rotated about its own centre with the gradient in its top half only, because RN has no transform-origin.
  ROTATION: `BALL_SPIN_MS` 6000 -> 15000 (4 RPM, inside the owner's 3-5). Pausing now STOPS EXACTLY WHERE IT IS — the old 1100ms coast meant pause/resume never landed on the same angle. Scrub already drove the turn both ways.
  ROOM: LightField spread wider (x1.35/x1.25) and made small-and-bright with a 1.6x shadow radius — rendered fat they read as coloured bubbles floating in front of the ball; the big soft shapes are BokehField's job. Counts 12->16 and 7->9.
  HARNESS: scratchpad/ball/room.js mocks the WHOLE mode in HTML from the component's own maths (tiles via ts.transpileModule as before, plus the shaft/glint/patch parameter generation copied across) at 393x852. That is what caught both shaft problems and the bubble-dots; the tiles-only render could not have. Keep it in step with the component by hand — it is a mock, not a transpile, for everything except buildSphereTiles.
- OTA COMMAND CORRECTION + MAC SETUP (2026-07-28): the owner switched to a MacBook and asked how to ship from it. Setting it up turned up a REAL BUG IN OUR OWN INSTRUCTIONS: from Expo SDK 55 onwards `eas update` REQUIRES `--environment <name>` and IGNORES local `.env` files entirely (docs.expo.dev/eas/environment-variables/usage + /faq; the SDK<=54 fallback-to-.env behaviour is what everyone remembers and it no longer applies). Cruise FM is SDK 56, so every place that documented the bare `eas update --branch preview` was wrong — AGENTS.md and docs/launch/ota-updates.md are now fixed; `.github/workflows/ota-update.yml` was already correct. WHY IT MATTERS: without the flag the bundle publishes with EMPTY EXPO_PUBLIC_SPOTIFY_CLIENT_ID/SECRET and looks completely successful, then nobody on the channel can sign in to Spotify. Verify with `eas env:list --environment preview --include-sensitive --format short` before publishing from any new machine — PRESENCE IS NOT ENOUGH: a var stored with `secret` visibility still appears in `env:list` by NAME but eas-cli cannot read it back (only EAS Build can), so it inlines as empty while a name-only grep reports green. That was the bug in our own workflow's guard; it now asserts a readable, non-masked value and anchors the match to `^KEY=`. CONSEQUENCE FOR SECOND MACHINES: `.env` does NOT need copying between computers — the keys live server-side in EAS; `eas env:pull --environment preview` fetches them into `.env.local` if the app is to be run locally. New guide: docs/launch/mac-setup.md. Facts verified against the repo rather than guessed: React Native 0.85.3 and metro both declare `engines.node: ^20.19.4 || ^22.13.0 || ^24.3.0 || >= 25.0.0` (so "Node 20" is NOT sufficient — the patch numbers bind), `package-lock.json` is committed so `npm ci` is the right install, and `main` is 161 commits behind `claude/cruise-fm-v4wk5f` — a fresh `git clone` lands on `main`, so the checkout step is load-bearing or the owner would silently publish a months-old bundle to their phone and every TestFlight tester. Guide uses `npx eas-cli` rather than a global install (the official Node .pkg makes `npm install -g` fail with EACCES, which is where non-coders get stuck), and tells the owner to check `node -p "process.arch"` prints arm64.
- OTA COMMAND — WHAT ACTUALLY HAPPENS IF YOU FORGET `--environment` (2026-07-28, verified by reading eas-cli 21.3.0's own source, `npm pack eas-cli@21.3.0`; docs.expo.dev returns 403 through this proxy so the shipped code is the authority): the docs say the flag is "required", which reads as "it will error and you'll notice". IT DOES NOT. `update/utils.js` `environmentFlagNeededForSdk550OrGreater()` triggers an INTERACTIVE PICKER — and the picker's answer is a red herring, because `commands/update/index.js:190` keys the env loading on the raw FLAG, not the prompt: `const maybeServerEnv = environmentFromFlags ? {...await getServerSideEnvironmentVariablesAsync(), EXPO_NO_DOTENV:'1'} : {}`. `environmentFromFlags` is destructured at line 134, BEFORE the prompt at line 152 assigns to a different variable used only for update metadata. So you can be prompted, pick "preview", publish successfully, and still ship EMPTY Spotify credentials. Owner-facing rule now in both guides: IF A LIST OF ENVIRONMENTS APPEARS, THE FLAG WAS FORGOTTEN — Ctrl+C and start again. Worse in CI: that same function returns false whenever `CI` or `EAS_BUILD` is set (GitHub Actions always sets CI=true), so in the workflow there is no error, no prompt and no warning at all — only the explicit flag prevents it. Comment added at the Publish step saying exactly that.
- SECRET-VISIBILITY TRAP (same session, same method): `eas env:list` WITHOUT `--include-sensitive` returns secrets too and prints them by name with a `*****` placeholder (`utils/variableUtils.js:31`), while `loadServerSideEnvironmentVariablesAsync.js` filters secrets out (`byAppIdWithSensitiveAsync` then `.filter(({name,value}) => name && value)` — secrets come back `value: null`). So a var stored as `secret` passes a name check and publishes blank. Our workflow's guard had exactly this hole; it now uses `--include-sensitive --format short`, anchors on `^KEY=` (the old substring grep would have matched a stray `..._OLD`) and fails on an empty or `*****` value. NOT CONFIRMED and worth checking before the next publish: whether the two Spotify vars are actually plaintext/sensitive rather than secret in the `preview` environment. Builds would work either way (EAS Build can read secrets), so the working TestFlight build proves nothing about this.
- SMALLER CORRECTIONS from the same verification pass, all now fixed in docs/launch/mac-setup.md: `eas login` defaults to BROWSER login (`commands/account/login.js`, `browser` flag `default: true`) — it does not prompt for a password in the terminal, so don't tell the owner to expect one. There is NO separate Apple Silicon Node installer: nodejs.org/dist lists only `osx-x64-pkg` for macOS (a legacy label on what is now a universal .pkg) plus arm64/x64 TARBALLS, so "download the Apple Silicon .pkg" is unactionable — if `node -p "process.arch"` says x86_64 the cause is Rosetta on Terminal.app and the fix is to untick it, not to reinstall. `eas update` refuses to run on a dirty working tree and offers "Commit changes to git?" which runs `commitAllFiles: true` — the owner is told to answer NO. `eas env:pull` writes `.env.local`, and eas-cli #2834 reports `.env.local` still being read during updates despite the documented exclusion, so the guide says delete it when finished. Also: `main` is ~160 commits behind but its last shared commit is 21.07, i.e. SIX DAYS not "months" — the guide now names the concrete stake (publishing from main reverts the 27.07 background-timer crash fix onto every tester) instead of a commit count that drifts.
- DONE 2026-07-28 (Mirror Ball round 21 — soft glints + Sunset's real hues, OTA; owner shipped round 20 and sent a close-up + the flare reference): three asks. (1) GLINTS WERE HARD: the round-20 MirrorGlints were literally two 1px solid Views crossed plus a solid white circle — on device that is a plus-sign drawn on the mirrors (owner screenshot proves it). Rebuilt as ALL FALLOFF: halo+core is one radial gradient per glint (per-glint ids `dbGlintHalo${i}` — 11 separate Svg roots, duplicate ids across roots render blank), arms are LinearGradients transparent at BOTH tips, inset from the box, vertical arm reaching further than horizontal (an even cross reads as a marker). (2) TOO FAST: 170ms in / 380ms out read as strobing — now 1100ms in / 1600ms out on Easing.inOut(sin), periods 3600+0..4600ms. A glint is a slow shine, not a camera flash. (3) SUNSET LIT COLOUR: the two coloured LAMPS used stationPalette indexes 4/7 = LIGHTENED eq[1]/eq[2], so Sunset's lit mirrors came out washed coral. Now indexes 0/6 = raw eq[0]/eq[2] at sat 0.58/0.46 (Sunset: true amber + magenta), and ColourReflections weights the OUTER stops via eq[[0,2,1,0,2][i%5]] with peaks 0.20-0.36 — the outer stops are where a station's character lives; the middle one mostly averages them. Chrome-base rule from round 20 untouched: all of this is light, none of it is material. room.js mock updated in step (soft glints + patch colours).
- DONE 2026-07-28 (Mirror Ball round 22 — the dark-teal tiles on Sunset, OTA): owner screenshot showed steel-teal mirrors scattered through the silver on Sunset AM. CAUSE: round 20's "slightly cool silver" SHADE_ANCHORS — slightly cool literally means blue-tinted, invisible on cool stations, but sat over Sunset's warm backdrop those blue-greys read as TEAL. A hue that only shows on half the stations is worse than no hue. FIX: every structural colour on the ball is now DEAD NEUTRAL — SHADE_ANCHORS pure greys (#0a0a0b..#ffffff), FACET_DIRS dark stops #05060d→#060606, ball body #0a0912→#0b0b0c, LightFace base #08070f→#080809, dbShade darks neutralised, seam strokes #07070f→#08080a, meridian lit stroke #e8eeff→#eeeff2, and stationPalette's deep-variant mix target #141726 (a NAVY — it was dragging every station's deep variant off-hue) → #161617. The metallic look survives entirely on range + scatter, exactly as the round-20 note predicted. MEASURED on the room.js mock: pixels with B−R>12 inside the ball went 4.4% → 0.0% on Sunset; Night Run unchanged (its blue arrives via its own eq casts/lighting, which is the rule working). RULE: nothing structural on this ball may carry a hue — if a tint is wanted, it goes through the lighting layers.
- DONE 2026-07-28 (owner's daily-use round 1 — sheet gesture trap, YOUR STATIONS block, intro animation removed, all OTA): three reports from real use. (1) PLAYLIST SHEET DRAGGED THE PAGE AWAY: PlaylistSheet has no gestures of its own and is drawn inside screens that DO (StationDetailModal + every mode's pull-down dismiss). Whenever its ScrollView declined a vertical drag — list at top, or shorter than maxHeight 300 so never scrollable — the parent claimed it and pulled the whole page out from under the sheet. FIX: a PanResponder `gestureTrap` on the sheet body (`onStartShouldSet false`, `onMoveShouldSet` when |d|>6, refuses termination) that swallows declined drags before the parent sees them. Lives INSIDE PlaylistSheet, so every caller everywhere is fixed at once — don't re-fix this per-screen. The list still scrolls because the deeper responder is asked first. (2) STATIONS PAGE — custom stations no longer interleave with the default AM stations by frequency (26.07 behaviour): they now sit in their own "YOUR STATIONS" block under the defaults, each group still dial-ascending. New `SubHeader` row carries the railLine so the dial's left edge runs through unbroken. Verified in the real web build with a seeded `cruise_custom_stations` localStorage entry (Playwright addInitScript — the trick for testing custom-station UI on web with no device). (3) OPENING ANIMATION REMOVED: BrandIntro.tsx DELETED and unmounted from _layout. It was a JS rAF loop calling setState every 33ms during the busiest moment of boot — jagged by construction, unfixable, and the native expo-splash-screen underneath already shows the identical still logo, which is what the owner asked for. assets/images/intro/ left in the repo (orphaned; contains the layer-generation scripts) — delete whenever convenient.
- DONE 2026-07-28 (STATIONS PAGE REBUILT — the receiver design, owner-driven through ~12 prototype rounds, OTA): the card grid is replaced by the head-unit design the owner settled via HTML mockups (harness in scratchpad/stations-proto — build*.js scripts render real station data + the DSEG fonts + MCI glyphs in Chromium; reuse it for any future page-design session, it made same-day iteration possible). FINAL RECIPE: photo-glass rows (station artwork under a horizontal dark-glass gradient, deepest at the left), REAL seven-segment dial numbers in premium white with unlit ghost segments behind (DSEG7Classic-Bold), amber DSEG14 band letters with '~~' ghosts ('~' is DSEG14's all-segments-on glyph), white names + white icons in a FIXED right-edge column (icon slot 28, control slot 16 — columns must stay strict), chevron/padlock outermost, ONE glow on the page (tuned station: full-height red line on the card's left edge + station-colour rim + white-hot number), locked FM dimmed to 0.72 + darker scrim but keeping photo/colour (shop-window rule), custom stations = colour-wash cards with MINE chip + emoji-in-ringed-badge (or white MCI glyph for new ones), title 'Now tuning' (poetry subtitle deleted), OnAir banner kept. FONTS: assets/fonts/DSEG7Classic-Bold.ttf + DSEG14Classic-Bold.ttf + DSEG-LICENSE.txt (SIL OFL, from the npm 'dseg' package), loaded via expo-font useFonts — fonts-as-assets are OTA-safe, no fingerprint change; until loaded, numbers fall back to Fonts.mono and ghosts don't render. NUM_COL_W=64 is shared across BOTH bands so all digits right-align and every name starts at the same x. DELETED from stations.tsx: DialRow/rail/freq styles, the old CustomStationCard, PremiumShimmer lock overlay, POETRY rotation, StationCard usage (the component itself LIVES ON — cruise.tsx home strip uses its compact variant). Verified in the real web build with a seeded custom station. NOTE: taglines are gone from rows by design — they live in the OnAir banner and the station detail page. Owner journey for the record: gradient names were tried and rejected ('looks like word design'), full-LCD names rejected, icons went white and moved left→trailing→fixed right column, marker went side-tab→full-width bottom line→full-height left line.
- DONE 2026-07-28 (MODES PAGE joins the receiver family + shared card size, OTA): owner reviewed a full receiver-style Modes prototype (preset numbers P1-P8 in DSEG14, tick scales) and REJECTED the radio hardware for this page — 'it's the modes page, not the stations space' — but took the furniture: CompactModeCard now has the stations card's exact anatomy — bare white icon (no ring; compactIconWrap/silverIconWrap styles deleted) in a fixed 28pt column at the RIGHT edge, chevron (or padlock when locked) outermost in a 16pt slot, hairline rim rgba(255,255,255,0.14), 18pt radius, and descs clipped to ONE line so every card lands at the same height. Mode gradients KEPT (owner call). Vinyl's icon 'album' → 'record-player' — with the rings gone, 'album' vs 'disc' were two near-identical circles. SHARED CARD SIZE: both pages' cards are now minHeight 84 / paddingVertical 20 / radius 18 — stations was 78 and still read shorter; the comment in stations.tsx says keep them in step. Featured Cassette card untouched. Prototypes that drove this: scratchpad/stations-proto/build8.js (also contains the custom-station colour-boost strips — owner has NOT yet picked a strength; 'boosted' [8C/47/1A wash] is the recommendation, apply when they confirm).
- DONE 2026-07-28 (MODES CARDS -> the 'F' glass finish, OTA; owner picked it from six directions after supplying a reference photo of a lit glass pane): the colour slabs are gone. Each card = its mode gradient SMOKED toward slate (`smoke()` = mix 0.42 into #4a4f62 then 0.06 into white — the exact mute the Cruise page's Recommended cards use) on the DIAGONAL, under a new `GlassPane` layer stack in modes.tsx: the home cards' GlossSheen recipe inlined (top sheen 0.20->dark 0.16, corner catch) + a top-edge hairline whose brightness PEAKS at a per-card specular point (position derived from the title hash, so it's stable but different on every card — one identical highlight on 7 cards reads as stamped), an Svg radial bloom sitting on that point (ids namespaced `ghs${uid}` — duplicate ids across Svg roots render blank), and two cool cyan glints near the bottom corners from the owner's reference. Rim = the card's own 1px border at white 0.30; radius 18->22 (the home cards' geometry); black drop shadow on a WRAPPER (overflow:hidden clips shadows on iOS). Mirror Ball's gradient became smoked silver hexes (#c8d2e2/#9aa6ba/#5a6274) through the same pipeline — silverCard style + the rgba gradient deleted, silverGlow + glitter kept. GlossSheen import dropped from modes.tsx (component itself still used elsewhere). EXTENDED same day: the glass stack moved to a SHARED component `src/components/GlassPane.tsx` (exports GlassPane + mixHex + smoke + specularSpot), and the YOUR STATIONS custom cards on the stations page now wear it too — smoked diagonal ramp derived from the station's chosen colour (light/base/deep via mixHex toward #fff 0.18 / raw / #0b0d16 0.38, each smoked), glass rim 0.30, per-card specular from specularSpot(name), Svg ids namespaced `yst<id>`. The full-colour C4/73/2E wash they wore for about an hour is superseded. REJECTED on the way here, owner-reviewed: E (smoke with no glass — 'flat and matte'), B tinted glass, C neon rim, D edge light, and the receiver/preset-numbers direction ('it's the modes page, not the stations space'). Prototypes: scratchpad/stations-proto/build9-11.js.
- DONE 2026-07-28 (CHANGE MOOD -> CHANGE MODE, owner's call after seeing the combined-sheet prototype: 'it just adds too much clutter... if they do want to change the mood, they can just exit and start again'): the in-drive pill now changes the VISUAL, not the station — one pill, one job; the stations page is the mood chooser. NEW `src/components/ModeSheet.tsx`: same sheet chrome as the old MoodSheet, horizontal shelf of glass chips (smoked gradient + sheen + hairline topline, active ringed white, locked shows a padlock), free modes first. `MODE_LOOK` in that file duplicates the modes-tab colours/icons — keep in step when a mode is added. NEW `np.setMode(mode)` on NowPlayingContext: swaps `session.mode` ONLY — music completely untouched (np.open would restart station music), preview recomputed so a free user picking a premium mode gets the usual taste-then-gate. All 8 modes swapped mechanically (`<ModeSheet visible={showMood} onClose>` — the showMood state name survives), MoodSheet.tsx DELETED. Tuner note: MoodSheet was also Tuner's cross-band station picker; that job now belongs to the dial alone, which is what the dial is for. Verified end-to-end in the web build: open Equalizer from the Modes tab -> Change Mode -> pick Vinyl -> deck swaps in place, same station, music state preserved. The S3+S1 hybrid sheet design (mode shelf + scrolling receiver list) is RETIRED unbuilt — prototypes remain in scratchpad/stations-proto/build12-13.js if it's ever revisited.
- DONE 2026-07-28 (CD spins like the record, OTA): the CD's horizontal swipe-scrub is replaced by the vinyl's ROTATIONAL gesture, owner's ask ('the CD should respond to the full turn like how the vinyl performs'). Drag anywhere on the disc and it follows the finger around its centre (atan2 from the case's measured centre, shortest-way-round angle diff so the ±180° seam doesn't read as a reverse spin), 360° = 5s of song — the vinyl convention — with the same light haptic tick per 5 wound seconds and the same tap-to-play/pause (judged by pixel travel, NOT degrees — near the centre a wobble reads as many degrees; the vinyl lesson). Wired through CD's existing wall-clock phase machinery (phaseRef/readPhase — never reads back a native value) and the useTrackClock scrub API. TAP TRAP: scrub.end() ALWAYS seeks, so a tap must bypass it entirely (togglePlay flips `playing` and the clock effect re-syncs) or every play/pause stutters the song with a stale seek. The disc now claims its touches on START like the record does, so swipe-down-dismiss doesn't work from the disc itself — the chevron and the area around the case still dismiss. Verified in the web build: a ¾-turn circular drag rotates the hub and the mode survives; tap toggles.
- DONE 2026-07-28 (stranger-walkthrough fixes, OTA): a fresh-profile web walk of the first ten minutes (screenshots in scratchpad/stranger/) found the first minute genuinely strong — skip the platform sheet and you're inside a full mode in ~40s with zero setup, because 'Skip for now' saves platform 'none' and the playlist gate deliberately stands down for non-Spotify listeners (that's DESIGNED — needsPlaylist = !linked && spotifyPlatform). Three fixes shipped: (1) 'Welcome back' greeted first-time users — now 'Welcome' until a cruise exists (lastCruise is the signal); (2) WakeSpotifyHint grew a second voice: `connected` prop — not-connected track-less drives get 'Visuals only for now — play music in any app and cruise on. Connect Spotify from the home page for full control.', auto-fades after ~9.5s (nothing to resolve, so it must not sit all drive); all 8 modes now pass show={playing && !track && !handoff} connected={spotify.connected}; (3) ConnectSpotifyCard carries the beta line 'Connections are in limited beta — if sign-in doesn't complete, you still get the full visual experience.' — the dev-mode 403 must read as a known limit, not a broken app. NOT FIXED, noted: the platform sheet's ✕ dismisses WITHOUT saving 'none' while Skip saves it, so two dismissal paths yield different gate behaviour later; PREMIUM badges on station-page mode chips during launch-free are deliberate (shop window).
- APPLE MUSIC — GREENLIT TO BUILD NOW (owner, 29.07: "it's almost August... best to get the app done, so we can fail faster and improve faster"). Owner-side steps written up in docs/launch/apple-music-setup.md (enable MusicKit on com.driftlore.CruiseFM App Services + have an Apple Music sub on the test phone). BUILD PLAN, in order, honouring the fingerprint rule: (1) JS-FIRST, ALL OTA-SAFE: refactor the mode-facing playback layer (useSpotifyPlayback is imported by all 8 modes + home) into a platform-agnostic hook behind musicPlatform.ts, with Spotify as the first implementation — zero native edits, shippable and testable before any build; (2) the AppleMusic implementation of that interface against a thin native bridge INTERFACE (JS side complete, returns unavailable until the module exists); (3) ONLY WHEN CUTTING THE BUILD: the native MusicKit module + app.json entitlement/usage-string/plugin edits, batched with expo-sharing + react-native-view-shot + dropping expo-audio (see NEXT FRESH BUILD list). Estimate given to owner: ~2-3 working sessions of code, 1-2 build cycles, plan-around-a-week elapsed, range 2-3 days to 2 weeks if MusicKit misbehaves. Apple review nudge advice given same day (submitted 23.07, ~6 days waiting = longer than Apple's usual 24-48h): check the submission isn't sitting in a needs-action state, then request EXPEDITED REVIEW via developer.apple.com/contact/app-store/?topic=expedite citing first-launch timing; do NOT cancel/resubmit (resets queue position).
- OWNER DECISION 2026-07-28 (music ceiling): owner was reminded of the 5-user Spotify dev quota ('that's embarrassing... the self-animating modes on top of music they can't control is a bit embarrassing when the app itself looks premium') and CONFIRMED Apple Music/MusicKit as the priority build so launch users aren't waiting on Spotify slots. Their idea, recorded: an in-app 'notify me when connections open' capture + notification when a music connection becomes available for them. MusicKit is a NATIVE integration → goes in the NEXT FRESH BUILD batch below.
- DONE 2026-07-29 (APPLE MUSIC step 1 — the music switchboard, OTA): playback is now platform-agnostic at every call site. NEW `src/utils/useMusicPlayback.ts` — the switchboard every mode + home + NowPlayingHost talks to; it calls BOTH `useSpotifyPlayback` and the new `useAppleMusicPlayback` unconditionally (rules of hooks) and returns Apple's result only when saved platform === 'appleMusic' AND the installed build carries the native module, so behaviour today is byte-identical to before. NEW `src/utils/useAppleMusicPlayback.ts` — a STRUCTURED STUB with the full interface (connected/track/contextName/shuffleOn/repeatMode/play/pause/next/prev/shuffle/repeat + `available`), documenting the native bridge contract (NativeModules.CruiseMusicKit: requestAuthorization/currentEntry/play/pause/next/previous/seekTo/setShuffle/setRepeat/playPlaylist/userPlaylists). All 13 call sites swapped mechanically (10 UI files use the hook; ShareCard/ModeActionRow/useTrackClock import the NowPlaying/RepeatMode types, now re-exported from the switchboard). Local variable is still named `spotify` in the modes — historic, renaming ~hundreds of refs was pure churn. Verified in the web build with zero page errors. NEXT STEPS for Apple Music: (2) real useAppleMusicPlayback against the bridge interface + connect flow UI + apple playlist linking; (3) the native Swift module + app.json entitlement/plugin — FRESH BUILD ONLY, batched.
- DONE 2026-07-29 (APPLE MUSIC step 2 — the whole JS side, OTA): everything except the native module now exists. NEW `src/utils/appleMusic.ts` — the peer of spotify.ts, a typed front door to `NativeModules.CruiseMusicKit` where EVERY call no-ops safely when the module is absent (`safe()` wrapper + `appleMusicAvailable()`), because this ships OTA into builds that don't have it. Playlists share the LinkedPlaylist store with Spotify, told apart by a `applemusic:playlist:<id>` uri prefix (`isApplePlaylist`/`applePlaylistId`). `startApplePlaylist` returns Spotify's StartResult vocabulary so the player's existing notices work unchanged; 'no-device' can never occur (playback is local, not a remote device). NEW real `useAppleMusicPlayback` — same shape as useSpotifyPlayback so the switchboard's callers can't tell them apart; simpler by design (no wake nudge, no start-result reporting, no playlist-context chasing — MusicKit plays on THIS phone). CRASH RULE OBEYED: its 5s poll is pollRef + AppState-gated exactly like Spotify's (bug_type 309 SIGKILL lesson; any repeating timer added here must do the same). ROUTING: `playStationMusic` in NowPlayingContext now takes an Apple branch FIRST when the build has MusicKit and the saved platform is appleMusic — a Spotify-uri playlist saved earlier returns 'no-playlist' rather than failing silently, since relinking is the fix. UI: NEW `ConnectMusicCard` (home, Apple-only, red) which NEVER auto-prompts — Apple's single dialog fires on a deliberate tap (owner decision 29.07), and it distinguishes DENIED from authorised-but-no-subscription, saying so plainly. ConnectSpotifyCard now stands down for appleMusic listeners (two connect cards = noise). PlaylistSheet lists the user's Apple playlists and HIDES the paste box for them (Apple has no shareable playlist link — offering one is a dead end). StationDetailModal copy is platform-aware, and the strict playlist gate extends to Apple ONLY when `appleMusicAvailable()` (gating a visual-companion listener would block a drive we could have shown them). VERIFIED in the web build, zero page errors, on BOTH paths: today's Spotify user (untouched) and an appleMusic-platform user on a module-less build (degrades to visuals exactly as before). Storage key is `cruisefm_platform` — worth remembering when seeding tests. REMAINING: step 3, the native Swift module + app.json entitlement/usage string — FRESH BUILD ONLY, batched.
- DONE 2026-08-01 (SHARE CARDS → FIVE DESIGN STYLES + a Pinterest pin, OTA): owner asked for design styles "like how Spotify lets users share design cards", and for the artwork to be savable as a Pinterest pin. NEW `src/components/ShareCardStyles.tsx` owns every layout; `ShareCard.tsx` keeps only the sheet, the capture plumbing and the picker. THE ONE INVARIANT: every style draws the SAME `ModeHero` (ShareModeArt) and differs only in how the card is FRAMED around it — the mode is the part nobody else has, so it is never the variable. NOW PLAYING (the shipping card, unchanged in look) = full-bleed station photograph, mode on it, song credited under. POSTER = station name as the HEADLINE at 88pt with the song demoted to a credit, photograph left much brighter (0.30/0.16/0.40/0.95 vs Now Playing's 0.46/0.30/0.52/0.90) because a poster IS the photograph. TICKET = the drive as a stub: rounded panel, dial number in dot-matrix, the mode in a window, torn perforation with two notch circles filled in the page's own dark, song printed on the counterfoil over a barcode. RECEIVER = the card as a head unit, matching the Stations page and the Tuner: brushed-metal hairlines, amber dot-matrix frequency, recessed window, dot-matrix song line, dial and red needle. MINIMAL = no photograph at all, the station's colours as a gradient, the mode floating in them, song set large with a lot of air. PIN FORMAT: same 1080 width, height 1350 (4:5) or 1620 (2:3); `FORMAT_H` is the only switch and every y in every style is derived from `cardH`, never hardcoded. SAVING: iOS shares a real PNG so the OS share sheet's Save Image puts it in Photos ready to pin; Android still shares text only until `expo-sharing` lands (already item 1 on the fresh-build list), and the sheet SAYS so rather than offering a button that won't work.
  FOUR THINGS THE OFFLINE RENDER CAUGHT, all the same class of bug — a block sized against an ASSUMPTION rather than a measurement: (1) the ticket's dial and station name shared a row with the name budgeted against a fixed 340px, but "103.5 FM" is half as wide again as "810 AM" in dot-matrix, so "Mountain Pass FM" ran straight through its own dial number — they get a row each now, which cannot collide whatever the widths; (2) the ticket's and the receiver's windows were sized off the panel's WIDTH, making them 592 and 639 tall, which pushed the counterfoil through the barcode and the song readout through the dial — both are now sized off their own HEIGHT and the hero is scaled to fit, centred; (3) a taller pin gave its extra 270px to whatever gap happened to be at the bottom, so the ticket opened a dead band above the barcode — the extra now goes into the window (0.62/0.60 of it) and into Minimal's hero scale, never into a gap; (4) Minimal at 82pt ellipsised a song title mid-word, so it is 72pt over three lines.
  IN-APP: the sheet grew a scrolling row of style chips and a Card 4:5 / Pin 2:3 toggle. TRAP: the chip ScrollView must be given a REAL width (`winW - 32`) — inside a centred column with no width of its own it sizes to its content, so five chips ran off both edges instead of scrolling. Checked in the real web build at 393x852 and 375x667, all five styles plus both formats, zero page errors; and offline across Night Run, Sunset, Mountain Pass (the pale station), a custom station with no photograph, and the no-track fallback. Poster skips its credit block when there is no live track, because the title falls back to the tagline it already prints above.
  STILL SVG PRIMITIVES ONLY, so this is a normal OTA — no view-shot, no new dependency. `heroTransform()`/`<G transform="translate(...) scale(...)">` is how a style repositions the hero; react-native-svg 15 parses string transforms (extractTransform.ts:174), and the harness emits them as plain SVG attributes so both paths agree. HARNESS: scratchpad/share/render.js now takes a job — `styles` (every design, one station), `pins` (the same at 2:3), `modes` (every mode in one style, `STYLE=` env) — and transpiles ShareCardStyles' own source, so it tests the shipped code. Run it with `NODE_PATH=/home/user/CruiseFM/node_modules`.
- THE PHONE WENT QUIET 2026-08-02 — a day of OTA updates published successfully and reached NOBODY, and the cause is a trap this repo had not written down. The owner: "I think we've been building on the wrong build I just updated to build 14 just now." Correct. MEASURED with the workflow's own diagnose mode (Actions → Ship OTA update → mode `diagnose`, which prints each iOS build's number beside its Profile and Channel — this is the tool for any "did it arrive?" question): build 14 = profile `production`, channel **production**; build 12 = profile `testflight`, channel **preview**. The production BRANCH has never carried a single update ("Recent update groups:" empty, Runtime Version N/A), so build 14 was running its own baked-in bundle from commit a1abf9f, thirteen commits behind, and every publish that day had gone to preview.
  ROOT CAUSE, and it is a documentation failure rather than a code one: the resubmission steps written on 02.08 correctly say to build the store binary with `--profile production` — but EVERY EAS build shows up in TestFlight, so the owner installed it, and a production-profile build listens on the production channel. Nothing warns you. The app simply stops improving, which is indistinguishable from "the fixes didn't work". Note this is NOT the pinned-runtime trap (runtimeVersion was 1.0.0 on both builds and matched perfectly) — the runtime was never the problem, the CHANNEL was, and the workflow's runtime guard passed happily because a preview build was indeed listening.
  RULE, now written into docs/launch/app-store-submission.md next to the build command: the owner's phone stays on the newest `testflight`-profile build; a `production`-profile build is for the store and must never be installed from TestFlight onto the testing phone. Getting back is free — TestFlight keeps the older build under **Previous Builds**, and reinstalling it rejoins the preview channel, at which point the already-published updates land on the next launch. The alternative, publishing to the production channel, is the one thing to weigh rather than reach for: it changes the app Apple is looking at if that build is in review.
  RESOLVED SAME EVENING, and the production channel was deliberately published to: the owner's point was that build 14 is the binary APPLE will run, and she wanted the reviewer to see the song picker and the mood picker rather than the barer version baked into it. The standing "never publish to production during review" note was raised first and she reaffirmed the request, so it stands as her call, not a slip — recorded here because the next person will read that rule and should know it has been consciously set aside once. VERIFIED after publishing rather than assumed: `eas channel:view production` now shows an update group at Runtime Version **1.0.0** against build 14's own Runtime Version **1.0.0**, an exact match, which is the only thing that decides whether an update is ever fetched. Worth knowing for next time: OTA is check-on-launch / apply-on-NEXT-launch, so the app has to be opened, fully closed and opened again before the change appears — on a phone or on a reviewer's device.
- APPLE MUSIC FAULTS ROUND 2026-08-04, off build 19 (OTA): the song list said "no playable songs" in a playlist that was AUDIBLY PLAYING. The screenshot located it precisely — the sheet opened and routed as Apple (so canList and the platform split worked), meaning the emptiness came from the READ. Cause, proven by reading not guessing: SongListSheet passes the FULL `applemusic:playlist:<id>` uri as the playlistId, `playAppleTrack` strips the prefix, but `getApplePlaylistTracks` handed it RAW to the bridge — whose MusicLibraryRequest id filter can match no playlist whose id literally starts "applemusic:". One-line fix (normalise inside getApplePlaylistTracks, accepting either form) and it is pure JS → OTA to build 19. RULE: any function taking a playlist identifier must accept the uri AND the bare id — the caller cannot be trusted to know which it holds.
  ARTWORK SETTLED BY THE INSTRUMENT 2026-08-04: the check reported `artwork MISSING` with songs listing fine — so MusicKit returns nil from BOTH `entry.artwork` and `song.artwork` for library-playlist tracks, a known MusicKit gap for library items. FIX (native, needs the NEXT BUILD — build 20): third fallback through MediaPlayer, which watches the same system player — `MPMusicPlayerController.applicationQueuePlayer.nowPlayingItem?.artwork` holds the actual IMAGE, rendered once per item to a cache file and returned as file:// (RN's Image loads it like any url). On MainActor; keyed on persistentID so it writes once, not per 5s poll. VERIFY after the build with the same quick check — the line should flip to `artwork yes`.
  (superseded detail) ARTWORK was still missing after the round-1 native attempt (entry.artwork then song.artwork, both nil). NOT guessed at a third time: `diagnoseAppleMusic()` in appleMusic.ts is the Apple twin of diagnoseSpotify — access status, subscription, playlist id asked, songs returned, and whether currentEntry carries an artworkUrl — surfaced via the same "Run a quick check" button in the sheet's Apple empty state. A screenshot of that output decides whether artwork is a native-returns-nil problem (next-build Swift work) or a JS mapping problem (OTA).
  IN-REVIEW CONSTRAINT: build 18 (in review) carries the same prefix bug, and the fix CANNOT be published to the production channel until the review ends (no mid-review production publishes). After approval, publish to production BEFORE the owner presses Release so day-one users never see it. Reviewer exposure is low — reading an Apple playlist requires linking one, which requires a subscription.
- ONE SERVICE AT A TIME, IN THE INTERFACE TOO 2026-08-04 (OTA): owner — "the app should pivot once Apple Music or Spotify is selected… I've accidentally pressed Spotify connect multiple times where I either unlink Spotify on accident or link Spotify when I'm using Apple Music. The link Spotify should come under the Spotify option, rather than having its own section." Correct, and it is the SAME mistake the playback switchboard was making underneath (see the both-players entry): the app offered two live services at once. `SpotifyConnectRow` sat in Profile directly under Music Platform on EVERY platform, so an Apple Music listener had a Spotify connect/disconnect control one thumb-width from the row they actually wanted. It is now gated on `platformId === 'spotify'` — the connection belongs to the CHOICE, not to the app at large. ConnectSpotifyCard on the home page already stood down for Apple listeners (checked, not assumed), so Profile was the only leak. RULE: any per-service control belongs inside that service's context and must be invisible when another service is selected.
- BOTH PLAYERS WERE RUNNING AT ONCE 2026-08-04 (OTA) — the owner's screen recording of a Spotify drive on build 22 settled this in eight frames: the transport alternated play/pause and the clock ran 0:09 → 0:06 → 0:10 → 0:11 → 0:12 → 0:13 → 0:10 → 0:15. Two sources reporting one song. CAUSE: `useMusicPlayback` passed the same `visible` to BOTH `useSpotifyPlayback` and `useAppleMusicPlayback`, and the comment above it claimed the unselected one "idles cheaply". It does not — each hook polls its own service and calls `adoptPlayState` to mirror reality, so on any build carrying MusicKit both were writing the SHARED play state from different services. Spotify said playing; Apple's leftover queue said paused; the app flapped, and the modes' animations (which gate on `playing`) lagged and stuttered along with it. FIX: `useSpotifyPlayback(visible && !useApple)` / `useAppleMusicPlayback(visible && useApple)` — one flag gates the poll, the adopt AND the AppState listener, because all three are keyed on it. Until the saved platform loads, Spotify is active, which is the historic default. LESSON: two hooks that both write shared state are not "one active, one idle" unless something actually stops the loser; a comment asserting it is not a mechanism.
- SCRUB, ROUND 2 — THE OTHER TWO MODES 2026-08-04 (OTA): routing `useTrackClock` by platform fixed CD and left VINYL broken, because Vinyl predates the shared clock and imports `seekTo` from spotify.ts DIRECTLY (two call sites: the record's rotational scrub and the seek bar). The bar moved, the record turned, and the song snapped back to its real position. CASSETTE had the identical import and was never reported — found by grepping `seekTo` across src/components once Vinyl proved the pattern, not by waiting for it. Both now call the exported `seekActive` from useTrackClock. THE RULE IS WORTH REPEATING BECAUSE IT WAS WRITTEN THE DAY BEFORE AND STILL MISSED THREE FILES: nothing below the UI layer may import a platform's transport directly. `grep -rn "from '@/utils/spotify'" src/components` is the check — anything transport-shaped in that list is a bug waiting for a second platform.
  PLAYLIST PICKER wore Spotify green on an Apple Music list (tick, active row, spinner) — it now takes Apple's red via the `apple` flag the sheet already had for hiding the paste box.
- HORIZON REBUILT: STATIC SCENE + NATIVE-DRIVER OVERLAYS 2026-08-04 (OTA) — owner: "horizon mode... currently experiencing lag — slowing down the entire app". Correct on both counts, and the cause was architectural: Horizon was the LAST mode animating by re-rendering React — a rAF loop calling `setPhase` ~15×/s, which re-rendered the ENTIRE fullscreen component (controls, action row, everything) plus a ~90-element SVG tree through react-native-svg's reconciler, all on the JS thread. A saturated JS thread lags the whole app, not just the mode — every tap and JS-driven animation anywhere queues behind it. The 24.07 25→15fps throttle halved the burn but kept the architecture. THE REBUILD: almost nothing in this scene moves — sky, sun, slats, spill, horizon line and rays are now STATIC (three stacked Svgs, drawn once per station/orientation); the only motion is (1) RollingLines — 11 plain Views on ONE looping native Animated.Value, each line's translateY/opacity/scaleY sampled into keyframe interpolations from its fixed journey (q = frac(offset + t), y = HORIZON + q^2.2·GRID_H); (2) TwinkleStars — the old per-frame star math had exactly FIVE distinct rates (0.7 + (i%5)·0.23 rad/s), so five shared loops serve every star, each star reading its bucket through its own sine keyframes; (3) the sun glow's pause-dim — the old `amp` was constant 1 while playing and 0.4 paused, i.e. never per-frame at all, so it is one animated opacity wrapper. Zero JS-thread work per frame. GEOMETRY RULE: the scene Svg's viewBox must EQUAL the window (`makeGeom(winW, winH)`, both orientations — GEOM_WIDE/GEOM_PORTRAIT deleted), because the overlay Views share the scene's numbers as screen pixels; a fixed-size geom under `slice` would rescale the Svg but not the overlays and the lines would float off the grid. THE TRAP THAT COST A ROUND: `Animated.loop(Animated.timing(v, {toValue: 1}))` — a sawtooth that RELIES on loop's reset — ran ONE pass and parked at 1 (measured: the probe's computed transform froze at the journey's exact end). Every loop proven in this app wraps an `Animated.sequence` that travels back down by itself; a sawtooth needs its restart written out — `.start(({finished}) => { if (finished && alive) run(); })` with `v.setValue(0)` at the top, no value read-backs (the Mirror Ball stall rule). Verified in the web build: lines measured moving, stars twinkling, landscape docks correctly, zero page errors. NOTE for measuring: 11 evenly-offset lines map onto themselves every cycle/11 ≈ 300ms, so two samples at a near-multiple read as "not moving" — the round-12 aliasing trap again.
- THE "NO UPDATES ARRIVING" RECOVERY PATH IS ALREADY ON THE PHONE (2026-08-04): when the owner reports updates not landing (twice today), check the server side with the workflow's diagnose mode FIRST (it verified: updates live on preview at runtime 1.1.0, builds 17-22 all listening) — and then remember that BUILD 22 BAKES IN both `UpdateCheckRow` (Profile → Settings card → "Check for updates": checks, downloads with visible status, second tap restarts into the new code) and the Profile footer version line (`appVersionLabel`: "base build" = no OTA ever applied vs "updated <date, time>" = which one). Both committed 30.07, ancestors of build 22's commit 1eacf99 — verified with `git merge-base --is-ancestor`. So there is no catch-22 of "the fix for updates ships by update": the button predates the drought. The footer line is the instrument for "which code is my phone running?" — ask for it before theorising.
- APPLE MUSIC ALBUM ART — THE PHONE DOES NOT HAVE THE PICTURE 2026-08-04 (OTA): third round on blank artwork in Vinyl and CD, and the first two both failed for the SAME unexamined reason — they asked the DEVICE for an image the device does not hold. Route 1 was MusicKit's `entry.artwork`/`song.artwork` (nil for library items, measured 04.08). Route 2 was MediaPlayer's `nowPlayingItem.artwork` (build 22+, still nil). The likely cause of route 2, and worth stating because it kills the whole family of local fixes: a STREAMED library is made of CLOUD items — the song is in the library but neither the file nor its artwork is on the phone, so there is no local image to return. Downloading every album is not something to ask of a driver. ROUTE 3, which ships: `src/utils/appleArtwork.ts` asks Apple's own public catalogue (`itunes.apple.com/search`, no key, no token, no permission — the same catalogue the song came from) and returns an https URL that `<Image>` loads like any other. PURE JS, so it reached the build already on her phone instead of waiting for build 24; the two native routes are still tried first because a local image is exact and needs no signal. NOT AWAITED BEFORE THE SONG: `chaseArtwork` runs beside the poll and patches `albumArt` into the existing track when it lands — build 21 already proved what happens when artwork sits between the poll and the title (the whole deck reads "no track" for the drive). The cache key is claimed BEFORE the chase starts so a 5s poll cannot launch the same lookup repeatedly. MATCHING IS THE RISKY PART and it is tested offline (scratchpad/artmatch.mjs transpiles the shipped file and stubs the endpoint — 15 cases): a title matches if it is equal after normalising or one contains the other (a library title is usually the shorter — "Africa" vs "Africa (Single Version)"), and THE ARTIST MUST AGREE TOO or a common title confidently returns the wrong album. NORMALISER TRAPS the tests caught: accents must be folded by an explicit map (`String.normalize('NFD')` is not worth betting on under Hermes) or "Déjà Vu" and "Beyoncé" never match; `&` must become "and"; apostrophes must be DELETED rather than turned into a word break ("Don't"/"Dont"). `artworkUrl100` is upscaled by rewriting the `/100x100bb/` path segment. Misses are cached in memory for the session (never on disk) so a failed lookup is not retried every poll; hits persist to AsyncStorage, capped at 150. NOT VERIFIED LIVE: outbound hosts are blocked from this build environment, so the response shape is from the endpoint's long-standing contract and every field read is defensive — a shape change degrades to no artwork, which is exactly today's behaviour. The quick check now prints a third line, `Catalogue artwork: found / no match`, so a future failure names its own route. PRIVACY: the policy had no Apple Music section at all, and the website's copy had drifted from the source by hand (missing a section the in-app one carried, different date) — there is now an Apple Music section covering the connection AND this lookup, and `scripts/gen-legal.mts` writes `website/` as well as `docs/legal/` so the two cannot diverge again.
- THE QUEUE-SCROLL FREEZE, ROOT-CAUSED BY THE OWNER 2026-08-04, late (OTA): her repro nailed it — "when I scroll fast to look at the queue, sometimes the station card also follows with the swipe and the card collapses to the bottom and then whole screen freezes". MECHANISM: SongListSheet presents in its own iOS window but its React tree lives inside the mode's, so a drag its list declines bubbles to the mode's dismissPan (the 03.08 (1b) finding); the 03.08 gestureTrap catches slow drags on the sheet BODY, but a FAST flick — or one starting on the sheet's backdrop, which the trap doesn't cover — still leaks. The card then dismisses UNDER the open sheet and tearing down both stacked iOS windows at once wedges the screen. FIX IS A RULE, NOT ANOTHER TRAP: while ANY sheet is open above a mode, the card's dismiss gesture STANDS DOWN — every mode's dismissPan claim is now gated on `sheetUpRef` fed from `useNowPlaying().sheetCount` (the counter AutoDim already uses; the responders are built once, so it must be a ref). All 8 modes patched identically; PlaylistSheet (mounted==open, so `useSheetOpen(true)`) and ModeSheet (`useSheetOpen(visible)`) now register in the count alongside SongListSheet and ShareCardSheet. RULE EXTENDED: any sheet that can appear over a drive registers via useSheetOpen — both so AutoDim stands down AND so the card cannot be dragged out from underneath it. Verified: full health walk green, and the 8-mode dismiss harness still passes with no sheet open (the guard must not eat normal dismissal).
- SNAPSHOT SHARE CARDS, ROUND 2 — TWO STYLES, THE REAL THING EVERYWHERE 2026-08-04, late (OTA): the owner saw snapshots on device ("Amazing! It works now" era) and called the line-up: "remove the receiver and sleeve mode and keep the snapshot and the ticket mode". SleeveStyle, ReceiverStyle and their exclusive helpers (ModeStage, BAND_RANGE, LAMP_RED, AMBER, dmFit/dmWidth imports) are DELETED — ~400 lines. SNAPSHOT (final crop, set from the owner's own cropped reference): `CROP_TOP_FRAC 0.120 / CROP_BOT_FRAC 0.118` of a PORTRAIT capture — cuts the status strip, mode label and chevron at the top (picture starts at the YOU'RE-LISTENING-TO line) and the pill row at the bottom, KEEPING the transport; landscape captures aren't cropped. The capture travels with the screen's point size (`ModeSnapshot = {uri,w,h}` from Dimensions.get('screen')); the window is fitted CONTAIN at the visible band's aspect and the clip takes both trimmed strips. The halo glow around the window is DELETED (owner: "make sure the atmosphere stops at the edge of the screenshot border"). No song block on this style: the page IS the song block. FINAL DRESS (owner, same night): NO header of any kind above the window — the page names its own station, printing it twice was clutter — the frame is a thin WHITE stroke (0.30; an accent-coloured frame was tried and rolled back on her call the same night), and the footer is the bare `cruisefm.app`. TICKET REFINED off her Circular EQ share: the full-bleed capture is BIASED DOWNWARD (`TK_TOP_FRAC 0.163`, clamped to the overflow) instead of centred — centring hid the mode's crown under the header — and the ticket's OWN song text is deleted when a capture exists (her "Holiday" ghost-layered under the ticket's "Holiday"; the screenshot carries title + seek bar itself, and she wants the scrub visible). The counterfoil then holds only the barcode (tucked up under the tear at `perfY+42`), the cover tile and cruisefm.app bottom-right, with the bottom fade eased 0.94→0.78 so the capture's song block reads through. Captureless fallback keeps the full counterfoil. TICKET with a capture is FULL-BLEED — the screenshot covers the whole panel edge to edge under the header/tear/counterfoil/barcode ("as if the ticket template just sits over the mode playing"; the framed window cut the mode off) — `slice` cover trims where the status bar and pills live, so the object band fills the card; captureless builds keep PhotoFill + the drawn ShareModeArt hero. Verified offline (render.js fixtures carry a 393x852 stand-in snapshot) + full health walk green.
- SNAPSHOT SHARE CARDS 2026-08-04, late (OTA — the native half has been in every build since 15): task #18 done. The share pill now PHOTOGRAPHS THE RUNNING MODE the instant it is tapped (`grabModeSnapshot` in ModeActionRow — react-native-view-shot's `captureScreen`, raced against 1.5s, every failure → null) and a new SNAPSHOT style — first chip, default when a capture exists — frames the real screenshot in the card: quiet backdrop, phone-shaped window rx46, song block, cruisefm.app footer. THE CROP IS FREE: the window's ratio (660 wide vs the tall screen) under `preserveAspectRatio slice` trims the top and bottom of the capture symmetrically — exactly where the status bar/eyebrow and the transport/pills live — so the object-and-title band survives on any phone shape with no image manipulation. CAPTURE ORDER IS LOAD-BEARING: the screen is photographed BEFORE `setSharing(true)` renders the sheet, or the screenshot would be of the sheet itself. Degradation: web/older builds/native failure → snapshotUri null → the Snapshot chip is hidden and the dispatcher falls back to Ticket, byte-identical to the pre-snapshot sheet (health walk re-run green). The drawn styles (Ticket/Sleeve/Receiver + ShareModeArt) remain both the fallback and the choices. Verified offline via scratchpad/share/render.js (fixtures now carry a stand-in `snapshotUri`); the true end-to-end capture can only be judged on device — owner has it next launch. NOT YET VERIFIED ON DEVICE: that Svg.toDataURL rasterises the file:// capture inside the exported PNG (album-art https images already do, so expected to hold; if the shared PNG's window comes out black, that is the suspect).
- THE "CD DISMISS FREEZE" WAS NEVER A BUG — IT WAS THE TEST LYING 2026-08-04, late (task #27 CLOSED): full health pass (15-step walk, 8-mode rotation sweep, 8-mode dismiss harness, fresh-user run — all green) left one deterministic repro: dismissing CD "froze" the Modes page in scratchpad/dismiss8.mjs. Forensics: the marked scroller after CD's dismissal had scrollTop 470 — EXACTLY max scroll (1322−852) — and every scroll operation on it worked perfectly. The probe tested by ADDING 200 to scrollTop, which at exact max clamps in place, so a fully-healthy page read as FROZEN. Why only CD: closing a mode returns browser focus to the card that opened it, which scrolls that card into view — Vinyl (mid-list) parks the page at 339, CD is the LAST card so it parks at exactly max. dismiss8's probe now tries BOTH directions. TWO LESSONS, both already written down once and worth repeating: a probe must not be able to pass (or fail) vacuously — test the negative direction before crying frozen; and "deterministic, one item only" should prompt "what is special about that item's POSITION", not just its code (CD's only specialness was being last in a list). The owner's real on-device freezes were the genuine bugs fixed 03.08 (terminate handlers, song-sheet gesture tree, auto-dim over sheets) — nothing remains reproducible.
- ARTWORK CONFIRMED WORKING ON DEVICE + THREE POLISH ITEMS 2026-08-04, evening (OTA): owner: "Amazing! It works now" — task #30's covers land on CD and Vinyl. Three adjustments off her screenshots: (1) TONEARM — THREE rounds ending in a SOLID STRAIGHT LINE, her words ("Just make it a solid straight line"). Round 1 hugged the pivot→stylus chord with a gentle S — "sort of jaggered"; round 2 copied her reference photo (straight tube two-thirds down, one sweep into the shell) — still not right; final: every spine point sits ON the pivot→stylus line (−9.4° off vertical), every tangent IS that line, and ARM_HEAD_A shares the same axis, so both cubics degenerate to rules and the arm is one straight rod, pivot to needle. Do not reintroduce an S without her asking. THE CONSTRAINT THAT MATTERS: the stylus landing point is load-bearing, so every reshape was SOLVED to keep S at (−0.158, 1.025)·armLen exactly — any future change must re-solve B/HEAD_A/HEAD_L together or the needle walks off the grooves. (2) SUNSET'S VINYL RING — went dusk-pink → amber '#FFA24B' → and the amber read as MUSTARD on device; the override is now DELETED so the ring falls through to eqColors[1] ('#FF6F5A'), the same peachy accent slot every other mode wears for Sunset. Lesson: don't invent a third colour for a station — the accent slot exists. (3) FIREFLIES SOFTENED — the "bounce back motion" she saw was structural: each flight was FOUR STRAIGHT LINES between random waypoints, snapping direction at every corner. Flights are now two sine harmonics per axis sampled into 18 keyframes — position AND velocity continuous everywhere including the loop seam (whole periods wrap by construction), so there is no corner to bounce off. Loops converted to explicit-restart sawtooths (the Horizon Animated.loop lesson, same day). Palette: ~half the flies now carry the station's colour nearly neat (mix 0.16-0.30 toward white vs the old 0.45-0.5), the rest stay warm-white so the swarm reads as light, not confetti. Verified: sunset ring + arm by web screenshot, firefly motion by the 48-boxes/14-moved probe.
- THE FOUND-BUT-BLANK ARTWORK BUG, SOLVED BY THE INSTRUMENT 2026-08-04 (OTA): fourth artwork round, and the upgraded quick check ended it in one screenshot — `Now playing: … — artwork yes / Backup artwork: found (file…) / Catalogue artwork: found`, ALL THREE routes healthy over a blank Vinyl label. Only one reading fits: the bridge's artwork URL is truthy but UNLOADABLE — MusicKit returns `musicKit://` scheme URLs for library artwork, renderable only by Apple's own ArtworkImage view; RN's `<Image>` draws nothing, silently, and because the string was non-empty the JS fallback chase NEVER RAN. The check itself colluded by printing "artwork yes" — it now prints the RAW url's first 34 chars, and the rule is recorded there: an instrument may never summarise the one detail under investigation. FIXES, all verified end-to-end in the web harness (scratchpad/artpipe.mjs — fake `__fakeAM` bridge via a TEMP hook in appleMusic.ts (added/removed in the same session, never committed enabled), Playwright route interception for the catalogue and image hosts; the musicKit:// case now chases and lands the cover on the deck): (1) `getAppleNowPlaying` nulls any artworkUrl not matching `^(https?|file|data):` so the chase runs; (2) the Swift `artworkURL(for:)` filters the scheme at the source — next build; (3) chase resilience: the artwork claim carries a timestamp and a missing cover is re-chased every ~20s instead of claimed once forever (a chase that died mid-flight — backgrounded app, dropped signal — used to blank the deck for the whole song), the chase's completion is deliberately NOT gated on cancelledRef (a completed lookup is valid data; dropping it left the claim blocking retries), and a network-failed catalogue lookup no longer poisons the session miss-cache (only a genuine no-match is cached). Also on record from this round: the OTA drought ended — the owner confirmed the red playlist cards, so the Profile update button works and the phone is receiving again.
- A `color` PROP ON AN ICON IS NOT THE LAST WORD, AND THAT IS WHY THE GREEN SURVIVED 2026-08-04 (OTA): owner, after the picker round above — "I still haven't got the Apple Music colour on the playlists, they're still green". Two things, and the second is the reusable one. (a) The station page's linked-playlist CARD was never touched: the previous round changed the little note icon and nothing else, so the wash, the border, the pencil and the toast were all still hardcoded green. It now takes one `PLATFORM_TINT` table (solid/wash/washHero/rim/rimStrong per platform) applied at the call site, so the card, its hero state, its icons and the toast cannot drift apart again. (b) THE ICON FIX HAD NEVER RENDERED AT ALL. react-native-vector-icons builds its style as `[{fontSize, color}, style]` — the STYLE WINS OVER THE `color` PROP — and both `styles.playlistBtnIcon` (StationDetailModal) and `ps.check` (PlaylistSheet) carried a hardcoded `color`, so a per-platform colour passed as a prop was silently discarded. Colour for these icons must come from the style, or from a style with no competing colour in it. SWEEP DONE: a script matched every `<MaterialCommunityIcons|Ionicons|…>` carrying BOTH a `color` prop and a `style={x.key}` whose stylesheet entry defines `color` — exactly one other hit (ThemeGenerator's `pv.playIcon`), and both colours there are `#fff`, so it is harmless. Re-run that sweep rather than eyeballing if a colour prop ever appears not to take. VERIFIED by forcing `appleMusicAvailable()` true in the web build (temporary, reverted) and seeding `localStorage['cruisefm_platform']` — the card measured `rgba(250,36,60,…)` on Apple Music and `rgba(29,185,84,…)` on Spotify. NOTE the platform key is stored as a BARE STRING, not JSON: seeding it JSON-encoded silently falls through to the Spotify path, which cost a run.
- THREE OFF A REAL APPLE MUSIC DRIVE 2026-08-04 (OTA): (1) SCRUB NEVER SEEKED. `useTrackClock` imported `seekTo` from spotify.ts and called it directly, so on Apple Music the record and the disc turned under the finger and the song stayed put — the gesture was fine, the seek went to an app that was not playing. New `seekActive()` in that file routes by platform (cached at module load, re-read after each Spotify seek so a mid-session switch lands on the next scrub rather than never). RULE: nothing below the UI layer may import a platform's transport directly — go through the switchboard or a router like this one.
  (2) A PLAYLIST SAVED FOR THE OTHER PLATFORM IS NOT A LINK. Stations kept showing their Spotify list while the listener was on Apple Music: it looked linked, Start Drive went ahead, and `playStationMusic` then refused it because an Apple player cannot open a `spotify:` uri — the card and the drive disagreed. Both now judge it the way playback does (`linkUsable` in StationDetailModal, `usableUri` in ModeActionRow): a link counts only if it belongs to the platform in use, the sub-line NAMES the mismatch rather than hiding it ("… is a Spotify playlist — pick an Apple Music one"), and the note icon takes Apple's red instead of Spotify's green. NOT FIXED, and worth doing properly later: linked playlists are still stored in ONE slot per station, so switching platform loses the other side's choice. Per-platform storage is the real answer.
  (3) CD FREEZE still hit-and-miss on build 21 — the terminate fixes helped but did not finish it. Task #27 stays open; the reproduction harness is scratchpad/dismiss8.mjs.
- APPLE MUSIC USES THE **SYSTEM** PLAYER, NOT THE APPLICATION PLAYER 2026-08-04 (native, build 22+): owner — "when i swipe Cruise FM to close, it closes the music also". Exactly what `ApplicationMusicPlayer` does: the queue lives in OUR process and dies with it. The deeper problem is the driving case — anything iOS does to a backgrounded app takes the music with it, and switching to Maps mid-drive is the NORMAL case, not an edge one. `SystemMusicPlayer` hands the queue to the Music app: survives force-quit, survives backgrounding, needs no background-audio capability (the one Apple rejected build 7 over), and matches the arrangement Cruise FM already has with Spotify, where their app plays and we are the visual layer. THE TRADE, accepted: the listener's Music app now shows what Cruise FM started, because it genuinely is playing it — which is also what makes the lock screen and CarPlay controls work during a drive. Implemented as ONE `private typealias CruisePlayer` so the choice lives in a single place; if it is ever reverted, `MPMusicPlayerController.systemMusicPlayer` in the artwork fallback must go back to `applicationQueuePlayer` — the two must always name the same player or artwork reads from the wrong queue.
  NOTE this may also be why artwork read as missing on builds 19-21: the MediaPlayer fallback asked `applicationQueuePlayer` for the now-playing item, and that is a different queue from the one the listener could hear if anything had shifted playback. Both now name the system player.
- SUBMITTED TO APP REVIEW (SECOND ATTEMPT) 2026-08-04: **build 18**, production profile/channel, version+runtime **1.1.0**, cut from current code — carries MusicKit/Apple Music, the freeze fixes, the song list, the fireflies, the platform-picker restyle and the hidden Premium card. It REPLACES build 14 (1.0.0, submitted 02.08): the owner removed that submission from review knowingly — the queue reset was weighed and accepted, because 14 could never carry Apple Music and Spotify's 5-user cap makes MusicKit the launch story. Notes for Review kept the 2.5.4 explanation + no-sign-in walkthrough, plus one line about optional Apple Music via MusicKit. Manual release. RULES WHILE IN REVIEW: no `production`-channel publishes at runtime 1.1.0 (that would change the app under review — preview stays safe); if still Waiting for Review after ~2 days (06.08), request expedited review at developer.apple.com/contact/app-store/?topic=expedite citing first-launch timing; do NOT cancel/resubmit. The owner's phone stays on testflight-profile builds (17+, preview channel) — build 18 must not be installed from TestFlight.
- PLATFORM PICKER CAUGHT UP 2026-08-03 (OTA): owner spotted the "Connect Your Music" sheet still wearing the pre-July look — violet sheet rim, a violet glow orb, six brand-coloured slabs and a violet gradient CTA — months after the rest of the app went to dark glass. It is the one screen a first-time user sees before anything else, so it was setting the wrong expectation for everything behind it. Now: sheet `#0a0a10` with a white hairline (the Profile page's black), cards `rgba(255,255,255,0.05)` + a 0.12 rim, the brand colour surviving only on the icon chip, the tick and a whisper of gradient on the SELECTED card, and the CTA as the app's standard solid-white pill with dark type (GlossSheen import dropped with it). COPY WAS ALSO OUT OF DATE and that mattered more: Apple Music read "Visuals now · full control coming" — written before MusicKit existed, and truncating to "full cont…" on a real phone. With the 1.1.0 build it is simply 'Full in-app control', and the blurb now says Spotify and Apple Music both play inside Cruise FM. NOTE this update publishes at runtime 1.1.0, so it reaches build 16 and later ONLY — build 12 and the production build 14 are on 1.0.0 and will never see it.
- APPLE MUSIC STEP 3 — THE NATIVE MODULE 2026-08-03 (FRESH BUILD REQUIRED, not OTA): `modules/cruise-music-kit/` implements the bridge contract `src/utils/appleMusic.ts` has documented since 29.07, name for name. It is an EXPO local module (expo-module.config.json + podspec + one Swift file), autolinked — verified with `npx expo-modules-autolinking search --platform apple`, which lists it. CONSEQUENCE WORTH KNOWING: an Expo module is NOT in React Native's `NativeModules` map, so the JS lookup now goes through `requireOptionalNativeModule('CruiseMusicKit')` from expo-modules-core — that returns null rather than throwing when absent, which is the exact contract this file needs since it ships OTA into builds without the module. The legacy NativeModules lookup is kept as a fallback.
  SWIFT: MusicKit throughout rather than mixing MPMusicPlayerController with ApplicationMusicPlayer — `MusicAuthorization` for the prompt, `MusicSubscription` to tell authorised-but-not-subscribed from denied (the connect card says which), `ApplicationMusicPlayer.shared` for transport, `MusicLibraryRequest<Playlist>` for the user's own playlists. Most of it is `@available(iOS 16.0, *)`, degrading to no-ops below that. TRAP recorded in the file: a library playlist arrives WITHOUT its tracks, so `playPlaylist` must call `.with(.tracks)` before queueing or it starts nothing at all. Every function degrades rather than throws — a thrown promise mid-drive surfaces as a dead transport with no explanation.
  app.json: `NSAppleMusicUsageDescription` ONLY — set declaratively, no config plugin needed. **DO NOT ADD `com.apple.developer.applemusic`.** Build 15 failed on exactly that: "Entitlement com.apple.developer.applemusic not found and could not be included in profile. This likely is not a valid entitlement and should be removed." Native MusicKit — `MusicAuthorization` + `ApplicationMusicPlayer` + `MusicLibraryRequest`, i.e. everything this module uses — needs the usage string and NOTHING else; the user's own subscription is what authorises catalogue playback. That entitlement belongs to the MusicKit *web/REST* flow with developer tokens and a private key, which Cruise FM does not use. Corroborating signal in the same log, worth reading next time: `Synced capabilities: No updates` — EAS did not recognise it as a capability to enable either. `version` AND `runtimeVersion` bumped together 1.0.0 -> 1.1.0, which the pinned-runtime rule requires whenever native code changes — it is the only thing stopping an update landing on a build that lacks the module. VERIFIED with `npx expo config --type introspect --json`: usage string present, entitlement present, and NO UIBackgroundModes (the key that got build 7 rejected).
  BATCHED INTO THE SAME BINARY: `expo-sharing` ~56.0.23 and `react-native-view-shot` ^5.1.1. VERSION TRAP worth remembering — `npm install expo-sharing` grabbed **^57.0.8**, i.e. the SDK 57 line, because expo-* packages are versioned to their SDK; every other expo package here is ~56.x, so it had to be pinned to ~56.0.23 by hand. `npx expo install` would have picked it correctly but the registry call failed through this proxy. Android's share now goes through `Sharing.shareAsync(path, {mimeType:'image/png'})` — RN's own Share cannot attach a file there — with the text form still behind it as the fallback. react-native-view-shot is installed but NOT yet wired: having the native library in the binary is what matters, and the capture rewrite in ShareCard is pure JS, so it can ship OTA afterwards without another build.
  BUILD ON THE `testflight` PROFILE, NOT production, or the owner's phone leaves the preview channel (see the 02.08 entry). Verified before handing over with `npx expo config --type introspect --json` (usage string, entitlement, no UIBackgroundModes, 1.1.0/1.1.0) and `npx expo-modules-autolinking search --platform apple` (cruise-music-kit and expo-sharing both listed; view-shot is not an Expo module and autolinks through the RN CLI instead).
- MIRROR BALL: FIREFLIES 2026-08-03 (OTA), owner: "let's go softly into the mirror ball… add more glitter floating and moving around. It doesn't have to be moving in the same direction as the spinning sphere. It can be as simple as moving fireflies when music is playing." NEW `Fireflies` — 14 soft motes that WANDER the room, gated on `live` so they only appear with the music. THE POINT IS WHAT THEY DON'T DO: the room already had glitter that twinkles in place and a static ray burst, so the ball was the only thing moving; every earlier attempt to fix that made the room travel WITH the ball and each one ended up reading as a second light system arguing with the first (see the scrolling-field and CastReflections entries, both retired). A firefly answers to nothing, which is exactly why it sidesteps that whole trap — and it is what the owner asked for. COST: five shared loops however many are drawn. Each fly picks a loop and traces its OWN path from it, so bucket-mates never move in step; the path returns to its start at t=1, so a LINEAR loop cycles seamlessly with no retracing (a sine here would make every fly pause at the same two points — the easing has to live in the path, not the timing). Brightness reads the same value through different stops so a fly doesn't dim at a fixed point on its journey. Same soft radial-gradient recipe as GlitterSpeck — never a hard circle. VERIFIED in the web build by snapshotting every small glow box twice 2.6s apart: 48 boxes, exactly 14 changed position, i.e. the fireflies travel and the glitter correctly stays put.
- SONG-LIST SHEET LOSES ITS DRAG 2026-08-03 (OTA), after the owner reproduced the freeze exactly: "I was looking at the upcoming songs and I swiped down to close the tab but it brought the station card with me as well and now all the buttons can't work." So the 28.07-style `gestureTrap` added earlier that day was NOT sufficient on its own — the sheet's own header drag is a second downward gesture living inside the mode's React tree, and the two can disagree about who owns a swipe. HER FIX, and it is the right one: "make the cross at the top right hand corner more obvious when leaving the queue, rather than putting both the top tab end and the cross." The grabber, the `drag` PanResponder and their styles are DELETED — SongListSheet now has NO gesture of its own, so there is nothing to hand to the wrong owner, and the ✕ went 28pt/0.08 fill/16pt glyph -> 38pt/0.16 fill + rim/21pt white glyph. RULE for any future sheet that opens from inside a mode: give it a button, not a drag. The trap stays, to swallow whatever the list declines.
- RECEIVER SHARE CARD — ROOM TO BREATHE 2026-08-03 (OTA): owner, on the card (not the Tuner mode — note the two exist separately, see the 03.08 tonearm lesson): "the top station information looks cramped can we free up some space between the text?" Measured: the frequency's 124-tall digits ended NINE pixels above the station name, so the three rows read as one block. Glass GH 276 -> 330 with the rows re-spaced off GY (30 / 94 / 258). That height has to come from somewhere: the hero moved down with it and shrank (0.76 -> 0.645 of card width), and the SONG BLOCK IS NOW ANCHORED UP FROM `dialY` instead of down from the hero — hanging it off the hero is why the artist line landed on the dial's tick marks the moment the hero changed size. Verified by rendering all eight modes through scratchpad/share/render.js (`STYLE=receiver node render.js modes night-run`, then screenshot the .html — the harness writes HTML, it does not shoot it).
- TWO OFF A REAL DRIVE 2026-08-03, evening (OTA): (1) THE EQUALIZER ONLY ANIMATED IF YOU PRESSED PLAY. Owner: "it only animates when you press play even though a song is running… you'd have to stop the song and replay, but replaying takes a second — which makes it seem like Spotify snoozed off". Real, and Equalizer was the ONLY mode with it: its bar loops were driven IMPERATIVELY from exactly two places, `togglePlay` and the open effect, with a third for orientation. So every OTHER route into a playing state left the meter dead — opening onto music already going, the mini-player's play button, and the AppState foreground re-sync adopting Spotify's own state. Replaced with ONE declarative effect on `[visible, playing, isLandscape]`; `togglePlay` no longer touches the bars at all, so a new caller cannot miss it. Checked the other seven while in there: Cassette and Vinyl have real `[playing]` effects, Horizon and Circular EQ read a `playingRef` inside a continuously-running rAF loop (so a change takes effect the next frame, no restart needed), and CD/Mirror Ball gate on a `spinning` derived from playing. Only Equalizer was hand-wired.
  (2) THE MOODS WERE BURIED. Owner: "all mood backgrounds have been heavily blurred and darkened… it sort of makes the mood themes insignificant." MEASURED before touching anything, which split it into two separate causes: the pre-blurred assets in `assets/stations/blur/` retained only 4-8% of the originals' detail (matching a ~radius-8 gaussian on a 1080-wide image) while their mean luminance was IDENTICAL to the sharp originals — so the blur was the assets and the darkness was NOT. Assets regenerated at RADIUS 3.5, picked by rendering 8/5/3.5/2.5 side by side on four stations and looking: at 2.5 the detail starts competing with the mode's own object, at 5 the scene still reads as colour rather than place. 612KB -> 774KB total, all still JPEG and well inside the ~600KB-per-file rule. NOTE the live `blurRadius` prop stays unused for stations that have a blur asset — re-blurring on the main thread every re-display is what got the app SIGKILLed once already (see StationBackdrop's comment); to change the blur, regenerate the assets. The DARKNESS is the 5-stop scrim, which only EqualizerMode and VinylMode carry (the other six rely on the backdrop alone) — eased 0.20/0.15/0.30/0.46/0.58 -> 0.10/0.06/0.18/0.32/0.46, keeping most of the weight at the foot where white type and the transport sit and giving up the top two thirds, which carry nothing but the picture.
- FOUR ITEMS OFF A REAL DRIVE 2026-08-03 (OTA), and item 1 is a bug that had been latent in ALL EIGHT MODES since the day pull-to-dismiss was written.
  (1) THE PAGE FREEZES AFTER DRAGGING A CARD AWAY. Owner: "Every time I leave the station card… by dragging the card down to the bottom. The whole page likes to freeze and not let me scroll immediately… I keep swiping up and down to get the page moving, or I cross out the song playing, or I eventually leave and fully close the app." Every clause of that is a symptom of ONE cause. iOS CANCELS a touch that leaves the bottom edge of the screen rather than releasing it — and dragging a card "down to the bottom" is exactly that gesture. All eight modes' `dismissPan` defined `onPanResponderRelease` and NOTHING ELSE, so a cancelled gesture simply stopped: `slideY` stayed parked wherever the finger left it, the mode stayed `visible` with its content translated off-screen, and its transparent modal window stayed over the app. Taps fell through to the page beneath (hence the tab bar still working — which she used as her "is the app dead?" test) while scrolling did not; swiping again re-grabbed the stranded card and closed it properly; the mini-player ✕ ended the session and unmounted it; force-quitting did the same. FIX: every mode now has `onPanResponderTerminationRequest: () => false` plus an `onPanResponderTerminate` that runs the SAME `settleDismiss(g)` as release — past the threshold it closes, otherwise it springs back, so the card can never be left stranded. The close also goes through a `dismissCloseRef` now, because these responders are built once with `useRef` and were closing over the first render's `winH` (wrong after a rotation). CD's own disc responder had a related defect — its terminate branch sprang back unconditionally instead of settling — and now shares the one rule via `settleDismissRef`.
  STILL OPEN, AND REPRODUCIBLE: in the web build, dismissing **CD** leaves the Modes page's scroller refusing `scrollTop` — deterministically, CD only (the other seven pass), not order-dependent, not transient at +4s, with no full-screen layer left behind, no stranded modal, and `body{overflow:hidden}` identical across all eight so that is not it. Harness scratchpad/dismiss8.mjs (opens each mode, drags off the bottom edge, then probes every scrollable div). CD is the only mode whose object claims touches on START with capture and refuses termination, which is the obvious suspect but is NOT yet proven. Do not "fix" it by guessing — reproduce with that harness first.
  (1b) AND THE OTHER HALF OF IT, 03.08 after the fix above went out — owner: "CD worked fine it was scrollable once and then the page froze. So it's kind of on and off", plus "when I scroll through to change songs… the card likes to also scroll down with the tab". Her screenshot shows the Modes page visible ABOVE an open mode, i.e. the card sitting ~130pt down its own screen with the song sheet open over it. SCROLLING THE SONG LIST WAS DRAGGING THE MODE. iOS presents SongListSheet in its own window, but RN's responder system is TREE-based, not window-based, and the sheet's React tree sits inside the mode's — so a drag the ScrollView declined (list already at its top) bubbled up to the mode's `dismissPan`. That also explains the "on and off" freeze: the card gets left part-way down without ever crossing the dismiss threshold, which is the stranded state from (1) by a different route. FIX: the same `gestureTrap` PlaylistSheet got on 28.07, on the sheet body — the list still scrolls (deeper responder asked first) and the header's drag-to-close still wins. NOT REPRODUCIBLE ON WEB: a control run with the trap removed also held the card, so RN Web does not model this. Shipped on the strength of the mechanism plus the 28.07 precedent, not on a passing test — if the card still drifts, that is the thing to re-examine.
  (1c) MODES TAB WASN'T STARTING THE MUSIC. Owner: "the redirect doesn't open for … modes page -> stations mood -> plain mode". Correct, and the redirect was a symptom rather than the bug: `modes.tsx` called `np.open(..., { paused: !pending.locked })`, so a free/owned mode opened IDLE and `startStationMusic` never ran — no playlist, no wake, no music. That flag made sense while tapping a mode meant "let me look at this one", but since the mood sheet landed (03.08) you choose a mode AND a station before anything opens, which is a drive by any reading. Now opens unpaused, exactly like the Stations page. Note the old expression was also backwards on its face: previews (locked) auto-played while the modes you actually own did not.
  (2) MOOD PICKER HID THE TAGLINE. `StationSheet`'s active row was a solid `#ffffff` fill, and `tagline` is `rgba(255,255,255,0.42)` — white at 42% ON white, i.e. literally invisible, so the station you had chosen was the one you could read least. Replaced with the Stations page's own "tuned" language: a 4px bar down the left edge in the station's accent, an accent rim, an accent checkmark, name and dial to full white, tagline lifted to 0.66. Row keeps the dark glass, so every row is the same colour, the same height and legible. The row needs `overflow:'hidden'` for the edge bar to clip to its 14px radius.
  (3b) REDIRECT ON THE FIRST PLAY, owner's refinement 03.08 after the conditional version above: "I only want the redirect for the first play. Like a new opened app or even after a time limit." NEW `src/utils/spotifyWake.ts`, and the whole design is in when it does NOT fire — a redirect on every play would throw people out mid-drive, which is the opposite of what it is for. Two signals only, both meaning "Spotify has probably dozed off": the first play after a COLD START (module state, so it resets itself), and the first play after the app has been BACKGROUNDED for more than 15 minutes (AppState listener). A mid-drive station switch is neither, so it stays out of the way exactly where the music is definitely already awake. `markSpotifyWoken()` is called by BOTH branches, so one redirect per stretch of use at most.
  NOT A HANDOFF, and this is the load-bearing part: `handoff` is only ever cleared by the NEXT start attempt (`reportStartResult`), so marking a proactive redirect as one would drop the listener back into a "Playing in Spotify" panel with a dead transport even though Spotify was now awake and the API would work. It is a new `'waking'` StartResult instead — a plain note saying what happened, no handoff flag, transport stays live, and the existing poll adopts the track when it arrives.
  (3)+(4) SPOTIFY SNOOZING, and these were ONE bug. Owner: "at what time does Spotify like to knock off… when I switch stations Spotify likes to snooze, then it becomes a game of opening, closing and switching apps", plus a request to redirect to the Spotify playlist on the first station tap. Spotify drops off Connect once its own app has been paused and backgrounded for a while, so `startPlayback` comes back `'no-device'` — and `playStationMusic` returned that verdict STRAIGHT OUT while `'error'` and `'restricted'` fell through to `openInSpotify(uri)`. So the one case where waking Spotify was the entire fix was the one case we didn't wake it: we showed a notice telling her to go and do it by hand, which is precisely the errand worth deleting. One-line fix — `'no-device'` now falls through to the deep link too, which wakes the app AND starts the right playlist in a single tap, then the existing "Playing in Spotify" handoff panel explains itself. DELIBERATELY NOT an unconditional redirect on the first station tap as asked: throwing people out of the app even when playback would have worked is the opposite of keeping them in it, and in practice the first tap IS the no-device case, so the conditional version fires exactly when it helps. The `'no-device'` notice now only appears if the deep link itself fails, and says so.
- THREE BUGS OFF ONE DRIVE 2026-08-03 (OTA) — owner: "the app keeps pausing, crashing and preventing me from swiping, when I pull a card down… and when I tried to change the song, the playlists songs aren't found". Two real bugs and one non-bug, and the interesting part is that ONE sentence described two separate faults.
  (1) THE PULL-DOWN THAT PAUSED THE MUSIC. The disc's rotational gesture (03.08) claims touches on START and sets `onPanResponderTerminationRequest: () => false`, so ANY touch beginning on the disc owned the gesture for good. Pulling down therefore never reached the mode's own dismiss — and worse, a short pull fell under the 12px tap threshold on release and hit togglePlay, so the pull-down PAUSED the song. Every word of the owner's sentence, from one cause. The record has been built the same way since 22.07, so it was fixed identically. FIX: the disc's own responder now classifies the drag and owns both outcomes — within ~35 degrees of straight down (`|dy| > |dx| * 1.4`) it drives the mode's `slideY` and closes on the same 120px/0.8 thresholds as the mode's dismiss; anything else winds. The scrub no longer BEGINS on grant (it can't, until the drag is judged), so a pull-down never raises the scrub pill or seeks.
    WHAT WAS TRIED AND MEASURED FIRST, so nobody rebuilds it: a physical discriminator, since pulling down through a disc's middle is RADIAL and winds nothing while a winding drag is tangential. Two rounds of it. Swept ANGLE is useless — near the hub, four pixels of offset swings the angle through tens of degrees (the same reason the tap test is in pixels, not degrees). Arc length in pixels (`|dθ| · r`) fixes that but fails differently: the call is made after only ~16px of travel, by which point a perfectly tangential drag has wound just 16px too, so an absolute threshold called everything a pull-down. A RATIO (arc ÷ travel) is scale-free and finally behaves — and is still wrong, which the harness showed: 90px right of a 150px disc a straight-down drag genuinely IS 0.86 tangential, so physics says "wind" while the person plainly meant "leave". WHERE PHYSICS AND INTENT DISAGREE, INTENT WINS. Knowingly traded away: you cannot wind a disc by dragging straight down; you wind it with a circular sweep or a drag across, which is what anyone does anyway. Harness: scratchpad/classify.mjs replicates the classifier against real 393x852 geometry and prints arc/travel per gesture — reuse it, because THE BROWSER CANNOT TEST THIS: RN Web's event target has no `measure()`, so `centerRef` stays {0,0} and every angle in a web run is nonsense. The final rule needs no centre at all, so drag3.mjs does verify it end-to-end.
  (2) AUTO-DIM FROZE THE APP OVER AN OPEN SHEET — a third modal window. iOS allows the mode's own Modal plus ONE sheet on top; a third never presents and swallows every touch anyway. AutoDim mounts an invisible full-screen catch Modal after 30s, and `activityTick` only fires on PLAYBACK CONTROLS, so reading a song list for half a minute trips it every time. The screen dimmed (brightness really is set to 0.22), the catch layer never appeared, and the taps that leaked through to the disc paused the music. This had been latent since the share card shipped on 26.07; the song list just made 30 idle seconds inside a sheet the normal case. FIX: `sheetCount` on NowPlayingContext + `useSheetOpen(visible)`, which every modal sheet that can appear mid-drive now calls (SongListSheet, ShareCardSheet). AutoDim requires the count to be zero — it is the only one of the three the user did not ask for — and PlaybackNotice/DriveCheckCard defer rather than cancel, appearing when the sheet closes. RULE: any new Modal that can present during a drive either calls useSheetOpen or must not exist.
  (3) "THE PLAYLIST'S SONGS AREN'T FOUND" was mostly an honesty bug. `spotifyFetch` folds EVERY failure into null — offline, 4s timeout, 401, 403, 404, 429 — which is right for the polling paths (a drive must not stop to explain itself) and wrong for a screen someone is reading: `getPlaylistTracks` turned all of it into `[]` and the sheet said the playlist had no songs. NEW `spotifyFetchDetailed()` returns a reason, with its own 12s timeout — the shared 4s exists so a failed START falls over to the handoff path quickly, and it is far too impatient for a list fetched over a moving car's signal, which is the single likeliest cause of what she actually saw. getPlaylistTracks now returns a RESULT, pages to 300 songs, and asks for `total` alongside the items precisely so "genuinely empty" (total 0) can be told from "the projection returned nothing" (total > 0), which retries WITHOUT `fields` instead of reporting an empty list. The sheet names the cause in plain words and offers Try again only where retrying could help. Also: the sheet had a grabber that did nothing — pulling it down now closes it (header block only, so the list keeps scrolling).
  (4) THE TWO .ips FILES SHE SENT ARE NOT A BUG. Both bug_type 309, EXC_CRASH/SIGKILL, codes 0x0, `procRole: "Non UI"` — killed 24 and 43 minutes after launch while BACKGROUNDED, i.e. ordinary iOS reclaiming a suspended app. Audited before saying so: every repeating timer in src/ is AppState-gated (the two polls and Vinyl's spin net), which was the real 27.07 fault, and the image working set is far smaller than it looks — the stations list rows are colour glass, not photographs, so only the hero banner and the current mode's backdrop are decoded, not all ten. Don't chase these; do chase any 309 that lands with procRole "Foreground".
  (5) FOLLOW-UP, same evening: the honest message worked and she immediately asked the right question — "how can I fix this?" The read was coming back 403, and Spotify uses that one status for refusals with completely different fixes, so `classify403` now also sniffs for "scope". A 403 for INSUFFICIENT SCOPE means the token was minted before Cruise FM asked for a permission it now needs — retrying can never change that answer and only reconnecting can, so the sheet offers a Reconnect Spotify button that runs the sign-in from where she is standing (the music is Spotify's, so it keeps playing). Any other playlist 403 means Spotify is refusing that particular list, which for a playlist means one of its OWN — a Daily Mix, Discover Weekly, an editorial list — and the message says so and points at using her own playlist. NOTE the two vocabularies: the START path speaks `StartResult` and has no 'scope' member, so `startResult403()` narrows for it; only `spotifyFetchDetailed` (screens someone is reading) sees the finer answer. RULE this round established twice: a message that only names the problem is a dead end — name the fix, and where the fix is one tap, put the tap there.
  (6) AND THE OTHER HALF OF THE SAME REPORT — "during the drive I tried changing the song but the options didn't come up". `canList` required `contextUri`, which is whatever SPOTIFY reports is playing: empty until the poll answers, and empty for good whenever the context isn't a playlist. The pill then silently fell back to the playlist PICKER, so tapping it opened the wrong sheet. The station already knows which playlist it drives, so ModeActionRow now loads `getStationPlaylist(session.stationId)` itself — no new prop through eight modes and two call sites each — and the song list uses it whenever Spotify's own context isn't a playlist. Only a station with nothing linked (or Apple Music, which has no queue-source id) still opens the picker. Jumping to a track works unchanged, since the linked uri is a perfectly good context to start within. SCOPE HISTORY, checked rather than assumed: `playlist-read-private` was added 08.07 (commit 7291509), and a Spotify refresh token NEVER gains scopes it wasn't granted — it just keeps rolling — so an account that authorised before that date is still missing it today and nothing showed it until something read a playlist's contents. That is the likeliest reading of her 403 on a playlist she owns, and it also explains why the picker still listed playlists (`/me/playlists` degrades to public-only rather than failing).
  (7) BOTH GUESSES AT THAT 403 WERE WRONG, and the lesson is the round's real output. The owner reconnected repeatedly and it never helped: the message stayed on the generic branch, which means Spotify's body did NOT contain "scope", so the insufficient-scope theory is dead — and she had already ruled out the editorial-playlist theory ("Europa is my playlist I made"). Two confident diagnoses, two wasted trips for her, both argued from plausibility rather than evidence. The `fields` projection is now retried bare on any forbidden/error first page in case the projection is what is refused, but the actual fix is to STOP PARAPHRASING: `classify403Detailed` keeps Spotify's own `error.message` and `spotifyFetchDetailed`/`getPlaylistTracks` carry it through as `detail`, which the sheet prints verbatim in small italics ("Spotify said: …"). The forbidden copy no longer offers a diagnosis at all, just the fact and the reassurance that playback is unaffected. Also fixed while in there: `restricted` was being folded into `forbidden`, so a dev-mode allowlist rejection would have shown the wrong explanation entirely; it has its own message now. NOTE the body can only be read ONCE — everything that wants it goes through classify403Detailed, never a second res.text().
  (8) SOLVED 03.08, AND ONLY BY MEASURING. The diagnostic added in (7) settled it in one screenshot: `/me` 200, `/me/playlists` 200, **`/playlists/{id}` 200**, `/playlists/{id}/tracks` **403 Forbidden**, on TWO different owner-made playlists. So it was never the account, the token, the scopes or the playlist — Spotify refuses that one SUB-ENDPOINT while happily serving the playlist object beside it. Every earlier theory was about the wrong noun. THE FIX is to read the songs off the playlist object, which carries its own first page in `tracks.items`.
  AND THE TRAP INSIDE THE FIX, which cost a round on its own: that fallback was already in the shipped code and STILL failed. A bare `/playlists/{id}` expands each track's album and artists AND their `available_markets` — roughly 180 country codes per track and again per album — so a 100-song playlist arrives as MEGABYTES and reading it blew the 12s budget, landing on `reason: 'error'` and falling through to the same forbidden message. Adding `?fields=tracks(items(track(uri,name,duration_ms,artists(name))))` collapses that to kilobytes. RULE: any Spotify read that returns full track objects needs a `fields` projection, not for tidiness but because `available_markets` alone will sink it.
  WHY THE PROBE DIDN'T CATCH IT, worth remembering when writing any diagnostic: the probes only read the STATUS on success and the body on failure, so `/playlists/{id}` scored a clean 200 while the real code path was drowning in the body it never fetched. A probe that doesn't do the same work as the code proves nothing about the code. There is now a fifth line that does the real read and reports how many songs came back.
  METHOD NOTE FOR THE WHOLE ROUND: three confident diagnoses (missing scope, an editorial playlist, then a working fallback) were all wrong, and each cost the owner a trip. What ended it was shipping an instrument instead of a theory — four probes and the granted scopes, printed verbatim. Reach for that on the SECOND wrong guess, not the fourth.
  (9) AND THE ANSWER, 03.08: SPOTIFY WILL NOT GIVE A DEVELOPMENT-TIER APP A PLAYLIST'S CONTENTS, BY ANY ROUTE. The instrument settled it in one screenshot — `/me` 200, `/me/playlists` 200, `/playlists/{id}` **200**, `/playlists/{id}/tracks` **403**, the projected read off the playlist object **0 songs**, and the granted scopes printed in full: `playlist-read-private, playlist-read-collaborative, user-modify-playback-state, user-read-playback-state, user-read-currently-playing`. Every permission we need, granted, and the contents withheld anyway. So the scope theory, the editorial-playlist theory and the which-endpoint theory are ALL dead, and no amount of code changes it. Do not spend another round trying to read a playlist.
  WHAT SHIPPED INSTEAD: the player's QUEUE (`/me/player/queue`, `getPlaybackQueue()`), which is player STATE rather than playlist CONTENT and rides `user-read-playback-state` — demonstrably working, since the whole app is built on it. When the playlist read fails the sheet shows what is queued next under a "Coming up" heading that says plainly that Spotify won't share the full playlist. Tapping one still jumps, because `playTrackInContext` seeks by uri WITHIN the existing context and the queued tracks are in it. Smaller than the feature asked for and honestly labelled as such; the real fix is Apple Music, which has no such tier.
- PICK A SONG WITHOUT LEAVING THE APP 2026-08-03 (OTA): owner, after a real drive — "I wanted to change the song without always hopping over to Spotify… without having too many functions that crowd the card." Skip only walks forward and back, so reaching a particular song meant leaving. NEW `src/components/SongListSheet.tsx`, and the design is entirely about WHERE it lives: nowhere new. The middle pill already names the playlist, so tapping it now opens THAT PLAYLIST'S SONGS instead of a picker of other playlists, and "Change playlist" becomes one row at the top of the same sheet. No new pill, no sixth transport button — the card is unchanged. Deliberately plain: a list and a tap, no search, no reordering, no drag; picking a song is already more interaction than skipping and this is used at the wheel.
  PLUMBING, all of it already permitted by the existing scopes: `getPlaylistTracks(id)` (100 rows, Spotify's own page size; local tracks and podcast episodes have no uri and are dropped), and `playTrackInContext(contextUri, trackUri)` which threads an `offset` through the existing `startPlayback` so its device-waking sequence is reused. THE OFFSET IS LOAD-BEARING: start a track on its own and Spotify forgets the playlist, so next/back stop walking it and the "playing from" pill empties. `useSpotifyPlayback` now exposes `contextUri` (it already tracked it privately) and `NowPlaying` gained `uri` so the list can mark the playing row; `useAppleMusicPlayback` returns `contextUri: null` for parity, since Apple's system player exposes no queue-source id. The pill FALLS BACK to the old picker whenever there is no playlist context — companion-mode and Apple listeners see exactly what they saw before.
  MODAL OR NOT, and the rule is subtler than "never": this sheet IS a Modal, while StationSheet on the Modes tab must not be. The difference is where it opens from. Inside the player's own modal a second one works — `ShareCardSheet` has done exactly that since July — whereas from a TAB page, with `NowPlayingHost` holding a modal elsewhere in the tree, it presents an invisible touch-swallowing window. Being a Modal is also the only escape from the mode's own layer order: `AmbientGlow` is a later sibling and painted its haze straight across the list (seen on the render; a nested zIndex cannot lift a child above an outer sibling). Any modal that can appear mid-drive needs `supportedOrientations`.
  VERIFIED: the no-context fallback drives for real in the web build, and the list itself was rendered by temporarily stubbing seven tracks — the browser has no Spotify, so it is the only way to see the screen before shipping it. Stub removed after. Rotation sweep clean.
- HEALTH CHECK 2026-08-03, and the one real bug it found: `WakeSpotifyHint` sat at `insets.top + 64` in portrait while every mode puts its station identity block at `Math.max(insets.top, 20) + 52` — a block about 46 tall. Those overlap AT ANY INSET, so the hint was lying across the station's own name; I had earlier told the owner this was a browser-only artefact, which was wrong (the arithmetic was never checked). It only shows when there is no track, which is precisely what a first run looks like for anyone who hasn't connected Spotify — so it was one of the first things a new user saw. Now `Math.max(insets.top, 20) + 106`, derived the same way the modes derive theirs so it cannot drift out of step again. HARNESSES worth reusing, all in scratchpad/store: `health.mjs` (15-step smoke walk over every page, sheet, the drive flow and the mini-player, collecting page AND console errors), `fresh.mjs`/`fresh2.mjs` (brand-new-user first run — note the home hero's button reads "Let's cruise", NOT "Start Drive", and it is found by shape rather than text). TWO VACUOUS TESTS TO AVOID REPEATING: clicking where the share button "should" be passes silently because the share pill only renders when `spotify.track` exists, so it can never be exercised in the web build — check the cards with scratchpad/share/render.js instead; and a step that only catches exceptions will report `ok` for a click that hit nothing, so assert on what appeared afterwards.
- MOOD PICKER ON THE MODES TAB + CIRCULAR EQ DETAIL PASS 2026-08-03 (OTA): (1) THE DOORWAY. Owner: "I find it annoying that Night Run FM is the default — which makes me jump back to Spotify or the stations page to get the right mode." Exactly right, and it was a one-word cause: `np.open(mode, undefined, …)` from modes.tsx, and `open`'s signature defaults `stationId = 'night-run'`. New `src/components/StationSheet.tsx` — tapping a mode now asks which mood first, AM band then FM then YOUR STATIONS, each climbing its own dial, matching the Stations page's reading order. Pre-ticked with the last cruise's station (`loadLastCruise`), falling back to `defaultStationForNow()` — the hour's own pick — so the common case is one extra tap on something already highlighted. This does NOT reinstate the in-drive mood sheet dropped on 28.07 ("one pill, one job"); mid-drive the mood is still the Stations page's business. **DO NOT MAKE THIS A MODAL** — the first cut was one, so it could cover the floating tab bar, and the owner reported the app FREEZING on device the moment a mode was tapped. iOS will not stack a second Modal window over the one `NowPlayingHost` already holds for the fullscreen player, so it presented an invisible, touch-swallowing window over the app: no sheet, no response, everything dead. Exactly the trap PreviewGate hit on 24.07 ("iOS won't stack a second Modal window — card never appeared"), and the fix is the same — stay in the page. It works fine in the web build either way, so this can only be caught on device or by knowing the rule.
  CONSEQUENCES OF STAYING IN THE PAGE, both handled: the floating tab bar is a sibling rendered AFTER the screen and floats over the sheet, and so does the MINI-PLAYER when a drive is running — so the list pads past both (`TAB_SAFE_INSET + insets.bottom + extraBottom`, with modes.tsx passing `np.session ? 76 : 0`). Kept mounted through the exit animation via a `mounted` flag or it vanishes instead of sliding away, and the open animation always does `y.setValue(winH)` first from the LIVE window height: relying on whatever the value happened to hold is how a sheet ends up parked below the screen with a live backdrop over the app, which is the same invisible-blocker symptom by a different route.
  (2) CIRCULAR EQ, which had never had a detail pass and it showed — the entire mode was one soft radial halo plus 64 radial dashes. Four additions, all in service of it reading as an instrument rather than a pattern: a hairline RAIL at R0 for the bars to stand on (without a baseline they float); a SHORT INWARD mirror of each bar at 0.34 of the outward length, which is what turns a sunburst into a spectrum wrapped round a circle — deliberately short, the hollow centre is this mode's whole silhouette; PEAK DOTS riding each bar's recent maximum, the polar version of the Equalizer's peak caps the owner kept, and computed with NO per-bar state by sampling the same wave backwards through `phase` (`max` of phase, phase−0.30, phase−0.62); and a BLOOM drawn as an annulus rather than a disc, breathing with the level so the whole ring brightens on a burst. All still one `<Path>` each for 64 bars — a zero-length dash with a round cap is a dot. FOUND ON THE RENDER: `amp` can run past 1, and the longest bars were being sliced off flat against the viewBox — a straight vertical cut down the right of the ring. Everything is clamped to `R_MAX = VB/2 - 3` now.
  (3) HORIZON got its main pass earlier the same day (see below); added here only the stars twinkling, driven off the `phase` the scene already advances every frame so it costs nothing. A sky of perfectly steady dots is the one thing that gives a drawn night away.
- CASSETTE ROUND 2026-08-03 (OTA) — three items off the owner's device screenshots, and the interesting one was a WRONG DIAGNOSIS caught by measuring. (1) THE COUNTER IS GONE from the shell. A real cassette has no counter — the counter lives on the DECK — so a big digital readout floating on the plastic was the one element reading as app-on-object rather than object; it also printed straight across the `CR-02 · HIGH BIAS · MADE FOR THE ROAD` micro-text, and said exactly what the elapsed time under the seek bar already says. `timeText` removed from `CassetteBody` and all three call sites. (2) FLOATING NOTES now render BEFORE the shell instead of after, so they pass behind it; drawn on top they landed between the reels and on the top edge, and on a physical object that reads as dirt rather than atmosphere. The shell is translucent so they still show faintly through it, which is correct and much softer.
  (3) THE TWO-TONE REELS, and the method matters more than the fix. The obvious diagnosis was that the tape packs were only 0.80 opaque, so the blurred station photograph bled through and tinted them differently. Plausible, and WRONG: measured before and after raising them to 0.97, the left/right gap in R−B went 13.7 → 14.1, i.e. nothing. (First measurement was junk too — the sample squares were centred on the reel hub rather than the tape, so always measure the ANNULUS between hub edge and pack edge.) The real cause was `SHEEN`: six diagonal iridescent bands, each a DIFFERENT hue — mint, sky, lilac, pink, peach, yellow — spaced 44 apart at −18°. The reels sit at the same height but 106 apart horizontally, which at that angle is 34 units of vertical shift, almost exactly one band. So each reel sat under a different colour of the rainbow. SHEEN is now six ~90%-white tints: the bands still read as light travelling over plastic, but no band can recolour what is under it. Gap 13.7 → 5.1. Same rule the mirror ball settled on — THE MATERIAL MAY NOT CARRY A HUE; mood arrives through the shell's station tint, which is applied evenly. The 0.97 pack opacity was kept because it is physically right (you cannot see the road through wound tape), with a comment saying plainly that it did not move the number.
  STILL OPEN: the frozen clock (two shots two minutes apart, bar at ~1:46 then ~2:17, readout stuck on 01:42 in both). Not touched here — it needs a reproduction against a real track and `elapsedTxt` comes off `progress.addListener`, so a guess would be an unverified change to playback timing. Note for whoever picks it up: RN's `AnimatedValue._updateValue` always calls `__callListeners`, so `setValue` during a scrub is NOT the explanation; check instead whether `progress` is being forced native by something sharing its props node.
- APP STORE SCREENSHOTS REGENERATED 2026-08-03, AND THE MODE LABEL FIX THAT WENT WITH THEM: the owner: "it's getting hard to do these screenshots while being weary of the playlist names and marketing. The modes won't show unless I click a song most of the time." Both halves are real. The old `screenshots-appstore/` set was shot 23.07 and had gone badly stale — `05-soundwaves-raindrive.jpg` advertises SOUND WAVES, a mode retired and deleted on 25.07, so the live listing was selling a feature that no longer exists; the home and stations shots predate the receiver rebuild; and Mirror Ball and CD, the two best modes, were not on the listing at all. Regenerated all six from the real web build at 428x926 logical / deviceScaleFactor 3 = **1284x2778**, Apple's 6.5" slot and the same size as the old set. HARNESS: scratchpad/store/shot.mjs (`node shot.mjs "<station>" "<Mode>" out.png`, or `stations` for the dial page).
  THE POINT OF DOING IT THIS WAY: with no Spotify connection there is no track, so every mode falls back to the STATION'S OWN TAGLINE — no song title, no artist, no playlist name, no album art anywhere in the set, which removes the whole rights/marketing worry at source and reads better than a song title would. Set: Mirror Ball/Downtown, Horizon/After Hours, the Stations dial, Cassette/Daylight, Tuner/Night Run, Vinyl/Coastal.
  TWO TRAPS IN THE HARNESS, both cost a round: (1) **Start Drive already leaves the mode PLAYING** — pressing the transport to "start" it PAUSES it, and a paused Mirror Ball is deliberately dark and unlit (28.07), so the first set had a dead grey ball. Touch nothing. (2) The Mirror Ball rests its chrome after a few untouched seconds, so a wake tap is needed — but `(214, 200)` is NOT neutral: on Vinyl and CD that lands on the record/disc, which toggles playback, and it paused the deck. Tap `(40, 118)`: above every mode's object, clear of the chevron and the grabber. Also wait ~13s before shooting so the "visuals only for now" hint self-clears.
  SHIPPED WITH IT: `CASSETTE` and `VINYL` mode-name tags, top-left, matching the other six. Six of the eight modes had one and those two never did (Equalizer's is inline rather than via `fs.modeLabel`, which is why a grep for the shared style under-counts it) — a plain inconsistency, and it would have shown across the store set.
- HORIZON DETAIL PASS 2026-08-03 (OTA) — owner's device screenshots, and the flat sun had a CAUSE, not a tuning problem: `hzSun` fed the station's three eqColors straight into a top-to-bottom gradient, and several stations have almost no internal contrast in that ramp. After Hours is `#FF4444 / #FF1111 / #FF0000` — three near-identical reds — so the sun painted as a FLAT red disc; Mountain Pass (three whites) painted flat white. A station's eq ramp is tuned for equalizer bars and mirror facets, and is the wrong input for a big smooth gradient. FIX: `sunRamp(eq)` DERIVES five stops — a hot near-white top (`mixHex(eq[0], '#FFFFFF', 0.62)`), the station's own three through the middle, and a deep bottom mixed 0.55 toward `#140F2A`. Deepening toward a very dark INDIGO rather than black is load-bearing twice over: black flattens into a silhouette, and the cool bias is what keeps the all-white station reading as ice instead of dirt. Checked on After Hours (hot red), Mountain Pass (silver/ice over fog), Sunset (gold→coral→pink) and Night Run (blue, landscape).
  THE SLATS were the other half, and also structural: `SUN_CUTS` spaced six cuts in flat viewBox units downward from the sun's own CENTRE, but the sun is masked at the horizon and its centre sits only 0.42 of a radius above it — so "from the centre down" is the bottom THIRD of the visible disc, and three of the six cuts landed under the horizon and were never drawn. That is exactly the owner's screenshot: a solid dome with two thin slots near the bottom. `sunCuts(cy, r, horizon)` now spans from `cy - r*0.36` (the middle of the VISIBLE disc) to just above the horizon, thickening `r*0.022 → r*0.097` on a `f^1.5` curve, everything a fraction of the radius so it holds at any size. Also added: a two-stage bloom (a tight hot core AND a wide soft haze — one gradient cannot do both, widen it and the core washes out), a sky that warms toward the horizon instead of staying dead black, and the sun's light spilling onto the grid.
  THE GRID WAS STRIKING THROUGH THE TYPE — song title, seek bar and transport all sat on red stripes. Cause: the backdrop's scrim (`LinearGradient` at the top of the tree) sits UNDER the full-bleed scene layer, so it protects nothing; the scene is deliberately drawn behind the controls (31.07) and there was nothing between it and the text. Fixed in two places rather than one, because a scrim alone would have had to be heavy enough to kill the scene the owner likes: (a) the rolling lines now FADE BACK OUT in the near foreground (`near = (q-0.34)/0.66`, opacity × `1 - near²·0.86`) — their opacity grew with `q`, which is right for perspective and wrong for legibility, so they were brightest exactly where the words are; the rays take a `userSpaceOnUse` gradient stroke doing the same thing with one def for all 15. (b) a gentle second scrim ABOVE the scene, portrait only, starting at nothing at 46% of the screen. Landscape needs none — the docked panel carries its own background.
  TWO THINGS THE RENDER CAUGHT: the light spill was a Rect and its two hard vertical edges showed as seams down the sides of the road (it is an Ellipse with a radial gradient now — same lesson as the mirror ball's rim and the share card's fade bands: any hard-edged light layer eventually reads as a drawn shape); and the first slat spread still bunched into a stripe because it started at the sun's centre rather than the middle of what you can see. HARNESS: scratchpad/arm/station-mode.mjs opens any station in any mode in the real web build (`node station-mode.mjs "After Hours" Horizon out.png`) — worth keeping, the Modes tab alone can only ever show the default station. Verified after with the 8-mode rotation sweep: zero page errors, zero misfit haze canvases, no leaked transforms.
- RESUBMISSION BUILD 2026-08-02 — AND WHY `runtimeVersion` DELIBERATELY DID **NOT** MOVE: the owner had not resubmitted after the 29.07 rejection (five days out of review), so the priority became getting a binary to Apple with the fewest possible failure modes. FINDING: the rejection fix (63b9f77) removed expo-audio but never bumped `version`/`runtimeVersion`, both still `1.0.0`, which looks like a violation of the pinned-runtime rule. It is not, and the reasoning is worth keeping because the rule reads stricter than its purpose. The seatbelt exists so an update that NEEDS a new native module cannot land on a build that lacks it. Here the native delta is a REMOVAL, and nothing in `src/` has referenced expo-audio since 22.07 — so a bundle built from today's source runs identically on a build that still contains the (unused) module and on one that doesn't. No update can break in either direction. Bumping would have cost real money: the store build goes out on the `production` profile while the owner's phone is a `preview`-channel build, so a bump would have stranded her phone with no OTA until she installed a SECOND build — during the week we most need to keep shipping polish to it. So: `version` stays 1.0.0 (the app has never been released; a first release at 1.0.1 is noise on the listing) and `runtimeVersion` stays 1.0.0. `eas.json` has `appVersionSource: "remote"` + `autoIncrement: true` on the testflight/production profiles, so the BUILD NUMBER — the only thing Apple actually requires to increase for a rejected submission — is handled server-side. Net: the resubmission needed ZERO code changes, which is also why nothing else was batched into it (expo-sharing and view-shot were considered and deliberately held for the Apple Music build — a build failure tonight costs another day in the queue, and one Android user is not worth that). VERIFIED before handing it over with `npx expo config --type introspect`: no `UIBackgroundModes`, no `NSMicrophoneUsageDescription`, no mention of audio anywhere in the resolved iOS config. Owner steps written up at the TOP of docs/launch/app-store-submission.md, including the reply to paste into the App Review conversation. NOTE THE CHANNEL SPLIT for whoever picks this up: the store build is `production` profile → production channel (build 7 was too), the owner's phone and TestFlight testers are `preview`; publishing to production while in review counts as changing the app mid-review, preview stays safe.
- VINYL TONEARM REBUILT 2026-08-03 (OTA) — AND THE MISS THAT MADE IT NECESSARY: the owner sent a reference photograph of a real arm on 02.08 and said the tonearm was "a funky line"; I rebuilt the one in `ShareModeArt.tsx` — the SHARE CARD's hero — and never touched `VinylMode.tsx`, so nothing changed on the screen she actually drives with. She sent the same photograph back: "it hasn't lived up to the expectation... I asked to change it with the same image in a previous round. Let's start from scratch." LESSON, and it generalises past this file: several modes now exist TWICE — once live and once as a share-card drawing — so a note about "the vinyl's tonearm" or "the cassette's hubs" must say WHICH, and a change to one is not a change to the other. Grep for the component before assuming.
  THE OLD ARM was a vertical line with `BEND = min(17, armLen*0.08)` hooked in over the last 42% and a rounded rect stuck on the end at a fixed 18° — a stick with a kink, which is exactly what "funky" described. REBUILT, and three things carry it, all structural: (1) THE SPINE IS AN S. Module constants `ARM_A/ARM_J/ARM_B` give a point and a tangent angle in fractions of armLen (`ARM_J` is the INFLECTION), and the tube is two cubics joined with MATCHING tangents — that is what puts the inflection where it belongs; a single cubic bends wherever its control points happen to land, which is how the old one went wrong. A real S-arm leaves the bearing leaning slightly OUTWARD (+10°), bows back across the middle (−18°) and straightens into the headshell (−8°). (2) THE TUBE IS ROUND — four strokes on the SAME path (outline, body, inner light, hot hairline offset to the lit side). One flat stroke is the whole difference between a tube and a drawn line. (3) THE HARDWARE IS REAL — bearing plate with radial vents and a slotted centre screw, counterweight on a SHORT stub (set further back it reads as a lollipop), collar, wedge headshell with vent slots, mounting screws and a finger lift, then the shell's bright front FACE and the cartridge nose with the stylus at its point.
  GEOMETRY THAT IS LOAD-BEARING: everything is a fraction of armLen, so the same code serves the fullscreen deck (armLen = platSize*0.70) and the preview card. The stylus must land ~1.03 armLen from the pivot and ~0.158 of it toward the spindle — that is what puts it at 82% of the record's radius when the arm drops and 69% when the song ends, both well outside the 45% label. Change `ARM_HEAD_L`/`ARM_HEAD_A` and the needle walks onto the artwork. SVG NOTE: `rotate(a)` turns a DOWNWARD vector toward −x, so a group that should point along `armDir(θ)` is rotated by −θ; both the headshell and the counterweight rely on it.
  THREE THINGS THE OFFLINE RENDER CAUGHT: the headshell was barely wider than the tube and read as a dark blob (it is a WEDGE now — `wTop = armW*1.15` to `wBot = headW`, headW 20→26); the finger lift floated beside the shell instead of touching it (it is anchored on `edge(f)`, the shell's real half-width at that fraction, and tapered); and the shell plus cartridge merged into one long dark shape so only the needle read — the fix is the shell's BRIGHT FRONT FACE between them, not a lighter cartridge. HARNESS: scratchpad/arm/shape.mjs mocks the arm's own maths over a record at three rotations and PRINTS where the stylus lands as a percentage of the radius; it is a mock, so keep it in step by hand. Verified after in the real web build (portrait + landscape deck) and by the 8-mode rotation sweep: zero page errors, zero misfit haze canvases, no leaked transforms.
- CD MODE REVERTED 2026-08-03, same day (OTA): owner, after seeing it on the device: "undo the changes done to the CD mode, it was better previously, but keep the new album cover opacity level." So `CDMode.tsx` is restored to its 42393d4 state — the ORIGINAL `JewelCase` (full-height hinge spine, three clips, corner brackets) and the ORIGINAL `buildFan` (`|cos(ang)|^3` with the `strength < 0.04` cull, wedge opacity 0.20/0.13) — with ONLY the album-art change re-applied on top (art 0.44 → 0.60, pewter wash 0.13 → 0.09). DO NOT re-fix the "dark areas in the rainbow": those two soft sectors at the top and bottom of the disc are the shipped look and she prefers them. The slimline-case rebuild and the unbroken-sweep fan are described in the entry below purely as a record of what was tried and rejected; the cassette half of that entry DID ship and stands.
- DONE 2026-08-03 (CD + CASSETTE detail round, OTA — the CASSETTE half shipped; the CD half was reverted, see above): five owner notes off the device, all against reference photographs.
  CD (REVERTED) — (1) THE DARK AREAS IN THE RAINBOW were `buildFan`'s own shaping: `strength = |cos(ang)|^3` reaches ZERO at the top and bottom of the disc, so a third of the wedges were skipped outright by the `strength < 0.04` guard and the sweep had dead sectors. Now `0.34 + 0.66 * |cos|^1.5` — never zero, still brightest left and right (a real disc's diffraction peaks along the light's axis), unbroken all the way round. Per-wedge opacity trimmed 0.20/0.13 → 0.165/0.108 because all 64 wedges draw now where ~22 used to be culled, and the overlap was stacking into spokes. (2) ALBUM ART 0.44 → 0.60 with the pewter wash 0.13 → 0.09 ("so each song feels more personal"). (3) THE CASE rebuilt off her slimline-jewel-case photograph: generous corner radii, a double wall PLUS a tray wall inside it, an L-shaped rib at each corner, moulded lugs straddling the top and bottom walls, and the hinge clip PROTRUDING from the left. The old full-height left "spine" was the wrong part entirely — a slimline case has a clip at mid-height, not a spine. All dimensions proportional to `size`, so it holds together at any scale; the hinge lives inside a `P = size*0.052` body inset, since anything drawn outside the Svg's own canvas is clipped.
  CASSETTE — (4) THE HUB. Owner: "the inner tape circles with the teeth look sort of cheap. Realistically they're not shaped like that." Correct: the old one was spokes radiating OUTWARD from a glowing centre, which reads as an asterisk. A real hub is the opposite — a white plastic ring with a dark spindle hole punched through it and six square teeth projecting INWARD into that hole, which is what the deck's spindle grips. The teeth are hub, not gaps. (5) THE BOTTOM EDGE, the end you actually hold and the busiest part of a real shell: head window with the felt pressure pad on its leaf spring, a shield behind the tape, two pinch-roller openings with the rollers sitting in them, two capstan holes, locating holes either side, guide rollers on their posts, and the moulded lip running the full width. (6) The three `Sparkle` stars in the corners are DELETED, component and all. (7) LANDSCAPE `cassetteW` 1.30/0.60 → 1.46/0.66 — the deck docks at 0.86 scale beside the panel, so the old numbers left a band of empty table either side; it now runs to about 20px of the pane's edges.
  VERIFIED: the 8-mode rotation sweep (portrait → docked → rested → back-portrait) with zero page errors, zero misfit haze canvases and no leaked transforms.
- BALL: GLITTER 2026-08-02, latest (OTA): owner: "add some glitter, sparkles and fun. It's a mirror ball! It should look fun and pretty! Place it all over!" NEW `GlitterField` — 64 tiny lights scattered across the WHOLE screen, each twinkling in place. It obeys both standing rules at once: nothing travels (a twinkle is brightness, not motion — which is what real glitter does), and every drawn thing is gradient falloff — each speck is a soft radial dot, and the brighter ~28% carry HAIRLINE cross-flares whose gradients are transparent at both tips (the round-21 soft-glint recipe, the one drawn-sparkle construction that has survived on device; never a stroked star). COST: EIGHT shared Animated loops however many specks are drawn — each speck borrows one of 8 phases AND one of 3 response curves (peak-mid / rising / falling), so bucket-mates pop at different moments instead of blinking in unison; 24 effective twinkle patterns from 8 animations. Mounted ABOVE the vignette (specks must sparkle in the corners too), gated on `lit` like every other light. Colours: white + the three whitened eq stops.
- BALL: STATIC FINE RAY BURST, CLEAN ROOM 2026-08-02, latest (OTA) — SUPERSEDES the scrolling-field entry below. Owner, with a reference photograph of a real ball throwing a dense burst of fine beams: "it's much odd now the rays move the opposite direction of the reflections. Start with a clean background and add realistic rays coming off of the mirror ball. Don't rotate them, just make it beam finely... Remove the small reflective squares." She is right about the conflict: two light systems moving at different rates in one room always read as disagreeing — the fix was never tuning one to match the other, it was having ONE.
  `CastReflections` (the scrolling rows/squares) is DELETED — component, constants and mount. The room is now: dark wash + `LightRays` + DustField + Vignette. `LightRays` REBUILT STATIC: 44 fine filaments at UNEVEN angles and lengths (jittered spacing — a machined fan reads as a drawing), a ~20% minority of longer brighter "heroes", widths 0.014-0.045 of the ball, every ray one stretched radial gradient soft in all three directions. NO ROTATION — a real beam is bolted to the geometry of lamp and mirror; instead each ray BREATHES between 55% and 100% of its own brightness on one of SIX shared staggered Animated loops (`RAY_PHASES = 6`) — alive without motion, and six animations however many rays are drawn. Gated on `lit` like everything else.
  RULE, now proven three ways on this mode: the room may have at most ONE moving light system, and whatever moves must ride `spin`. The ball's own surface is that system; everything else holds still and breathes.
- BALL: THE ROOM IS A SCROLLING FIELD 2026-08-02, later (OTA): the ring model was right for a ceiling and wrong for this screen. Owner: "since we are facing the mirror ball head on... the background should just have the light rows move right to left at the same pace as the mirror ball, covering the entire screen... the top and bottom rows move in opposite directions, which gives me a headache." That headache is inherent to ANY rotating pattern — one pattern turning cannot have every row travelling the same way — so the rotation is gone.
  `CastReflections` is now a SCROLLING FIELD: 9 rows of patches sliding right to left, every row the same way, across the whole screen. Perspective supplies what the rotation used to: rows crowd toward the top (`yBase = winH * (0.02 + 0.98 * u^1.55)`), patches grow and SPREAD toward the foot (count `17 - k*1.35` per screen, so ~17 at the top and 5 at the bottom — the owner's "more dispersion between the light reflections"), and each row bows on a periodic curve rather than ruling a straight line. THREE copies a screen apart with `translateX 0 → -2*winW` per revolution makes the wrap seamless; the bow is evaluated from the patch's own x so every copy computes the same offset and the seam cannot show. Colours whitened much further (0.62/0.66 toward white, opacity 0.20-0.40) — "enhance the light reflections/spots to be lighter". It also mounted in the wrong place before: it lived inside the BALL's anchor box, which is why it cut off at the bottom; it is a room layer now, positioned against the screen.
  MEASUREMENT NOTE, and it finally worked: with every row travelling the same way a horizontal cross-correlation between frames IS a valid instrument (it was not when the rows opposed each other — see the entry below). Median −40px per 420ms step, left, in 9 of 11 steps. TRAP: the first two attempts read exactly 0 because the bands I sampled were dominated by STATIC UI — the notice card at the top and the transport at the foot. Sample between the ball and the song title, where nothing but the field is drawn.
  `LightRays` REPLACES the deleted MirrorBeams ("bring back the light rays"). The old ones gave every beam its own mirror and its own trajectory, so one could park near the right-hand limb and sit there — that is what "stubborn" meant. These are 18 EVENLY SPACED shafts turning as one fan with the spin: identical to each other, always moving, no single one able to go stubborn. Each is one stretched radial glow, soft along its length, across its width and at its far end.
  `UnderGlow` DELETED — the broad pool beneath the ball was the "bottom half bubble". The per-patch halo ellipses went with it: a soft disc behind every spot is what made them read as bubbles rather than light on a wall.
- BALL: THE ROOM'S LIGHT REBUILT AS TRAVELLING ROWS 2026-08-02 (OTA): owner sent a recording plus two reference photographs of real mirror balls and three asks. (1) THE SPARKLE IS GONE — the four-point star that rode inside SpotlightPan ("a light star moves across the ball making it look cheap"). Fourth time a drawn highlight on this mode has been reported; the rule holds. (2) `MirrorBeams` DELETED outright ("the light rays that live on the right is stubborn and won't be removed"). Three rounds of feathering never stopped a beam parked near the limb reading as a streak laid over the picture, and with the cast light rebuilt the room no longer needs them. The component and BEAM_COUNT are removed, not just unmounted. (3) `CastReflections` REBUILT. It was ~34 patches in one group rotated 0→720deg about the ball — a rigid clockwise pinwheel, which is exactly what she described as "light pieces circulate the ball like a circle".
  THE MODEL, and it is the whole component: a real ball throws its mirrors onto the room as a grid arranged in RINGS, and as it turns each patch travels along its own ring. THE TRICK: an ellipse is a rotated circle that has been squashed — so the patches sit on plain CIRCLES inside a view that rotates, and that view sits inside another that scales it vertically (`CAST_FLATTEN = 0.52`). Points therefore travel along ellipses: wide sweeps at the sides, tight compressed arcs at the top and bottom, which is the row structure in her photographs. Still native-driver transform only; the entire room is two nested views and ONE animated rotation. Count 34 → 120 across 5 rings (12/18/24/30/36), each patch drawn TALL (h / CAST_FLATTEN) so the wrapper's squash leaves it near-round instead of a flat dash.
  DIRECTION, and the honest part: rotation is now 0 → −720deg, i.e. ANTICLOCKWISE, so along the top rows the light travels RIGHT TO LEFT as asked. The bottom rows necessarily travel the other way — one pattern turning cannot have every row moving the same direction, and the reference photographs behave the same. It rides `spin`, so it can never disagree with the ball: stops when it stops, follows a finger drag, and stays at 2× the ball's rate because reflecting off a turning mirror doubles the angle.
  TUNING that mattered: at `reach` 0.92 of the screen and a high lift, most rings landed off-screen and the arcs never crossed the upper background — reach 0.64, lift R*0.30 puts arcs across the whole frame. MEASUREMENT NOTE: a horizontal cross-correlation between frames CANNOT verify the travel direction here and three attempts at it produced noise — the arcs curve, so patches move diagonally, and any band wide enough to sample mixes the top rows (moving left) with the bottom rows (moving right), which cancel. The rotation SIGN is the thing to reason about, not the pixels.
- DONE 2026-08-02 (HOME HEADER EQUALIZER — the bars never moved on device, OTA): owner: "the equalizer at the top of the home page STILL doesn't animate when a card is open and music is playing... looks like it's active but something is stopping the equalizer to move." The gate was fine and the label was right; the ANIMATION was the problem, and it was invisible here because it works perfectly in the browser. METHOD, reuse it: force `live={true}` in cruise.tsx, run the web build and sample the bars from the DOM (scratchpad/share/eqprobe.mjs reads each 3px bar's computed height / transform matrix ten times over 2s). Under Reanimated the heights cycled 3..24 exactly as designed — so the bug could not be the gate, `np.playing`, or the 15s poll. WHAT IT ACTUALLY WAS: `EqualizerHeader` was the ONLY component in the live app still using react-native-reanimated, AND the only one animating HEIGHT — a layout property, which cannot take the native driver. Every other animation in Cruise FM is an `Animated` transform on the native driver and every one of those runs on her phone. One component, different engine, the one property that can't be native-driven: not a coincidence worth defending. FIX: rewritten on React Native's own `Animated`, animating `scaleY` instead of height. The bar is drawn at its FULL height and scaled down; scaling happens about the centre, so it is paired with a `translateY` of `(maxH - REST_H) / 2` that puts the bottom edge back on the baseline — RN has no transform-origin, and without it short bars float. VERIFIED by measuring again: scaleY sweeps 0.14..1.00 out of phase across all seven bars, and every bar's bounding-rect BOTTOM is pinned at the same pixel in all ten samples, so nothing drifts off the baseline. Also fixed while in there: the per-bar `delay` now runs ONCE before the loop instead of inside it, where it made every bar pause at the bottom of every cycle and the row breathe in unison. Reanimated stays in package.json (react-navigation depends on it); the only remaining importers are unrouted Expo template files.
- DONE 2026-08-02 (SHARE CARDS round 5 — full-bleed photograph, and a tonearm that's a curve, OTA): three owner notes off her phone.
  (1) "MAKE THE IMAGE COVER THE ENTIRE BACKGROUND of the ticket and receiver — allow for etching and blurring for the top and bottom. But don't cut off the image." Two different things, and the split matters: the PHOTOGRAPH now covers the whole card and is allowed to be trimmed (`PhotoFill`, always cover — that is what photographs are for), while the mode's OBJECT is laid over it CONTAINed at a fixed width and is never cropped. A framed window is gone from both styles. Type reads because the top and bottom fade rather than being boxed off (`FadeBand`, fading to nothing at its inner edge — a scrim with a visible boundary is just a rectangle, the lesson the mirror ball's rim taught). The ticket's top fade carries a little of the station's colour, which is where the old opaque header band went. LAYOUT NOTE: at the panel's FULL width the ticket's hero left the counterfoil too short and the artist ran into the barcode, so the object sits at 0.95 of it; the receiver's at 0.76, growing to ~0.83 on a 2:3 pin so the extra height buys a bigger object and a lower fascia instead of a dead band of metal.
  (2) "The equalizer in the receiver mode is quite hilarious" — a set inside a set. TWO causes, both fixed: the mode used to sit in its own recessed WINDOW, so it read as a second screen inside the first (gone with the full-bleed change), and the receiver drew its own needle directly under the Tuner's. `ownDial = modeId !== 'radio'` now prints a plain scale there instead of a second working dial.
  (3) TONEARM. She sent a reference photo of a real arm and said the headshell points the right way but the tube is "a funky line" — correct, it was a straight `<Line>` with a wedge stuck on the end. It is now a real J: one cubic bezier leaving the bearing downward and bending left into the headshell, stroked THREE times on the same path (shadow, chrome, hairline catch — a tube is round, and one flat stroke is the whole difference), with a proper bearing housing, an angled counterweight, a headshell drawn from the tangent at the tip with the cartridge and stylus at its end, and a finger lift. The stylus also had to move OUT: at the first try it sat on the label, which is the one place a needle never is — it now lands about 0.55 of the way out on the grooves.
- DONE 2026-08-02 (SHARE CARDS round 4 — THE HEROES NOW MATCH THE MODES, OTA): owner, from her phone: "the image that renders from the mode — it's much of a different depiction from what the modes actually look like. The cassette mode is probably the best depiction." She was right, and the cause turned out to be ONE decision, not eight: **only VINYL and CD show album artwork on screen** (verified by grepping `albumArt` through all eight mode components — 6 hits in VinylMode, 5 in CDMode, ZERO in the other six). Every hero had been drawing the cover into the picture anyway, so the mirror ball had a square floating under it, the horizon's sun WAS the cover, the equalizer stood in front of one and the tuner had one in its panel — none of which exists in the app. Pasting the cover in was most of why each hero looked like a different product.
  THE FIX, and it is also her layout request: the cover moved OUT of the artwork and into the credit line beside the song (`CoverTile` in ShareCardStyles), and each hero was rewritten off its real component. CASSETTE is now ported coordinate-for-coordinate from CassetteMode's own 340x210 drawing (LX/RX/RY 118/224/118, pack r52, the cream paper label at 30,24,280,40 carrying "A · STEREO · C90" + the song, the ribbon down to the capstans at 74,168 and 268,168, the bottom lip, the screws). MIRROR BALL lost the cover and gained the mode's own shading model — brightness from the reflection direction r = 2(n·v)n − v against three fixed lamps, scatter pushed to the ends of its range, brick bond, hairline gaps top and bottom ONLY. EQUALIZER is 30 bell-curved bars on a baseline with white peak caps and nothing else. CIRCULAR EQ has a HOLLOW centre, which the mode's own comment insists on. TUNER is the dot-matrix display: ON AIR alone on its row, song and artist full width, then STEREO · AM/FM keys · TUNED on ONE row (where the mode puts the band button), the frequency under it, then the dial. HORIZON's sun is a GRADIENT sliced by six widening cuts at the mode's own makeGeom offsets, not the album cover. CD went to the mode's real numbers (translucent disc, art ghosted to 0.86R under a pewter wash, hub 0.155R / grippers 0.235R / hub ring 0.31R / stacking ring 0.62R) and its diffraction fan dropped to 0.065 — 50 overlapping wedges at 0.20 stack into a flower.
  NEW `ModeStage` (ShareCardStyles) draws what a mode ACTUALLY is: the station photograph full-bleed, the mode's scrim over it, the object on top. All three styles use it, so the window is a picture of the app rather than an object on a flat panel. `heroBox(x,y,w,h,'cover'|'contain')` replaces heroFit; cover is the default because a window with bars down its sides doesn't read as a picture. CONSEQUENCE, and it is a standing rule now: a window wider than 1080:740 trims the stage top and bottom, so **every hero must keep its content ≥40 units inside the stage** — the Tuner's panel lost its head at the first attempt, and the CD's jewel case had to come in from 352 to 330.
  TICKET REBUILT to the owner's own running order: header and text / rule / the mode full bleed across the whole ticket / tear / NOW PLAYING · song · album cover / barcode. Header band deepened again (0.68 → 0.80 toward black) as she asked. SLEEVE's board now carries the station photograph at 0.30 under the board tint and the grain — a flat tint was the last thing making the jacket read as a coloured rectangle rather than card off a press.
  STILL A DRAWING, NOT A SCREENSHOT. A real capture of the live mode needs `react-native-view-shot`, which is native and therefore item 2 on the NEXT FRESH BUILD list; this round closes most of the gap without it. Verified: all eight modes in all three styles, both formats, on Night Run, Sunset, Mountain Pass and a custom station with no artwork; driven in the real web build at 393x852 and 375x667 with zero page errors.
- DONE 2026-08-01 (SHARE CARDS round 3 — CUT TO THREE, then a detail pass, OTA): owner picked TICKET, SLEEVE and RECEIVER out of the eight ("8 is too many to choose from... we'll improve the graphics and design on these"). `NowStyle`, `PosterStyle`, `PolaroidStyle`, `PostcardStyle` and `MinimalStyle` are DELETED — including the original Now Playing card, so `DEFAULT_SHARE_STYLE` is now `ticket`. With three chips the row fits without scrolling, so `sc.chips` gained `flexGrow:1` + `justifyContent:'center'` (it still scrolls if the set ever grows again).
  WHAT THE DETAIL PASS ACTUALLY CHANGED, and the principle under all of it: each of these is pretending to be a physical object, and what makes an object read is the marks a MACHINE would have left on it — not more colour. TICKET: a coloured header band in the station's own light (deepened 0.68 toward black — at 0.42 it came out a bright saturated blue and read as a UI header, not card stock), a diagonal guilloche weave under everything, a stable serial number (`derive()` seeds it from station+mode+dial so the same drive always prints the same number — a serial that changed every render is the one detail that gives the whole thing away), "ADMIT ONE" set on its side down the counterfoil edge via `transform="rotate(90 x y)"` (string transforms confirmed working on device paths and in the harness), and the perforation rebuilt as PUNCHED HOLES in the card's own dark rather than a dashed stroke — a dash reads as a border style, holes read as a tear. SLEEVE: board grain (64 hairlines), RING WEAR from the record inside (the single detail that says "owned"), a shrink-wrap sheen as a soft multi-stop diagonal, a bumped corner, and a HYPE STICKER roundel carrying the dial number. RECEIVER: a moulded bezel with four slot screws, a recessed GLASS display holding the lamp row + readout with one soft reflection sweep across it, the amber readout glowing onto its own glass (an ellipse of amber under the digits), the station name inside the glass in dot-matrix, and — the one that matters — THE NEEDLE NOW SITS WHERE THE STATION REALLY IS (`BAND_RANGE` FM 87.5–108.5 / AM 530–1600, needle at the station's own dial value). Parked dead centre on every card it quietly said none of this was real.
  THREE COLLISIONS THE RENDER CAUGHT, all the same failure as rounds 1 and 2: (1) the receiver's foot had a volume knob, an etched wordmark, the install line AND the dial's band labels sharing 90px, and all four printed over each other — the knob was the least load-bearing and went; (2) the receiver's frequency and the station name inside the glass touched, so FREQ_DOT 15→13.5 and the glass grew 300→316; (3) the ticket's counterfoil block is now CENTRED between the tear and the barcode instead of hung off the tear, because pinned to the top the 2:3 pin opened a dead band under the artist.
  VERIFIED: all three styles x all eight modes offline, plus both formats on Night Run, Sunset, Mountain Pass (pale + FM, so the long name and the wide dial) and a custom station with no artwork; and driven in the real web build at 393x852 and 375x667, zero page errors.
- DONE 2026-08-01 (SHARE CARDS round 2 — THREE MORE OBJECTS, OTA): owner picked the OBJECT direction out of round 1 ("expand on the mirror ball and ticket") and asked to see it across the other modes. Three additions, all in ShareCardStyles.tsx, all drawing the same `ModeHero` as everything else: SLEEVE = a record jacket with the disc easing out of the right-hand side, station/song/artist PRINTED on the board and a catalogue number under it (CFM-<dial>); INSTANT = an instant photo, white border with a deep chin and the caption in DARK type — the inversion is the point, it is the only light card in the set and it is what stops a feed; POSTCARD = "GREETINGS FROM <station>" over the picture with a paper half underneath carrying the song, a perforated stamp holding the dial number, a postmark and address rules. The postcard is the closest fit to the product's own premise, since the mood stations are places. `SHARE_STYLES` is now eight, ordered so the object family sits together.
  NEW HELPER `heroFit(x,y,w,h)` — fits the whole 1080x740 hero stage inside an arbitrary box, CONTAIN not cover. Cropping is not available to us: the modes draw to the stage's full width (the cassette shell alone is 896 of 1080), so trimming the sides to fill a square window slices the object rather than the background.
  FIVE THINGS THE RENDER CAUGHT, and note that four of them are the SAME failure as round 1 — a block sized against an assumption: (1) the sleeve's station name and artist shared a baseline with the artist right-aligned, so a long name printed straight over it — a line each now; (2) the sleeve's disc was drawn at a record's own near-black and simply vanished into the card, taking the whole "it's a sleeve" idea with it: only a sliver clears the jacket, so that sliver has to be LIT (lifted base, sheen gradient, rim stroke, brighter rings); (3) the sleeve also has to be NARROWER than the card or the disc has nowhere to come out to — a jacket that fills the frame is just a square; (4) the instant's chin was eyeballed at 176 and could not hold two caption lines plus the artist plus the footer, so the artist fell off the bottom of the print and the footer ran through the caption — 262 now, and the caption's width budget leaves the dial stamp alone; (5) the postcard fitted its hero from 30px ABOVE the card's top edge, which clipped the vinyl's tonearm off the picture.
  PIN FORMAT: the sleeve centres the jacket AND its catalogue line as one block. Pinned to the card's foot instead, the 2:3 version stranded the sleeve in the top half with 370px of nothing under it — the same "where does the extra height go" question every style has to answer explicitly.
  VERIFIED: all eight styles x all eight modes offline (scratchpad/share/render.js, `STYLE=<id> node render.js modes <station>`), plus both formats on Night Run, Sunset and the no-artwork custom station; and driven for real in the web build at 393x852 and 375x667 through every chip, zero page errors.
- BALL: THE AWKWARD STREAK 2026-08-01 (OTA): owner's recording showed a broad band on the right with visible parallel edges and a blunt end, plus thin lines crossing near it. TWO SOURCES, both fixed. (1) `MirrorBeams`' feathered build was three stacked strips of a LENGTHWISE gradient — the gradient faded along the beam but each strip's SIDES were hard lines, and on device the widest beams read as drawn bands hanging in the room. Each beam is now ONE stretched radial glow (an Ellipse centred on the beam's root at the ball, radial gradient in bounding-box units) — soft along its length, across its width and at its far end; there is no edge anywhere to see. (2) `LightStreaks` DELETED: the seven static diagonal hairlines were hired when the room was empty; with CastReflections filling it, fixed lines crossing the scene read as scratches on the picture. RULE consolidating three rounds of this: any light drawn as a stroked or hard-edged shape eventually gets reported as an artefact ("Os", "wire cage", "awkward streak") — light layers on this mode must be pure gradient falloff.
- BALL: THE ROOM CATCHES THE LIGHT 2026-08-01 (OTA): owner's recording showed the background empty — "the reflections aren't hitting the background, it doesn't circulate around the sphere". TWO CAUSES: the vignette round had darkened the room, and the orbiting specks rotated about the SCREEN's centre (an absoluteFill transform), which sits well below the ball — so nothing ever read as circulating the sphere. `LightField` DELETED; new `CastReflections`, the thing everyone knows a mirror ball does: ~34 mirror-shaped patches of light cast onto the room, sized UP with distance (a projected square spreads) and stretched along their own radial direction (it lands at an angle, which smears it), each a soft halo ellipse under a rounded core. Mounted INSIDE the ball's anchor box so the container rotates about the ball's true centre, at 2x the ball's rate (reflection doubles the angle). ONE Svg root, static shapes, ONE animated rotation — the whole effect is a single animated view. TUNING that mattered: the first cut's patches were 2x too big with a 0.30-opacity halo and read as pills floating in front of the scene; halo 0.10, core 0.75, op 0.05-0.16, w 0.022-0.072·size. Patches are the station's LIGHT colours (two whitened eq stops + neutral).
- BALL: THE CINEMATIC ROOM 2026-07-31 (owner's full lighting brief — premium studio, not club — OTA): four additions on top of the specks/beams round, all restrained by instruction. (1) RAYS FEATHERED: MirrorBeams' laser look was two things — a near-white spark at the gradient's start and a hard edge down each side of the rect. The gradient now starts at the station colour at 0.50 (no white tip), and each ray is THREE strips of the same gradient (full-width at 0.5, middle at 0.7, core at 1.0) so the edges dissolve. Widths up ~3x (0.020–0.095·size) and peaks down to 0.14–0.36: wide-and-faint is what reads as volumetric, thin-and-bright is what read as lasers. Still per-mirror trajectories riding `spin`. (2) `Vignette`: static full-screen radial, transparent to 62% then #020208 at 0.52 in the corners, mounted over the room layers and under the chrome. (3) `DustField`/`DustMote`: 14 near-invisible motes (1.5–3.7px) drifting slowly upward through the light, each catching a brief highlight mid-loop (opacity 0.02→0.20→0.02 on a 5–10s cycle); a third are tinted toward the station colour. Deliberately dust-in-a-still-room, not snow; gated on `live`. (4) THE ONE SPARKLE: two gradient-falloff arms + nothing else, drawn INSIDE the SpotlightPan pool's own Svg so it rides the pan and only ever sits on the brightest mirrors. All soft — no stroked shapes, which is what made the old glints and lens-flare Os read as stickers. Checks: tsc clean, 3125 paths / 65 roots, open-to-settled 2.9s, rotation cycle + all pages clean.
- BALL: SPECKS ORBIT, AND A REAL BEAM BUG 2026-07-31 (OTA): (1) BUG — `MirrorBeams` gated brightness on `1 - Math.max(0, nz)`, which is 1 for the ENTIRE BACK of the ball, so every mirror round the back was throwing a full-strength beam. Added `vis = clamp(nz/0.14)` so a mirror has to be on our side to throw anything. That, plus BEAM_COUNT 15 -> 30 and shorter beams (reach winW*1.7 -> 1.0), turned "one stray bar crossing the screen" into a burst of rays round the ball, which is what the reference photographs show. (2) THE SPECKS NOW ORBIT rather than sliding sideways. The slide was a `translateX 0 -> winW` on two wrapped copies, and the owner read it as going the OPPOSITE way to the surface — a horizontal slide is ambiguous (a spot above the ball and one below appear to go opposite ways when the real motion is rotational) and the wrap is what made them read as "floating". They now rotate about the ball at TWICE the ball's rate, which is the actual physics: reflecting off a turning mirror doubles the angle, so the spots sweep twice as fast as the surface that made them. One copy instead of two, since a rotation wraps by itself.
- BALL ROUND 2026-07-31 evening (five owner notes: the Os, sphere-accurate beams, mirror specks, colour that shows, spotlight on the ball only — OTA): (1) THE "Os" WERE `LensFlare`. It drew three hollow white circles (`fill="none" stroke`) at fixed points across the ball — lens-flare ghosts, which over a detailed mosaic read as stray O outlines sitting on the mirrors. DELETED. Worth noting the first guess was wrong: the floating bokeh circles were removed first and the Os survived, so if a ring ever reappears, look for a stroked Circle, not a soft gradient. (2) BEAMS NOW COME OFF THE SPHERE. `LightShafts` was a rigid fan of 22 shafts radiating from the centre and turning as one piece — a flat pinwheel (owner: "it is circulating the mirror ball like a 2D shape"). New `MirrorBeams`: each beam belongs to a MIRROR at a fixed latitude/longitude, so as the ball turns that mirror orbits and the beam is drawn from wherever it currently sits, pointing along its outward normal. KEY CHOICE: opacity peaks at the LIMB and falls to nothing at the centre — a mirror square-on throws its beam down the lens where the ball is in the way, and the shafts you see in a photograph come off the edges. That is what makes the light travel around a sphere instead of spinning like a wheel. Whole trajectory sampled once into interpolation tables driven by `spin`, so no per-frame work and it stops/scrubs with the ball. (3) FLOATING CIRCLES -> MIRROR SPECKS: `BokehField`/`BokehDot` deleted; `LightField`'s dots are smaller and sharper and are pushed OUT of the ball's own disc (a reflection lands on the room; drawn over the mirrors it reads as a blemish). (4) COLOUR THAT SHOWS: the spotlight pool now carries the station's colour (white core, station mid, station deep), and in the flipbook a mirror catching a coloured lamp is LIFTED before it is tinted — `base = t + cast*0.42` and the cast goes toward a WHITENED station colour, never a deepened one. Mixing a mid-grey tile toward a deep palette entry is exactly what left the colour "in the background and in deep shades" (owner). Colour lobe widened ^40 -> ^28, cast ceiling 0.42 -> 0.72. (5) SPOTLIGHT ON THE BALL ONLY: `SpotBeam` (the wedge reaching in from off-screen) is deleted — it overran the ball and was being clipped into a hard-edged rectangle below it. The pool alone, clipped to the ball, is what she asked for. CHECKS: tsc clean, 3125 paths / 49 SVG roots, open-to-settled 2.8s, full rotation cycle and all five pages with zero errors.
- THE FLIPBOOK IS THE BALL 2026-07-31 (panning spotlight, and the old ball deleted, OTA): owner: "the diagonal light moves across the mirror ball and the stars that light up from the ball... look decorative"; the ball itself now looks good enough that the add-ons read as stickers on top of it. (1) `BallSheen` (a diagonal band sweeping corner to corner at a fixed angle) and `MirrorGlints` (four-point stars) are DELETED. (2) NEW `SpotlightPan` + `SpotBeam`, built from the geometry rather than drawn: a light from direction L lights the part of the sphere facing it, so the pool sits at screen point R·L, and because that patch is a disc on a sphere seen at an angle it projects to an ELLIPSE — squashed along the line back to the ball's centre by exactly L.z and turned to face it. So the pool narrows and leans as the light swings toward the limb. L traces a slow Lissajous (sideways swing and up-down drift at different rates) so the light arrives from a genuinely different angle each pass instead of orbiting on rails. THE BEAM IS WHAT SELLS IT: a bright patch alone is just a patch, which is precisely why the sheen band read as decoration — `SpotBeam` is a wedge reaching the ball from off-screen, drawn BEHIND the ball so the ball occludes its tip. Pool and beam share ONE `spotPan` value created in the mode body; two loops of the same length would drift apart and a beam that misses its own pool is worse than no beam. The ball's baked lamps stay: a room has fixed lamps AND a moving spot, and the fixed ones are what make individual mirrors flare. (3) `LightShafts` now rides `spin` instead of its own 74s loop (owner: "make sure the reflected light move at the same pace as the ball") — these are beams thrown OFF the mirrors, so they can only travel at the speed of the surface throwing them, and riding spin also stops them when it stops and follows a finger drag.
  TIDIED INTO THE MAIN PATH, as asked: the `FLIPBOOK` switch is gone and with it `buildSphereTiles`, `SphereGrid`, `MirrorFlash`, `RotatingMeridians`, `Meridian`, `buildMeridianBase`, `buildLatitudeArcs`, `LightPatches`, `BallSheen`, `MirrorGlints`, plus the constants only they used (`Tile`, `BEVEL`, `FACET_DIRS`, `facetDir`, `SHADE_STEPS`, `shadeAt`, `FLASH_*`, `LAMPS`). DiscoBallMode.tsx 2223 -> 1583 lines, 640 deleted. The ball's geometry and lighting now live entirely in MirrorBallFlipbook.tsx. Checks: tsc clean, 3127 paths / 45 SVG roots (down from 56 — the deletion paid for the spotlight), open-to-settled 2.96s, full rotation cycle and all five pages with zero errors.
- FLIPBOOK ROUND 2 2026-07-31 (denser, silver, and less blur — owner picked the flipbook over the pre-render, OTA): she compared it against an offline Python render (scratchpad/prerender/ball.py, 34x64 mirrors with a real bevel and bloom) and chose the flipbook — "it's missing that interactive touch" — but wanted three things from the render. (1) DENSER: 17x32 -> 23x44, paid for by a MIN_AREA cull on each tile's own drawn area (silhouette slivers cost a shape and show nothing). 3126 paths vs the shipping ball's 2368; open-to-settled unchanged at ~2.9s. (2) SILVER, NOT MATTE — three causes, found by measuring rather than eyeballing. The mirror FILLS were already fine (mean saturation 0.065, essentially the same as the offline render's 0.078), so the matte look was entirely layers on top: LightPatches is now dropped outright under the flipbook (it exists to move light over a STATIC grid; over mirrors that carry their own it is just a veil), ColourReflections -> 0.22, NeonSweep -> 0.24, dbShade's inner white stops and dbHot cut, dbRim halved. Then the highlights: the ball had almost no blown-out pixels (1.4% above luminance 200 vs the render's 7%), so the specular lobe went from dot^15 to TWO lobes — a WIDE one (^15) for brightness and a NARROW one (^40) for colour. Keeping them apart is the point: widening a single lobe lit more mirrors but tinted half the ball at the same time. Finally the resting surface was lifted, which the shipping ball could never do (rounds 6/13: a bright resting surface swallowed light sliding over a still grid) — the flipbook's motion IS the grid, so that ceiling no longer binds. (3) LESS BLUR: the frames cross-faded continuously, so every moment was a blend of two — permanent motion blur that read as spinning far too fast. Now a TRAPEZOID: each frame holds at full strength across the middle of its slot and blends only over the last 40%. HOLD must stay under 0.5 or the two active weights stop summing to one and the ball pulses; five samples per slot lands exactly on the plateau corners and both ramp ends, and because the sampled weights sum to one at every sample the interpolated sum is one between them too.
  MEASUREMENT NOTE for next time: do NOT chase the offline render's saturation number. That ball is rendered in a void; this one hangs in a blue-lit room with the station's own light on it, so a much higher on-screen saturation is correct rather than a fault. Compare HIGHLIGHTS (share above 200) and the fills' own saturation instead — those are like for like.
- PROTOTYPE 2026-07-31 (THE FLIPBOOK — mirrors that actually turn, OTA, switchable): owner picked this from five proposed options. `src/components/MirrorBallFlipbook.tsx`, wired into DiscoBallMode behind `const FLIPBOOK = true` (one line to fall back to the shipping ball). THE TRICK: a mirror ball is regular in longitude, so rotating the grid by exactly one tile's width maps the tile SET back onto itself — six frames spanning that one tile-width therefore loop forever with no seam, and the mirrors genuinely move between them. Six grids are built once and never touched; the only animation is which is showing, i.e. opacity on six views, cross-faded (triangular weights summing to 1, so the ball never dims between frames) and derived from `spin` itself via a multi-segment interpolation so the frames stay locked to the ball even under a finger drag.
  WHAT IT FORCED, and it is an improvement not a compromise: the shipping ball keys each tile's brightness to its IDENTITY (hash of j,k), which cannot survive the wrap — tile k lands where k+1 was and would arrive with the wrong brightness. Brightness here is a function of where the mirror POINTS: reflection direction r = 2(n·v)n − v, sampled against smooth 3D value noise (the room) plus three sharp specular lobes (the lamps). Neighbouring mirrors point ~11° apart and reflection doubles that, so they land in different noise cells and come out wildly different — the dark-beside-bright checkerboard that sells chrome — while a single mirror changes SMOOTHLY as it turns, which is what stops six frames strobing. MirrorFlash is not rendered under the flipbook: faking exactly this was its only job.
  MEASURED, 14-frame burst over ~6s, temporal std per pixel inside the ball: flipbook median 10.08 / 69.1% of pixels changing >8 levels, vs 8.17 / 51.1% for the shipping ball as it stands and 9.90 / 61.3% for the old version WITH the rotating meridians. So it beats the wire-cage version without drawing a single line, and static vertical-edge energy falls 0.85 → 0.23 (nothing is pinned any more). COST: 1846 paths vs the shipping ball's 2368, 56 SVG roots vs 77, and 6 animated views instead of 26 — cheaper on every count. TRADE the owner is judging: grid drops from 25x48 to 17x32, so the mirrors are chunkier. Side-by-side GIF built with scratchpad/landscape/gif.mjs (clip-cropped Playwright frames assembled with PIL).
- DONE 2026-07-31 (four owner fixes: tagline, one gutter, Horizon's size, meridians off, OTA): (1) Night Run's tagline "Empty expressways. Violet dashboards." → "Blue-lit dashboards." — the station went jet blue, so the copy had stopped describing it. (2) ONE PAGE GUTTER. Cruise and Modes sat on 22, Stations on 20 and the floating tab bar on 16, so the bar's edges hung outside every page's text column — that is what "the nav panel isn't central on the page titles" was (owner, 31.07; the pill itself measured dead symmetric on her screenshot, 48px each side, so the asymmetry was never within the bar). New `PAGE_GUTTER = 20` in theme.ts, used by all four. Verified by measuring the pill's edges against the titles on the real renders. (3) HORIZON WAS BEING MAGNIFIED, not drawn too big. Its portrait scene was a fixed 360x460 viewBox with `preserveAspectRatio="slice"`, so on a 393x852 screen the scale is max(393/360, 852/460) = 1.85 — the sun blown up to ~84% of the width and only 59% of the drawing's width visible. Worse, the Svg was sized winW x winH inside the middle flex slot, so it OVERFLOWED its box, and that overflow was the only reason the scene appeared behind the controls at all. The owner likes it behind the controls, so that is now deliberate: the portrait scene is a full-bleed absoluteFill layer like the landscape one, with `geomPortrait = makeGeom(winW, winH)` so the viewBox matches the screen and nothing is cropped or magnified. Sun radius falls out at min(W*0.239, H*0.20) ≈ 48% of the width, the stars spread across the real width instead of being cropped, and the sky above the sun stops being empty. (4) ROTATING MERIDIANS REMOVED from the ball (owner: "try the mirror animating without the vertical lines") — on device the drawn verticals read as a wire cage over the chrome. `RotatingMeridians`/`Meridian` are kept in the file, unused, because this is a straight one-line swap back. MEASURED both ways with a 14-frame burst over ~6s (scratchpad/landscape/burst.mjs), temporal std per pixel inside the ball: WITH meridians median 9.90 and 61.3% of pixels changing >8 levels; WITHOUT, median 8.17 and 51.1%. So the ball still visibly changes — the flashing-mirror layer from round 13 is carrying it — but the specific "the grid is turning" cue is weaker (travelling vertical-edge energy 4.33 → 1.56). If the rotation stops reading on device, put the one line back.
- DONE 2026-07-31 (modes-page ball — a REAL sphere at last, OTA): third attempt, and the first two failed for the same reason. The thumbnail was a square lattice squeezed toward the edges: perfectly straight horizontal rows, perfectly straight vertical columns. Round 1 added foreshortening, round 2 added Lambert shading off each tile's own normal — both correct, both irrelevant, because no sphere has straight rows and the eye reads the GRID before it reads the light (owner: "this 2D mirror ball is not cooperating"). LESSON: curvature has to be in the geometry; shading can only support it. Each mirror is now a genuine latitude/longitude quad on a unit sphere with a ~17° downward viewing TILT, back-face culled — so latitude rows project to arcs that dip toward the viewer and columns converge at the pole. That is the full mode's construction; the mode keeps its own tilt near zero because it TURNS and has lighting to sell it, but a still picture needs the tilt to say "ball" on sight. TILT IS NEGATIVE — we look UP at the ball, because that is how you meet one, hanging above you (owner, 31.07). Two things follow and both are load-bearing: the rows bow UPWARD across the front, and the pole facing us is the BOTTOM one, so the fitting that covers the column convergence sits at the bottom (drawn as a squashed ellipse, inside the clip) while the stem simply runs to the top of the silhouette and stops — looking up, the top fitting is on the far side of the sphere. ROWS/COLS 13x24 at hero size, 8x14 at 62px (checked by temporarily demoting the hero — the ball is normally the hero, so the small path is otherwise never rendered). Each quad is shrunk 0.90 toward its own centre so the dark body shows between mirrors: the GAP is the grid. Kept from the earlier rounds: Lambert + end-weighted brightness scatter, the one shared facet gradient (bounding-box units, so a single def rescales to every quad), the offset shadow crowding the lower-right limb and the key highlight at the light. NEW: a dark cap at the top pole — a real ball has a fitting there, and it covers the one place this projection looks busy, where the columns converge.
- DONE 2026-07-31 (Night Run goes blue everywhere, gold premium chip, and DAYLIGHT, OTA): (1) JET BLUE STATION-WIDE — the 31.07 orb-only override is DELETED and `night-run`'s own colours moved instead: eqColors `['#C6ECFF','#2E7DFF','#1340E6']`, plus gradientColors/cardGradient/glowColor/iconBg off violet, because a blue mode over a purple card is worse than either. NOTE LEFT FOR THE OWNER: the tagline still reads "Empty expressways. Violet dashboards." — her copy, so not touched. (2) PREMIUM CHIP under the profile name now wears `PREMIUM_GOLD` (#F7B733, the upgrade card's eyebrow) rather than the app violet; FREE PLAN keeps the accent. (3) DAYLIGHT — new setting (`cruise_daylight`, default OFF) on MotionContext with `useDaylight()` shorthand, plus a Profile toggle that sits directly above Auto-dim and HIDES it (a toggle that cannot fire is worse than no toggle). DELIBERATELY NOT A WHITE THEME, and this is the reasoning to keep: in sun the problem is not that the app is dark — black behind white type is the highest contrast a phone can make and the cheapest on OLED. What vanishes outdoors is everything drawn at half strength. So Daylight lifts the quiet things and deepens the scrims UNDER type: `StationBackdrop` gains a 22% veil over the photograph (one component, so all 8 modes + the hero + the landscape deck at once), StationIdentity's eyebrow/dial/name go to near-full white, ModeActionRow's glass pills go opaque with a 0.42 rim, LandscapeChrome's panel wash deepens and its artist line brightens, and the Stations list lifts its dial numbers (0.42→0.86), names, chevrons and hairlines. Auto-dim stands down entirely — dimming with the sun on the screen makes the driver reach for the brightness slider, which costs more battery than the dim ever saved. WHAT IT DOES NOT DO: the modes themselves are still dark cinematic scenes; if the owner wants genuinely light LIST pages that is a much bigger piece of work and a different product decision. Verified with a seeded `cruise_daylight` localStorage entry (the same Playwright trick used for custom stations) — off/on compared side by side on the Stations page and inside a mode.
- DONE 2026-07-31 (band letters + a jet-blue orb, OTA): (1) SEVEN SEGMENTS CANNOT DRAW AN M. The station page's dial line set the whole string in DSEG7, and band letters inherit the parent Text's fontFamily, so "94.7 FM" came out "94.7 FN" (owner screenshots). A seven-segment cell has no diagonals and no vertical centre bar — the glyph does not exist to be fixed. Band letters now take DSEG14, which does have them, and which the Stations page's own band headers have always used. `useDsegFont()` is now a wrapper over `useDsegFonts()` returning `{seg7, seg14}` from one `useFonts` call; every future caller that prints a BAND rather than a NUMBER must reach for seg14. Only StationDetailModal printed one (StationIdentity carries the band inside the station's plain-font name), so that is the only site changed. Note the `loaded` flag now gates both faces, so a missing DSEG14 asset would drop the numbers to mono everywhere — both ttfs are committed under assets/fonts. (2) CIRCULAR EQ — new `ORB_PALETTE` in CircularWaveMode overrides the station's eqColors for THIS MODE ONLY; `night-run` is jet blue (#C6ECFF → #2E7DFF → #1340E6). Why an override rather than editing the station: eqColors is a three-stop ramp tuned for bars and mirrors, and the orb maps the LAST stop onto the ring's whole outer edge, so Night Run's magenta was carrying a station whose own photograph is blue. The station keeps its cyan/violet in all seven other modes. Order is centre → out, matching the cwStroke gradient's 0.5/0.75/1 stops. First ramp landed the tips in near-navy and read dull against the backdrop; the shipped one keeps them luminous.
- DONE 2026-07-31 (owner round: six asks off the device, OTA): (1) CD WIND MARKER — the disc's rotational scrub now shows the record's centre pill (`▶▶ +7s`), same shape, position and 1s-then-fade timing, in the STATION's colour rather than the vinyl's gold so it belongs to that deck. A tap still bypasses it entirely (the pill is set straight to 0), because a tap must not go through scrub.end. (2) TONEARM CARTRIDGE — the headshell was rotated -18°, and since the pivot sits top-right while the shaft hooks LEFT toward the spindle, a negative SVG angle kicked the cartridge back OUT and broke the J. It is +18 now (`HEAD_TILT`), which also matches how a real arm is built: the headshell's offset angles the stylus in toward the spindle. (3) STATIONS PAGE — the Create pill is the app's primary button (solid white, dark type, a `+`); as grey glass on a photograph it read as chrome rather than the thing to press. The CREATE SHEET's preview is no longer a violet-rimmed card: the stations page is a printed list now, so the preview is an actual ROW off the dial — number, name, MINE chip, the chosen colour on the icon, chevron, hairlines — with the tagline as a caption beneath (it doesn't appear on the dial). The dial NUMBER IS REAL: `custom-<Date.now()>` is settled when the sheet OPENS rather than at save time, so the number previewed is the number the station gets. CTA went from a slab in whatever colour the user had just picked (the button changed identity as you scrolled the swatches) to the white primary pill. Column widths and type sizes are copied from stations.tsx — if they drift the preview stops being one. (4) MODES-PAGE BALL — foreshortening alone still read flat, and it would: the old shading was a RADIAL dim, which is symmetric, and a symmetric dim is a vignette on a disc — it says nothing about where the light is. Each tile is now lit by its own surface normal: under an orthographic view a sphere's normal falls straight out of screen position (nz = √(1−nx²−ny²)), so Lambert against a light up/left/slightly-front gives a real lit side, terminator and shadow side. Plus per-facet gradient (ONE shared def — SVG gradients are in bounding-box units so they rescale per tile), scatter pushed to the ends of its range, square-cut tiles (the rx rounding read as candy), N=15 at hero size and 10 at row size, and the key highlight moved to where the light actually is. The square LATTICE stays — a lat/long projection is truer but its pole tiles read as noise at 62px. (5) MIRROR BALL PORTRAIT KEEPS ITS CONTROLS — portrait resting was this mode's own invention and no other mode does it, so it was the one screen where the controls vanished while you held the phone normally. The rest timer is now gated on `isLandscape`; the landscape deck still docks and rests, which is the agreed grammar every mode shares. (6) BIGGER — CD case winH*0.86→0.94 (cap 350→384) sideways and winW*0.93→0.97 / winH*0.44→0.47 (cap 410→430) upright; Equalizer meter winH*0.60→0.68 sideways with the docked scale 0.62→0.68, and VIZ_H 0.26→0.285 upright. Checked at 667pt, where the upright meter is the binding constraint.
  CHECKS AFTER: tsc clean; the 8-mode rotation sweep (portrait → landscape → rest → wake → portrait) with zero page errors, zero misfit haze canvases and no leaked transforms; all five pages plus the create sheet and the CD at 393x852 AND 375x667 with zero page errors; and the rotational scrub driven for real with a circular drag on both the disc and the record.
- DONE 2026-07-30 (ROTATION ROUND 5 — the two cut-offs, and a rotation smoothness sweep, OTA): owner on device: "a cut off in the equalizer and in the mirror mode... it looks like it's from the atmosphere", plus the vinyl left half off the screen in portrait, plus "run checks on the smoothness for the rotations". TWO ROOT CAUSES, both found by measuring her screenshots rather than eyeballing, and both a whole CLASS of bug rather than one-offs.
  (1) THE ATMOSPHERE CUT-OFF — `AmbientGlow`'s `Haze` was `<Svg width="100%" height="100%">` with `50%` geometry inside, the ONLY percentage-sized SVG in the app. A percentage canvas is resolved natively and does NOT re-resolve when the window changes shape, so after a turn the ellipse was still being drawn at its PORTRAIT proportions inside a view that had already re-laid-out to the landscape box — and the view clips, so the haze was sliced off in a straight horizontal line. PROOF, and reuse this method: the step is ONE pixel with a ~3× brightness ratio, so it is a layer ending, not a gradient. Solve for which layer: on her 926×428 screen the portrait hero canvas is 582×463 anchored at the landscape box's top-left, which puts the ellipse's right edge at x=0.448 (measured: step reaches zero at ~0.45) and the clip at y = 141 + s·107 for breath scale s∈[0.92,1.1] → 0.560..0.605 (measured: 0.568 on Mirror Ball, 0.604 on Circular EQ — the same layer at two breath phases). Nothing about the INTENDED geometry fits those numbers; the stale-canvas geometry fits both axes to a couple of percent. FIX: every haze now gets a real pixel width/height and draws through a `0 0 100 100` viewBox with `preserveAspectRatio="none"`, so the canvas is a prop that changes on rotation and the drawing no longer depends on how any percentage resolves; the layer is also keyed on orientation so a turn rebuilds it outright. Boxes moved from left+right and `%` tops to left+width/top+height in px — arithmetically identical, just concrete. Swept the same treatment through the two other percentage canvases (Mirror Ball's UnderGlow, Horizon's scene — both had a viewBox so they could only stretch, not clip, but `slice` cropping for the old screen is the same failure). RULE: never size an `<Svg>` in percentages in this app.
  (2) THE VINYL CUT-OFF — `isLandscape ? deckScene : null`. A native-driven transform lives on the NATIVE view; dropping it from the style prop sends no transform at all, so the view simply KEEPS the last one it was given — the record stayed shrunk to 0.86 and pushed −0.2·winW left, forever, in portrait. FIX: `useDeckScene(chrome, winW, scale, active)` — portrait flattens the interpolation to identity instead, and every mode now renders the style unconditionally. Never write `isLandscape ? <animated style> : null` again, in this codebase or any other.
  SMOOTHNESS SWEEP (all 8 modes, portrait → landscape → rest → tap-wake → portrait, in the real web build; harness scratchpad/landscape/sweep.mjs, which also dumps each haze's canvas-vs-box fit and every non-identity transform on the page): zero page errors, no misfit canvases, no leaked transforms. It also caught four real things. The arrival flashed one frame of the deck glide because the turn re-renders as landscape while `chrome` is still parked at 1 — the reset now runs in a LAYOUT effect (`useIsoLayoutEffect`, exported from LandscapeChrome; Mirror Ball's own re-dock effect uses it too). Vinyl's rotational scrub called `evt.target.measure(...)` unguarded (threw on web, and a throw in a grant handler kills the gesture) and BOTH Vinyl and CD anchored the gesture's starting angle before the async measure answered, so the first move after the centre had moved — i.e. right after a turn, or mid-glide — jumped; the starting angle is now re-anchored inside the measure callback. Tuner still slid off by the module-load PORTRAIT height, so sideways it exited 2.2× further than the screen and the last half of the 320ms was blank. Cassette's 400-dot grain field was seeded against portrait dimensions, so sideways almost all of it fell below the screen and what showed was crammed into the left half. WakeSpotifyHint hung at `insets.top + 64` full-width, which sideways lands across the object — it now tucks into the top strip of the left pane, past the chevron and short of where the panel docks.
- DONE 2026-07-30 (LANDSCAPE round 3 — the DOCKING DECK replaces the bottom strip, OTA): owner, after living with the L1 grammar on device, picked "Option C" from three prototyped panel behaviours (fixed deck / sleeps-in-place / docking; scratchpad/landscape/options.js): AWAKE = an L2 head-unit panel docked on the right (DECK_FRAC 0.40 of the screen) with identity, title, panel-width seek, transport and pills — the scene sits in the left pane clear of every control; AT REST (6s) = the panel slides off the right edge and the scene glides to centre stage at FULL SIZE; tap anywhere docks it back to the same fixed spot (driver muscle memory). WHY: L1's rest state inherited the awake spacing (object high + small over dead space), and its seek bar was a full-screen reach. MECHANICS: same `chrome` value drives everything — panel translateX (slide replaces fade; wake now 260ms, teleporting panels read wrong), and NEW `useDeckScene(chrome, winW, scale)` gives each mode's scene a transform-only glide (translateX −DECK_FRAC/2·winW + scale). Per-mode scales: objects 0.86 (default), EQUALIZER 0.62 (full-width meter must fit the pane) and its meter now anchors LOW (bottom: 44 — owner: "not dead centre", a meter rises from a baseline), HORIZON 1 (slide only — shrinking a full-bleed scene reveals its edges; the sun lands at the left pane's centre, panel covers the rest). Disco's ROOM stays full-bleed, only the ball group glides. `LS_CHROME_CLEAR` is deprecated (nothing spaces off the bottom any more). Verified full cycle in the web build (awake → rest → tap re-dock), zero page errors. WATCH ON DEVICE: Vinyl/CD rotational scrub measures the object's centre — the deck transform may offset touch mapping while the panel is docked.
- DONE 2026-07-30 (Mirror Ball round 23 — the premium-detail brief, OTA): owner sent an Apple-style reference sheet with a full spec (micro-bevels, chrome variance, speculars, bloom, under-glow, white-dominant lighting) and — crucially — said NOT to adopt its per-station full recolours, which VALIDATES the round-20 chrome architecture outright (silver material, mood arrives as light only). Deltas, all additive within the measured rules: (1) MICRO-BEVEL: a third path per visible tile referencing ONE shared vertical gradient `dbLip` (lit top lip 0.8→0, shadow bottom lip 0→0.65) — horizontal edges ONLY (vertical edges are the round-12 static-seam trap), opacity jittered per tile (uniform lips read stamped), skipped where depth<0.35 (silhouette slivers). (2) `KeySpecular` — one soft breathing radial at the key light's upper-left, 0.10–0.22 opacity on a ~8s cycle, gated on `lit`; all falloff, never a hard ellipse (round 4). (3) `UnderGlow` — the mood-tinted pool beneath the ball (light, not material), lit-gated, all-gradient. (4) ColourReflections grew a SIXTH patch that is deliberately NEUTRAL cool-white (ambient room light) at a lower peak, so mood colour always sits beside plain light. Composite measured after: median 121, >200 share 16.4% — inside the round-18/19 approved band, no rebalance needed. Everything else on the brief (multiple soft sources, rays, flares, particles, shimmer, sparkle cadence) already existed from rounds 18–22.
- DONE 2026-07-30 (LANDSCAPE round 2 — the other five modes + a readiness gate, OTA): owner on device: "a lot of them do squish, the mirror ball works the best" — the un-built modes were rendering their portrait columns sideways. (1) NEW GATE: `LANDSCAPE_READY` set in orientation.ts — OrientationGate only unlocks rotation for modes listed there; a mode without a landscape composition stays portrait (squish is worse than upright). TUNER is the one holdout — the dial deserves its own sideways design round. (2) CASSETTE/VINYL/CD/CIRCULAR-EQ/HORIZON all got the L1+L3 treatment via the same template as round 1: hero sized off HEIGHT alone (the portrait formulas' winH*0.44-ish terms are what shrank everything), portrait chrome behind `!isLandscape`, LandscapeChrome + useChromeFade + root wake sniffer, AmbientGlow gated out (portrait-derived smoke geometry). Cassette's stale early-dev landscape branch (two columns, FAKE track list, Play-on-Spotify button — it shipped by accident the day rotation unlocked) is DELETED. (3) LandscapeChrome grew an optional `seekBar` node prop — Vinyl predates the shared clock and passes its own ScrubProgressBar; progress/scrub are now optional. (4) HORIZON WIDE GEOMETRY: slice-cropping the portrait viewBox into a wide window blew the sun up 2.4x (only its midriff showed). `makeGeom(W,H)` now derives the whole composition (horizon line, sun off the short edge, stars across the real width, slats scaled with the sun) — GEOM_PORTRAIT is bit-identical to the old constants, GEOM_WIDE (800x370) feeds the landscape's full-bleed scene. TRAP hit twice this round: inserting a hook anchored on the playlist-effect line can land BEFORE `isLandscape` is declared — declare it at the useWindowDimensions line, not at the sizing line.
- LANDSCAPE TRAP FOUND ON DEVICE 2026-07-30, FIXED SAME DAY (OTA): rotation unlock alone does NOT rotate the modes — every fullscreen mode presents inside an RN `Modal`, and on iPhone `RCTModalHostView.supportedOrientationsMask` returns PORTRAIT-ONLY when the `supportedOrientations` prop is absent (verified in node_modules/react-native/React/Views/RCTModalHostView.m), independently of the app-level mask. Symptom: fade worked, screen never turned. FIX: `supportedOrientations={['portrait','landscape']}` on all 8 mode Modals AND every Modal that can present during a drive (AutoDim's catch layer, PlaybackNotice, DriveCheckCard, ShareCardSheet) — a portrait-pinned Modal presenting OVER a landscape mode snaps the whole screen upright. Page-level modals (CreateStation, PlatformSelector, Settings, StationDetail, ThemeGenerator) deliberately left portrait. ANY NEW MODAL that can appear mid-drive must carry this prop. Also verified from expo-screen-orientation's own Swift: JS `OrientationLock.DEFAULT`(0) → native allButUpsideDown, and the registry mask from lockAsync OVERRIDES Info.plist at the root VC — so runtime rotation works despite app.json "orientation": "portrait", no fresh build needed.
- DONE 2026-07-30 (LANDSCAPE round 1 — rotation + Mirror Ball & Equalizer, OTA): owner picked "L1+L3 together" from three prototyped directions (scratchpad/landscape/build.js — HTML mocks at 852x393 with real station photos + the ball cropped out of ball/room.png; L2 "Deck" split-screen REJECTED). The grammar: full-bleed scene, portrait furniture redistributed along the bottom (title left, pills right, seek across, transport centred, chevron+eyebrow top-left), and after 6 untouched seconds of playback ALL chrome fades to a pure scene with a whisper of the station name — the Mirror Ball's portrait chrome-fade generalised. PLUMBING: `src/utils/orientation.ts` (defensive wrappers over expo-screen-orientation — module is in EVERY build since 08.07, so this whole feature ships OTA) + `OrientationGate` in NowPlayingHost: rotation allowed ONLY while a mode is expanded, PORTRAIT_UP everywhere else; closing a sideways mode snaps the app upright. The list pages never rotate — deliberate, matches Apple Music. SHARED: `src/components/LandscapeChrome.tsx` = presentation (`LandscapeChrome`) + fade machinery (`useChromeFade` — gate read through a ref, the wake callback lives in a once-built capture sniffer) + `LS_CHROME_CLEAR` (px the awake chrome needs; scenes keep their object above it). The wake MUST be `onStartShouldSetResponderCapture={() => { wake(); return false; }}` on the mode's ROOT — an absoluteFill box-none overlay cannot reliably see taps on the scene beneath. ModeActionRow slots in via its `style` prop (zero out marginTop/paddingHorizontal/alignSelf). EQUALIZER: the ORIGINAL early-dev landscape branch (two columns, own VolumeSlider, safety banner, dim button — never shipped, app was portrait-locked, predated every shared piece) is DELETED along with VioletProgressBar/VolumeSlider/platform-row state; new branch = full-width meter standing at bottom: LS_CHROME_CLEAR above the chrome. MIRROR BALL: reuses its OWN chrome/wakeChrome machinery for the landscape overlay (one timer, one behaviour); ball sizes off height alone in landscape (winH*0.56 cap 280 — the portrait formula's winH*0.39 makes a grapefruit) and the flex box's paddingBottom is LS_CHROME_CLEAR*0.54 (verified at 852x393: less sinks the ball into the seek bar). TRAP: AmbientGlow is gated `!isLandscape` — its smoke geometry derives from PORTRAIT screen height at module load, so in a 393-high window the haze starts below the screen; running it is invisible cost. Verified in the real web build at 852x393 + portrait regression shots, zero page errors, full rest→wake cycle. REMAINING 6 modes (Cassette/Vinyl/Tuner/Horizon/CircularEQ/CD) follow the same recipe after the owner approves these two on device; Tuner and Cassette will want their own scene compositions, not just the overlay.
- DONE 2026-07-30 (owner tweak round from device screenshots — icon, purple settings, thumbnails, paywall): (1) APP ICON logo enlarged 65% → 80% of the tile (it sat visibly smaller than Spotify's mark on the home screen) — PIL crop/scale/recentre on the same #0e0e14 background; NEXT-BUILD ONLY, icons bake into the binary. (2) SETTINGS DE-PURPLED: SettingsPageShell's navy-violet gradient → flat #0a0a10 (the Profile page's black); section cards to the Profile card recipe (white 0.04 + hairline 0.12); Refer-a-Friend card was a violet gradient slab with a glowing violet button → black glass + WHITE pill button (the app's primary-button language); name input violet → white. Violet survives only on Switch accents. (3) ModeThumb fixes: HORIZON sun was BUILT from bands with gaps so the circle came out shredded at row size — now one whole Circle with 4 thin dark cuts clipped to the chord (silhouette must be one unbroken shape, slats are texture); grid verticals converge AT the horizon. DISCO thumb: sphere-projection replaced by the PAYWALL ball's recipe (uniform square grid + brick bond, ClipPath circle, frozen light band, top-left catch, radial dark rim, stem) — owner preferred it outright; projection pole tiles read as noise at 62px. (4) PAYWALL: CD scene ADDED (the copy said "5 Premium Visual Modes" over four previews) — disc + 12 narrow diffraction petals rotated-then-translated so they occupy the outer ring (6 wide ones crossing the centre = pinwheel star, then gem facets; both rejected on burst screenshots) + a sheen bar riding the spin; HORIZON scene's rays now pivot ON the horizon line (height 2×ground, top −ground) so the fan converges at a true vanishing point — the old mid-ground pivot made the lines cross in an X below the sun ("the vertical lines are kind of awkward"). Burst-screenshot method for the showcase: clip to the card, shoot every 2.1s across the 21s cycle, tile into a contact sheet.
- DONE 2026-07-30 (device round 2 — tuner feel, cassette colour, mode chips, station identity, curved ball, OTA): (1) TUNER loosened: BAND_CFG px 110→72 (FM) / 2.2→1.45 (AM), lock window 0.45→0.65 / 25→38 — a third less thumb travel between stations and the needle grabs from further out. (2) CASSETTE shell now wears eqColors[1] (the app-wide accent slot: mini-player edge, glow bands) instead of eqColors[2] — Sunset's cassette was hot pink on an otherwise-orange station; Night Run shifts violet→blue as a side effect (the accent-slot rule). (3) ModeSheet chips DE-SLABBED: MODE_LOOK's per-mode gradients deleted, chips are dark glass (white 0.06 + hairline 0.14) with the ACTIVE one a solid white pill with dark type; MirrorBallGlyph grew a `color` prop solely so it can go dark on that white chip. (4) STATION IDENTITY: new `src/components/StationIdentity.tsx` — "(dial no. in DSEG7) (name)" under the YOU'RE LISTENING TO eyebrow, one component in all 8 mode headers (swapped mechanically; TRAP: an import-inserter that checks `'X' not in src` AFTER splicing X's JSX will always skip the import) + LandscapeChrome (compact/left) + ShelfCard captions (nested Text carries the 7-seg face) + StationDetailModal (dial line above the 40pt title). Also exports `useDsegFont()` + `stationDisplayName()`. Names already end AM/FM; custom stations get the band appended. (5) MODES-PAGE BALL CURVED: the uniform grid read flat, so the lattice is now warped through `c + R*sin(u*π/2)` on BOTH axes — squares foreshorten into slivers at the silhouette (plus per-tile shade falloff), which is the curvature cue; poles never pinch because it's a square lattice, not the lat/long projection that sank the first thumb. All verified in the web build; zero page errors.
- REJECTED BY APPLE 2026-07-29, FIXED SAME DAY (build 7, submission c3094be4-98c0-4bac-be68-e51408b78f8c, reviewed on an iPad Air 11-inch M3): **Guideline 2.5.4 Performance — Software Requirements**. "The app declares support for audio in the UIBackgroundModes key... but we are unable to play any audible content when the app is running in the background." CORRECT AND OUR FAULT: Cruise FM never plays audio itself — Spotify does — so it has no business claiming background audio. CAUSE: nothing in app.json declared it; the **expo-audio plugin** adds it automatically (`node_modules/expo-audio/plugin/build/withAudio.js` pushes 'audio' into UIBackgroundModes). expo-audio was installed 20.07 for mic-reactive visuals, those were REMOVED 22.07, and the plugin was deliberately kept only to protect the OTA fingerprint — i.e. this rejection was caused by the one piece of dead weight already sitting at #4 on the NEXT FRESH BUILD list. FIX: plugin removed from app.json AND `npm uninstall expo-audio` (nothing in src/ imported it — verified). That also removes the unexplained microphone permission from the store listing, which was the other reason it was queued. VERIFIED with `npx expo config --type introspect`: no UIBackgroundModes, no NSMicrophoneUsageDescription anywhere in the resolved iOS config. This is a FRESH BUILD event by definition (app.json + package.json both changed), which is fine — a rejection requires a new build to resubmit anyway. LESSON: an Expo plugin can inject Info.plist keys you never wrote; before any submission, run the introspect command above and read what Apple will actually see.
- NEXT (owner, 26.07 sign-off): "polish on those card artwork" — pick this up first next session. Ask WHICH cards they mean before starting: the Stations-page station cards (smoked glass just landed, owner said "keep the cards for now, might change them later") or the share card's per-mode artwork. The four-direction mockup harness is worth rebuilding for either — see the smoked-glass entry above for how it was made.
- MODE LINE-UP DECISION (2026-07-25, owner): supersedes the 22.07 freemium split. Retire SOUND WAVES (`waves`) — it only ever existed as the redundant-with-Equalizer giveaway, and the bar moved once Vinyl and Disco Ball got their detail passes. Add CD as a PREMIUM mode; promote CIRCULAR EQ (`orb`) to FREE in Sound Waves' place — same "second EQ is the right giveaway" logic, but a better-looking mode, so the free tier gets an upgrade while the newest work stays a paid draw. Resulting split: FREE = Cassette, Equalizer, Circular EQ. PREMIUM = Vinyl, Tuner, Horizon, Disco Ball, CD. TRAP when removing a mode: `lastCruise` (and the Modes tab / per-station selections) store the mode as a BARE STRING, so deleting `waves` strands anyone whose saved cruise points at it — add an unknown-mode → Equalizer fallback in the same change. Cheapest moment to delete is BEFORE public release (nobody has it saved); build on the preview channel only while iOS is in review.
- DONE 2026-07-25 (CD MODE BUILT + line-up change, OTA): `src/components/CDMode.tsx`, premium, id `cd`, icon `disc` (verified in the MCI glyphmap). Album-as-object per the references: mirrored disc behind clear jewel-case plastic (hinge spine + clips, corner brackets, hub rosette, micro-text ring, hairline scratches, dust). LAYERING IS THE WHOLE TRICK and it's the Disco Ball lesson applied up front: the iridescent fan is STATIC (diffraction belongs to the light, not the plastic) and only the printed label, wear and hub turn; the label sits ABOVE the fan because a printed face is matte and rainbow streaking across the artwork kills it. Album art turning with the disc is also what makes the rotation legible at a glance — the exact failure that cost four rounds on the ball. Fan = 50 overlapping low-opacity wedges (heavy overlap or they read as spokes); concentric data rings are rotation-invariant so they stay in the static layer. Rotation reuses the clock-derived phase (never reads back from the animation, so it cannot stall), 3.4s/turn vs Vinyl's slow drift, swipe-to-scrub. Built by prototyping offscreen first, then verifying by transpiling the component's OWN builder functions with `ts.transpileModule` and rendering their real output — reuse that trick, it checks the shipped geometry rather than a lookalike. LINE-UP: Sound Waves retired + file deleted, Circular EQ promoted to FREE in its slot, CD added premium. Free = Cassette/Equalizer/Circular EQ; premium = Vinyl/Tuner/Horizon/Disco Ball/CD. Added `knownMode()` in modeCatalog so a saved cruise naming a retired mode falls back to Equalizer instead of resuming into nothing.
- CD MODE design settled 2026-07-25 (round 2, owner reference = a Rumours CD in a clear case): disc is TRANSLUCENT (the drive shows through — that's what makes it read as an object), album art is GHOSTED faintly across nearly the whole face with a pewter wash over it (etched, not a printed label) and it doubles as the rotation cue, hub is small per the reference (0.155R centre hole, 4 gripper holes at 0.235R, hub ring 0.31R, stacking ring 0.62R) — the first cut's 0.52R printed label was the "inner dark circle too big" complaint, and with no track it fell back to a solid dark half-disc. Case dimension comes from PAIRED strokes: lit outer edge + dark line just behind + faint inner highlight = moulded plastic. TRAP: diffraction opacity is coupled to disc transparency — 0.30/0.22 is subtle over opaque silver but goes psychedelic over a transparent disc; it's 0.20/0.13 now. Still open: jewel case (shipped) vs dash slot-load framing, which is arguably more on-theme for driving. Do NOT commit the reference images: third-party album art, public repo.
- NEXT (post-launch, AMBITION — agreed 2026-07-25, and deliberately still inside the driving theme): (1) BREAK THE MUSIC CEILING FIRST. Spotify's 5-user dev quota means that for essentially every real user the visuals are disconnected from their actual playback — handoff mode, station tagline instead of song titles. Everything else is built on sand until that's fixed. Apple Music via MusicKit has no equivalent quota gate and works natively on the platform we launch on, so it is the single highest-value post-launch build, ahead of any new mode. (2) MAKE THE MOOD LIVE. The modes are a skin over someone else's player and could be copied; the taxonomy ("organises by how a drive feels") can't. Ambitious version = stations that respond to the actual drive — time of day, length, motion, weather — so it stops being a visualiser and becomes a category nobody occupies. (3) GIVE IT A REASON TO SPREAD. Nothing currently leaves the phone. A POST-DRIVE recap (route, songs, mood) rendered in the app's own visual language turns each drive into something shareable — growth with no ad budget. Post-drive only, never mid-drive; it must not become a reason to touch the phone while moving. NOT ambition: a tenth visual mode. Fewer and better beats more — six modes worth screenshotting beats nine that aren't.
- NEXT (post-launch): realism detail pass on other modes — owner loved the vinyl hardware round (album-art label, silver J tonearm w/ counterweight/dial/cartridge, groove-riding needle, 2026-07-23) and wants the same treatment elsewhere. Cassette is DONE (24.07). Remaining tail by the new bar: HORIZON and CIRCULAR EQ have never had a detail pass — bring them up to Vinyl/Disco standard or retire them the way Sound Waves is being retired. Reference-image-driven, SVG-drawn, all OTA-able.
- NEXT (post-launch): Apple Music support via MusicKit (platform concept already in code); Google ID verification clears → upload build, £1.99 product in Play Console, swap test_ key for goog_ key → closed test (12 testers/14 days); iOS home-screen widgets (owner asked 2026-07-23) — order: Start Drive widget (station-art tile deep-linking into last station/mode) → drive-stats/streak tile → "Tonight's pick" card → Live Activity during drives (lock screen/Dynamic Island, genuinely live). All native (Swift via config plugin, e.g. @bacons/apple-targets), EAS cloud-buildable without a Mac, fresh build per iteration (no OTA). NOTE: live Spotify now-playing in a widget is NOT feasible (no Apple Music-style access + ~15min refresh) — don't promise it
- DONE 2026-07-26 (ship from the phone): `.github/workflows/ota-update.yml` — a MANUAL GitHub Actions button (`workflow_dispatch` only, never on push, so the owner still decides what ships during launch) that runs `eas update` for a chosen channel. Owner presses it from the GitHub mobile app / browser, no PC needed. REQUIRES a repo secret `EXPO_TOKEN` (expo.dev → Account settings → Access tokens → robot/personal token). Before publishing it asserts EXPO_PUBLIC_SPOTIFY_CLIENT_ID/SECRET exist in that EAS environment — `.env` is gitignored so CI has no local copy, and a bundle published without them would silently break Spotify sign-in for everyone on the channel. Workflow files are NOT a fingerprint source, so adding this did not disturb OTA. STILL PENDING (post-launch): making it fire automatically on every push.
